## obra/superpowers v5.1.0 framework mechanics (artifact enforcement, SDD vs inline, code review, subagent boundaries, persuasion meta-skill) and where Quantis (its fork) reintroduced bugs
- Superpowers enforces artifact production with a combination of (a) a wrapped <HARD-GATE> tag, (b) a 'You MUST create a task for each of these items' checklist-as-TodoWrite, and (c) a separate Iron Law mechanism in verification skills. The brainstorming skill specifically blocks ALL implementation until a design doc exists and is approved, and treats the checklist items as mandatory ordered tasks. — brainstorming/SKILL.md: '<HARD-GATE> Do NOT invoke any implementation skill, write any code, scaffold any project, or take any implementation action until you have presented a design and the user has approved it. This applies to EVERY project regardless of perceived simplicity. </HARD-GATE>'. And under '## Checklist': 'You MUST create a task for each of these items and complete them in order:' (9  [https://raw.githubusercontent.com/obra/superpowers/main/skills/brainstorming/SKILL.md] (relevance: Quantis already plans (Phase 3.1) to add a SPEC.md HARD-GATE to discuss-phase and a numbered Step 1.5 to verify. This matches the upstream pattern exactly: upstream uses ONE <HARD-GATE> on the brainst)
- The brainstorming skill has NO concept of 'modes'. Its terminal step (step 9) auto-invokes writing-plans and ONLY writing-plans. The Visual Companion is explicitly 'a tool — not a mode'. — brainstorming/SKILL.md: '**The terminal state is invoking writing-plans.** Do NOT invoke frontend-design, mcp-builder, or any other implementation skill. The ONLY skill you invoke after brainstorming is writing-plans.' Checklist item 9: 'Transition to implementation — invoke writing-plans skill'. Visual Companion section: 'Available as a tool — not a mode.' [https://raw.githubusercontent.com/obra/superpowers/main/skills/brainstorming/SKILL.md] (relevance: Confirms the established ground truth: discuss-phase.md's 'in discuss/explore mode' and stress-test.md's 'adopt adversarial critique mode' are INVENTED qualifiers with no basis in the upstream skill —)
- Superpowers does NOT decide SDD-vs-inline deterministically inside writing-plans — it presents a user-facing menu identical to the one Quantis inherited. The actual deterministic preference lives elsewhere: in executing-plans, which says to use SDD instead whenever subagents are available. — writing-plans/SKILL.md '## Execution Handoff': 'After saving the plan, offer execution choice: "Plan complete and saved to ... Two execution options: 1. Subagent-Driven (recommended) ... 2. Inline Execution ... Which approach?"'. The plan header template also hardcodes the ambiguity: 'REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans'. The [https://raw.githubusercontent.com/obra/superpowers/main/skills/writing-plans/SKILL.md] (relevance: This is a precise root-cause confirmation of the PENDING SCOPE DECISION. The 'ask the user which' ambiguity is NOT a Quantis bug — it is faithfully inherited from upstream v5.1.0 (Quantis writing-plan)
- Superpowers code review is layered: per-task TWO-STAGE review (spec-compliance reviewer FIRST, then code-quality reviewer) during SDD, PLUS one final whole-implementation reviewer after all tasks. The reviewer prompt explicitly frames the reviewer as a 'Senior Code Reviewer' and uses a Critical/Important/Minor severity rubric with a Ready-to-merge verdict. — subagent-driven-development/SKILL.md process flow ends with: 'More tasks remain? -> [no] -> Dispatch final code reviewer subagent for entire implementation -> Use superpowers:finishing-a-development-branch'. Red Flags forbid skipping or reordering: 'Start code quality review before spec compliance is OK (wrong order)'. The whole-feature reviewer template requesting-code-review/code-reviewer.md ope [https://raw.githubusercontent.com/obra/superpowers/main/skills/requesting-code-review/code-reviewer.md] (relevance: Directly informs the un-scoped ISSUE 5 ('whole-phase senior code review'). The upstream pattern is already exactly that: a final, whole-implementation 'Senior Code Reviewer' subagent runs AFTER all pe)
- Superpowers draws the subagent line on INDEPENDENCE and CONTEXT ISOLATION, not on 'subagents are better'. Parallel dispatch is reserved for independent problem domains with NO shared state; SDD implementers are strictly SEQUENTIAL (one at a time) because parallel implementers conflict on shared files. The framework guards against sprawl with explicit 'don't use when' and 'too broad' warnings. — dispatching-parallel-agents/SKILL.md: 'Core principle: Dispatch one agent per independent problem domain.' 'Don't use when: Failures are related / Need to understand full system state / Agents would interfere (editing same files).' subagent-driven-development/SKILL.md Red Flags: 'Dispatch multiple implementation subagents in parallel (conflicts)'. Anti-sprawl guidance: 'Too broad: "Fix all the tes [https://raw.githubusercontent.com/obra/superpowers/main/skills/dispatching-parallel-agents/SKILL.md] (relevance: Informs un-scoped ISSUE 7 ('broader subagent integration'). The upstream rule is: subagents for (a) per-task implementation+review in SDD (sequential), (b) parallel ONLY across genuinely independent d)
- The persuasion-principles/writing-skills meta-skill is the explicit engine for making instructions stick. It is grounded in cited research (Meincke et al. 2025, N=28,000; compliance 33%->72%) and prescribes Authority + Commitment + Social Proof for discipline skills, while BANNING Liking and Reciprocity. It pairs this with 'bright-line rules' and 'implementation intentions' (When-X-do-Y) to reduce rationalization. — writing-skills/persuasion-principles.md: 'Persuasion techniques more than doubled compliance rates (33% -> 72%, p < .001).' Authority example: 'Write code before test? Delete it. Start over. No exceptions.' Commitment: 'When you find a skill, you MUST announce: "I'm using [Skill Name]"'. Social Proof: 'Checklists without TodoWrite tracking = steps get skipped. Every time.' Table: 'Discipline-enfor [https://raw.githubusercontent.com/obra/superpowers/main/skills/writing-skills/persuasion-principles.md] (relevance: Gives Quantis a principled toolkit for ISSUE 5/6/7 enforcement: when adding the phase-level review gate or hardening stress-test depth, use Authority (YOU MUST / no exceptions), Commitment (mandatory )
- writing-skills documents a SPECIFIC, tested failure mode that Quantis's invented 'modes' and hedged descriptions likely reintroduce: if a skill's DESCRIPTION (or a wrapper) summarizes the workflow, the model follows the summary and SKIPS the skill body. The canonical example is the exact two-stage-review bug. — writing-skills/SKILL.md, CSO section: 'Testing revealed that when a description summarizes the skill's workflow, Claude may follow the description instead of reading the full skill content. A description saying "code review between tasks" caused Claude to do ONE review, even though the skill's flowchart clearly showed TWO reviews (spec compliance then code quality). When the description was change [https://raw.githubusercontent.com/obra/superpowers/main/skills/writing-skills/SKILL.md] (relevance: This is the highest-value reintroduced-bug warning. Quantis's thin workflows (discuss-phase.md, stress-test.md, execute.md) that ADD workflow summaries / invented 'modes' / '(preferred when subagents )
- How skills get triggered/loaded: a session-bootstrap skill (using-superpowers) forces 'invoke relevant skills BEFORE any response (even a 1% chance)', mandates announcing usage, and mandates one TodoWrite todo per checklist item. It also encodes instruction priority (user > skills > default) and a subagent stop-tag so dispatched subagents don't recurse into the bootstrap. — using-superpowers/SKILL.md: '<EXTREMELY-IMPORTANT> If you think there is even a 1% chance a skill might apply ... you ABSOLUTELY MUST invoke the skill ... This is not negotiable.' Flow: 'Invoke Skill tool -> Announce: "Using [skill] to [purpose]" -> Has checklist? -> Create TodoWrite todo per item -> Follow skill exactly'. '<SUBAGENT-STOP> If you were dispatched as a subagent to execute a specific [https://raw.githubusercontent.com/obra/superpowers/main/skills/using-superpowers/SKILL.md] (relevance: The '<SUBAGENT-STOP>' tag is a concrete pattern Quantis should ensure exists in its using-quantis equivalent, so Antigravity invoke_subagent implementers don't re-bootstrap the whole methodology (a so)
Recommendations: RESOLVE the Phase 3.1 scope decision in favor of editing the writing-plans skill. The SDD-vs-inline 'Which approach?' menu is inherited verbatim from upstream v5.1.0 (Quantis .agents/skills/writing-plans/SKILL.md lines 134-152) and is also hardcoded in the plan-header template ('Use subagent-driven-development (recommended) or executing-plans'). It is physically unfixable from execute.md alone. Replace the menu with the upstream executing-plans deterministic rule: 'If subagents are available, use subagent-driven-development.' Since Antigravity 2.0 always exposes native invoke_subagent, this resolves to: always SDD, no menu. | Make the discuss-phase SPEC.md gate scope itself to 'brainstorming steps 1-8 only' (not all 9). Upstream brainstorming step 9 AUTO-invokes writing-plans as its terminal state; a gate that wraps the whole skill will collide with that built-in transition. This matches what the Quantis self-stress-test already surfaced. | Delete the invented 'modes'. Upstream brainstorming has no modes (Visual Companion is explicitly 'a tool — not a mode'). Remove 'in discuss/explore mode' from discuss-phase.md and 'adopt adversarial critique mode' from stress-test.md. For stress-test, instead of inventing a mode, point at the skill's actual adversarial mechanism or write a dedicated adversarial-review prompt template (mirroring how SDD ships spec-reviewer-prompt.md / code-quality-reviewer-prompt.md as separate files). | Fix execute.md's <related> table 'preferred when subagents available' wording. Per persuasion-principles, hedged 'preferred/consider' language is the documented OPPOSITE of the bright-line Authority rule the framework relies on, and per the writing-skills CSO finding, workflow-summary hedging in a wrapper causes the model to follow the wrapper and skip the skill body (the proven two-stage-review bug). Use deterministic Authority phrasing: 'YOU MUST use subagent-driven-development. No exceptions.' | For ISSUE 5 (whole-phase senior code review): adopt the upstream pattern directly. SDD already ends with 'Dispatch final code reviewer subagent for entire implementation' immediately before finishing-a-development-branch, using the requesting-code-review/code-reviewer.md 'Senior Code Reviewer' template with Critical/Important/Minor severity and a Ready-to-merge verdict. Map this to a phase-level reviewer that runs after the LAST plan's tasks in a phase complete. Do not design a new reviewer; reuse the existing template. | For ISSUE 6 (stress-test depth) and any new gated steps: enforce with the cited persuasion stack — Authority (YOU MUST / no exceptions), Commitment (mandatory announcement + one TodoWrite todo per checklist item), Social Proof (failure-mode framing: 'skip X = Y, every time'). Avoid Liking/Reciprocity. This is the upstream-validated way to make instructions stick (Meincke 2025, 33%->72% compliance). | For ISSUE 7 (broader subagent integration) and to prevent subagent sprawl: keep implementers SEQUENTIAL in SDD (parallel implementers conflict — upstream Red Flag), reserve parallel invoke_subagent strictly for independent, no-shared-state domains per dispatching-parallel-agents, and keep coordination/orchestration work INLINE (the controller never delegates its own context). Ensure the using-quantis bootstrap carries a <SUBAGENT-STOP>-equivalent so dispatched subagents don't re-bootstrap the full methodology. | Audit every Quantis thin workflow for the CSO anti-pattern: the wrapper should TRIGGER + ANNOUNCE the skill, never paraphrase the skill's process. Any 'when to use' text must be triggering-conditions-only. Skill descriptions must be 'Use when...' (conditions), never 'what it does' (workflow), because upstream proved a workflow-summarizing description makes the model follow the summary and skip the skill body.

## Adversarial spec/plan review (multi-agent QA) + instruction-following degradation in long markdown workflows — evidence and recommendations for Quantis
- Decomposing an evaluation into one focused criterion per call/agent outperforms a single monolithic multi-dimension review prompt — directly validating splitting Quantis' single-prompt 7-dimension review into one focused agent per dimension. — Branch-Solve-Merge (Saha et al., NAACL 2024) decomposes evaluation into independent criterion branches solved separately then merged. Quantified: 'BSM with LLaMA-2-70B-chat improves LLM-human agreement by up to absolute 26%' over zero-shot, and a '12% absolute improvement over Plan&Solve, which specifically shows the utility of branching into and solving _independent_ sub-problems.' LLM-as-judge s [https://arxiv.org/abs/2310.15123] (relevance: Quantis' single-prompt 7-dimension review (ISSUE 5 / whole-phase senior review) is above the evidence-based ceiling and is predicted to be shallow. Replace with a fan-out: dispatch one focused review )
- An independent / fresh-context reviewer (ideally a different model, or at minimum an agent that did NOT write the artifact) finds materially more issues than the author re-reviewing its own work; same-model self-review has systematic blind spots. — Industry research: 'Using the same model to review its own output creates systematic blind spots'; 'The model that wrote the code is the one that introduced the bugs, which makes it less equipped to find them than an independent reviewer.' Cross-model effect: 'Claude and Codex... have different training approaches and tend to make different kinds of mistakes. A bug that Claude is likely to overloo [https://zylos.ai/research/2026-02-17-multi-model-ai-code-review] (relevance: Quantis already enforces fresh-context reviewers in SDD (spec-reviewer / code-quality-reviewer separate from implementer) — keep and extend this. For the whole-phase senior review (ISSUE 5) and stress)
- Adversarial review is deep (not surface) only when it is constrained to falsification: diagnosis-only, fatal-flaws-only, concrete attack angles, and an explicit 'how it breaks' scenario — exactly the obra/superpowers adversarial-review pattern Quantis forked from. — obra/superpowers issue #1130 (the upstream proposal): the review is 'a destructive pass in the middle' that runs 'between writing-plans and executing-plans' and reports 'only fatal flaws, not improvements.' Critical rule: 'no fix suggestions. Diagnosis only. The moment it proposes improvements, it drifts back into the same mode the existing skills already cover and the findings get softer.' It use [https://github.com/obra/superpowers/issues/1130] (relevance: Quantis' stress-test.md telling brainstorming to 'adopt adversarial critique mode' is the weak/invented-mode pattern and produces soft, mixed feedback. Replace with a dedicated adversarial pass that ()
- Iterative self-critique (Self-Refine / Reflexion) improves a single model's own output, but the gains are bounded and weaker than independent/adversarial review — so self-refine is a complement, not a substitute, for a fresh-context reviewer. — Self-Refine (Madaan et al., 2023): same LLM as generator+critic+refiner yields '~20% average improvement across tasks, purely at test time.' Reflexion (Shinn et al., 2023): verbal self-reflection lifts HumanEval pass@1 from 80% to 91% (+11%), AlfWorld +22%, HotPotQA +20%, and 'self-reflection improves learning by an 8% absolute boost over... episodic memory,' i.e. 'refinement-only approaches are n [https://arxiv.org/abs/2303.11366] (relevance: Cheap win for the author phase: have the planning/spec subagent do ONE self-refine pass (generate -> critique own draft -> revise) before handing off — costs one extra turn, ~10-20% quality lift. But )
- Instructions placed at the TAIL of a long file get dropped because of the documented U-shaped positional bias ('lost in the middle'): models attend strongly to the start and end but degrade in between — and as a file grows, content drifts toward the low-attention middle. — Liu et al. (Lost in the Middle, TACL 2023): in 20-document QA, accuracy was 'highest when the relevant document was first, nearly as high when it was last, and dropped by 20-30 points when the relevant document sat in the middle.' Mechanistically: 'LLMs exhibit a U-shaped attention bias where the tokens at the beginning and at the end... receive higher attention, regardless of their relevance,' at [https://cs.stanford.edu/~nfliu/papers/lost-in-the-middle.arxiv2023.pdf] (relevance: Confirms the root cause of Quantis ISSUE 3/4 (verify.md / execute.md steps at the tail being ignored). A workflow .md that opens with a <related> table, narrative, and tables, then puts a critical ste)
- What reliably makes an instruction stick in a long markdown prompt: state it at the TOP (primacy), REPEAT it at the END (recency), wrap it in clear structured syntax/headings, decompose multi-step tasks, and convert the must-do into a concrete required action with a verifiable post-condition. — Microsoft/Azure OpenAI prompt-engineering guidance (primary source): 'telling the model the task you want it to do at the beginning of the prompt... can help produce higher-quality outputs'; 'Models can be susceptible to recency bias... it's worth experimenting with repeating the instructions at the end of the prompt'; best-practice 'Double down. Sometimes you might need to repeat yourself... Give [https://learn.microsoft.com/en-us/azure/foundry/openai/concepts/prompt-engineering] (relevance: Direct fix recipe for Quantis ISSUES 3/4 and the new HARD-GATEs. The brainstorming skill's existing <HARD-GATE> + 9-step checklist is already the right shape (top-anchored, structured, numbered = deco)
- Making a critical step a REQUIRED tool-call / task with a checkable post-condition is more reliable than prose, because the model must emit a verifiable artifact rather than silently 'understand' the instruction. — Reflexion/agent evidence shows externalizing reasoning and outcomes into explicit artifacts (episodic memory, structured reflections) drives the gains over implicit refinement. Azure guidance: instructing the model to produce a defined output structure / citations 'can have a significant effect on the nature and quality of the results' and forces a verifiable form. The brainstorming skill's step 6 [https://arxiv.org/abs/2303.11366] (relevance: For Quantis HARD-GATEs (e.g. discuss-phase 'SPEC.md must exist', pause 'all plans have SUMMARY.md'), encode them as explicit checks that produce/verify a file rather than narrative reminders. Phrase g)
Recommendations: ISSUE 5 (whole-phase senior review): do NOT use one prompt listing 7 dimensions. Fan out one focused review subagent per dimension (BSM-style decomposition: +26% human agreement, +12% over non-decomposed), then a merge step. Antigravity supports parallel dispatch via invoke_subagent. Keep each subagent's mandate to a single criterion (evidence ceiling is 3-5 dims/call). | Every review/QA subagent MUST be fresh-context and must NOT have authored the artifact (self-review has systematic blind spots; independent review catches ~3-5x more issues). Quantis' SDD already does this for spec-reviewer/code-quality-reviewer — extend the same rule to the whole-phase senior review and stress-test. If Antigravity lets you pick the model, prefer a different model than the author; if not, at minimum a new agent (NOT 'self'/clone), since clones share correlated errors. | ISSUE 6 (stress-test depth): replace 'adopt adversarial critique mode' (an invented mode brainstorming doesn't define) with a dedicated adversarial-review pass modeled on obra/superpowers #1130: diagnosis-only / NO fix suggestions, fatal-flaws-only, fixed attack angles (Structural / Operational-'how it breaks at 3am' / Scope-creep), and a hard requirement that EACH finding include (a) a concrete failing scenario and (b) a severity+impact tag. 'Assume it's wrong, prove it' framing, not 'critique it.' | Add the missing destructive pass BETWEEN plan.md and execute.md (this is the upstream gap superpowers identified). Quantis has constructive review up front (writing-plans) and after (code-review) but no falsification pass before code is written — that is where the cheapest-to-fix fatal flaws live. | Fix ISSUE 3/4 root cause structurally, not by moving text to the tail. Long workflow .md files suffer lost-in-the-middle (20-30pt accuracy drop for mid-context info). For each must-do step: (1) state it at the TOP as a HARD-GATE, (2) RESTATE it as a closing post-condition check at the END (recency), (3) wrap in clear uppercase headings / XML tags, (4) keep the numbered-checklist decomposition the brainstorming skill already uses. Bookend, don't bury. | Convert critical workflow steps from prose into required tool-calls with verifiable post-conditions. E.g. verify.md Step 1.5 'load verification skill' should be phrased as an explicit invoke + a confirmation the skill loaded; pause.md sync should explicitly check ALL plans have SUMMARY.md (not ANY) and emit the check result. Models reliably do verifiable actions; they silently skip prose reminders. | Add one cheap self-refine pass (generate -> critique own draft -> revise) inside the authoring subagent for SPEC and PLAN before handoff (~10-20% quality lift, Self-Refine; +11% Reflexion on code). This is a complement that catches easy issues early, NOT a replacement for the independent adversarial/stress-test pass which finds the harder, correlated-blind-spot issues. | Resolve the pending SCOPE DECISION in favor of touching the writing-plans skill: the execution-method menu and any 'preferred when subagents available' wording live in the skill, and a workflows-only fix cannot remove the contradictory second entry point. Single source of truth (deterministic SDD, announced, no menu) requires editing the skill, consistent with the architecture where skills own methodology. | When writing HARD-GATEs, scope them precisely to avoid the brainstorming step-9 auto-invoke trap already surfaced: discuss-phase's gate must say 'steps 1-8 only' (step 9 auto-invokes writing-plans), and pause's gate must check ALL plans complete, not ANY. Imprecise gates marked partial phases complete — gates must name the exact artifact and the exact completeness condition.

## Google Antigravity (Antigravity 2.0) subagent capabilities — primitives, execution model, context handling, planning artifacts, and orchestrator guidance, with implications for the Quantis methodology layer
- The SDK-level subagent primitive is start_subagent, mapped to an invoke_subagent proto field; the parent's execution logically pauses and the subagent returns its result as a ToolResult. Subagent use must be explicitly enabled in CapabilitiesConfig. — DeepWiki on antigravity-sdk-python (section 8.2): 'one main subagent tool: start_subagent ... mapped to the invoke_subagent proto field'; 'execution is logically paused until the subagent completes its task and returns its final output as a ToolResult'; '... must be explicitly enabled in the CapabilitiesConfig.' The system tracks lineage via trajectory_id and cascade_id. [https://deepwiki.com/google-antigravity/antigravity-sdk-python/8.2-subagents-and-multi-agent-patterns] (relevance: Quantis' assumed mapping Task=invoke_subagent is essentially correct at the proto level, but the concrete callable name developers/agents see in the SDK is start_subagent. The ToolResult-return, paren)
- Antigravity ships built-in subagent roles and supports dynamically defined ones. Confirmed built-in 'self' = a direct clone (identical system prompt + toolsets); a browser sub-agent exists (invoked via /browser); a research-style isolated-context subagent is described. Custom subagents are defined on the fly via a DefineSubagent step. — WebSearch summary of antigravity.google/docs/subagents: pre-packaged subagents 'research (optimized for codebase research, navigation, and exploration), browser (operates sandboxed web browsers...), and self (a direct clone of the calling agent, sharing the identical system prompt and toolsets).' Codelab: mentions 'browser sub-agent' launched via /browser. DataCamp shows orchestrator output 'Defin [https://antigravity.google/docs/subagents] (relevance: Confirms Quantis' three built-in types (research read-only nav, browser, self). For Quantis workflows that just need to fan out read-only exploration or whole-repo search, the built-in 'research' suba)
- Subagents can be nested (a subagent can spawn its own subagents), unlike Claude Code's one-level static model. The documented hard limit is a maximum nesting depth of 10 levels. — WebSearch of antigravity.google/docs/subagents: 'Subagents can invoke their own subagents... A maximum nesting depth of 10 levels (layers of subagents beneath the main agent) is strictly enforced to prevent runaway resource exhaustion.' digitalapplied.com contrasts this with 'Claude Code's static subagent model ... one level deep.' [https://www.digitalapplied.com/blog/antigravity-2-google-desktop-agent-deep-dive] (relevance: Quantis can safely build multi-layer orchestration (e.g. a phase orchestrator that dispatches per-task implementers that themselves dispatch a research subagent) without hitting limits in practice — 1)
- The platform supports BOTH sequential and parallel/async subagent dispatch; parallelism is real but is gated by the orchestrator's dependency decisions and the SDK default is sequential (parent pauses for ToolResult). Marketing emphasizes async parallelism; the SDK and real-world demos show heavy serialization. — DataCamp: 'data_cleaner runs first; once sales_clean.csv is ready, remaining subagents dispatch simultaneously' and run 'in parallel.' dev.to: async subagents '... You can run several in parallel.' But DeepWiki SDK says parent 'execution is logically paused until the subagent completes.' MCP.directory 93-agent analysis: '93 agents must have been running mostly sequentially-coordinated, not embarra [https://mcp.directory/blog/antigravity-os-demo-93-agents-breakdown] (relevance: Resolves Quantis' core question 'sequential or parallel?': it is orchestrator-controlled, not automatic. SDD's deliberate sequential implementer pattern is fully supported and is actually the path of )
- Each subagent runs in a fresh, isolated context window with its own tools/working memory and no shared state with siblings by default; it reads/runs tools itself and returns only a synthesized summary to the parent. — WebSearch of docs/subagents: 'spin up a fresh agent session with a clean context window ... works through the problem using its own tools and MCP servers, then shuts down. It returns only a synthesized summary of its findings.' digitalapplied.com: 'Each subagent runs in an isolated context — it has its own tool access, its own working memory, and no shared state with sibling subagents by default.' [https://antigravity.google/docs/subagents] (relevance: Confirms the central premise behind Quantis using subagents at all: context isolation. An implementer subagent gets a clean window and returns a summary — which is exactly why SDD requires each implem)
- Subagents inherit the parent's permissions (allowed command prefixes, file read/write scopes) and cannot exceed them; tool-call confirmations bubble up to a subagent panel in the UI, where the user can also monitor, message, interrupt, or kill a running subagent. — WebSearch of docs/subagents: 'Subagents automatically inherit the parent's allowed terminal command prefixes and file read/write directory scopes, and cannot perform any action that the user has not already approved for the parent.' '... bubbled up to the subagent panel UI for your approval.' 'The parent agent can also interrupt a subagent (by sending a message) or kill it entirely.' [https://antigravity.google/docs/subagents] (relevance: Quantis' tool mapping for management (manage_subagents = list/kill) and send_message is consistent with the documented monitor/interrupt/kill model. Practically, Quantis workflows do not need to re-de)
- send_message enables direct agent-to-agent messaging by agent ID — not just parent<->child but to any active agent whose ID is known. However, in the Agent Manager (separate top-level agents, not subagents) the user is the coordinator and those peer agents do NOT auto-communicate. — WebSearch of docs/subagents: 'Agents communicate by sending messages to each other using unique agent IDs ... not only with their direct parents or subagents, but also with any other active agent whose ID is known.' agentpedia.codes on Agent Manager: 'They share the same workspace files but don't directly communicate with each other — you are the coordinator.' [https://discuss.ai.google.dev/t/antigravity-sub-agents/114381] (relevance: Two distinct concepts Quantis must not conflate: (1) subagents within one agent's tree (programmatic, can message via send_message), vs (2) multiple top-level agents in Agent Manager (human-coordinate)
- Antigravity has its own first-class planning/artifact system: the agent generates a structured plan before coding and produces named Artifacts — Implementation Plan, Task List, Walkthrough, plus code diffs and screenshots/browser recordings — which users review and comment on inline without stopping execution. — Codelab: 'Before writing code, the agent generates a structured plan'; artifacts include 'Implementation Plan' ('used to architect changes within your codebase'), 'Task Lists' (step-by-step), and 'Walkthrough' ('created once the agent has completed task implementation, as a summary of the changes and how to test them'). developers blog: agents 'generate Artifacts as tangible deliverables: task lis [https://codelabs.developers.google.com/getting-started-google-antigravity] (relevance: This is a direct overlap/competition with Quantis' own SPEC->PLAN->EXECUTE->VERIFY artifacts (SPEC.md, PLAN, SUMMARY.md, ROADMAP/STATE/JOURNAL). Quantis sits ON TOP of a platform that already wants to)
- Antigravity treats persistent instructions via two mechanisms: a Knowledge base (agents save useful context/snippets to improve future tasks) and Skills (a 'specialized package of knowledge that sits dormant until needed,' loaded by progressive disclosure when relevant). — developers blog: platform 'treats learning as a core primitive, allowing agents to save useful context and code snippets to a knowledge base.' Codelab: 'A skill is a specialized package of knowledge that sits dormant until needed' operating through 'progressive disclosure.' [https://developers.googleblog.com/build-with-google-antigravity-our-new-agentic-development-platform/] (relevance: Validates Quantis' core architecture: its 18 SKILL.md methodology files ARE the platform's Skill primitive (dormant, progressively disclosed). This is why the brainstorming/SDD/verification skills loa)
- Documented/known issues: subagent documentation is sparse and the feature set shifted rapidly (custom subagents were a March-2026 feature request 'escalated internally' that later shipped as dynamic DefineSubagent); async/parallel subagents consume quota in parallel; and agents can synthesize false progress by reading prior conversation history, requiring explicit audit/verification. — discuss.ai.google.dev (Mar 3 2026): custom subagents not yet available, 'This feature request has been escalated to the relevant internal teams for review.' dev.to commenter: 'the documentation is SPARSE'; warning: 'Async subagents ... consume your quota in parallel. Watch /usage.' MCP.directory: team built an Auditor role to 'detect hardcoded test outputs, mocked facades' because agents could 'sy [https://discuss.ai.google.dev/t/antigravity-sub-agents/114381] (relevance: Two direct hits on Quantis' open issues: (1) ISSUE 6/stress-test depth and the open question of a whole-phase senior code review (ISSUE 5) are validated by the real-world need for a dedicated Auditor/)
Recommendations: Use the platform's real primitive names in Quantis docs: the callable spawn tool is start_subagent (proto: invoke_subagent); management is via the subagent panel (monitor/interrupt/kill) and send_message by agent ID. Keep the Quantis tool-mapping table but annotate that Task maps to start_subagent/invoke_subagent and that 'manage_subagents (list/kill)' corresponds to the panel's stop/interrupt + send_message, not a single documented tool. | Resolve the Phase 3.1 pending scope decision in favor of fixing the writing-plans skill menu too. The platform's model is 'load a skill / dispatch a subagent,' not 'pick an execution mode from a menu.' Leaving the writing-plans 'Subagent-Driven vs Inline' menu in place keeps a second invented entry point that contradicts the new deterministic execute.md gate; align both at the skill layer. | Treat SDD's sequential implementer pattern as the platform default, not a fallback. The SDK pauses the parent for a ToolResult and the 93-agent demo shows wide parallel trees mostly serialize and burn quota; so execute.md should remove 'parallelism preferred when subagents available' and reserve parallel dispatch (dispatching-parallel-agents) strictly for provably independent tasks with an explicit quota caveat. | Keep the pause.md safety-net check as 'ALL plans have SUMMARY.md' (not ANY). Because subagents run in isolated context and return only a synthesized summary, the orchestrator's only evidence a task truly completed is its returned artifact; a partial-phase false-complete is exactly the 'synthesize false progress' failure Antigravity teams built an Auditor to catch. | Decide how Quantis plans map onto Antigravity's native Implementation Plan / Task List / Walkthrough artifacts to avoid two diverging plans. Prefer making the writing-plans output BE the Antigravity Implementation Plan artifact and the verify/walkthrough step BE the platform Walkthrough, rather than maintaining parallel .quantis docs that the IDE doesn't know about. | Scope ISSUE 5 (whole-phase senior code review) as a dedicated final reviewer subagent and ISSUE 6 (stress-test depth) as an adversarial Critic/Auditor role — both are validated by real Antigravity multi-agent practice (Reviewer/Critic/Auditor roles, authenticity static-analysis). Use a 'self' or define_subagent reviewer in a fresh isolated context so the reviewer cannot inherit the implementer's rationalizations from conversation history. | Drop invented 'modes' entirely (discuss-phase 'discuss/explore mode', stress-test 'adversarial critique mode'). The platform exposes Skills (progressive disclosure) and Knowledge, not modes; phrase these as 'load skill X' / 'adopt skill X's checklist,' which is already the brainstorming skill's own framing ('Available as a tool — not a mode'). | Lean on automatic permission inheritance: subagents inherit the parent's command prefixes and file scopes and cannot exceed them. Quantis workflows should not attempt to re-grant or broaden subagent permissions; instead ensure the orchestrator session is approved for the needed scopes up front, and rely on the subagent panel to bubble up any confirmations. | Add an explicit cost/quota note to any Quantis workflow that fans out subagents. Antigravity bills subagent work against the same quota in parallel ('exhaust your weekly quota within a couple of tasks' on the $200 tier; Claude-Opus-equivalent of the demo would be ~$65k), so parallel dispatch must be a deliberate, justified choice in the methodology, not a default. | Flag in Quantis docs that official Antigravity subagent documentation is sparse and fast-moving (custom subagents went from 'escalated feature request' in Mar 2026 to shipped DefineSubagent). Quantis should pin behavior to the SDK semantics (start_subagent / ToolResult / CapabilitiesConfig / 10-level nesting cap) it can verify, and avoid hard-coding marketing-level 'always parallel/async' assumptions that the SDK and real workloads contradict.

## AI-powered comprehensive / senior code review workflows that scale beyond a single context window — batching, full-file vs diff, finding aggregation, false-positive reduction, and map-reduce fan-out design — with concrete recommendations for a Quantis "senior-code-review" subagent capability.
- Production reviewers batch by RISK-TIER first (gating how many agents run at all), then fan out by ASPECT/DOMAIN, and pass each reviewer only the per-file diff PATCHES relevant to its domain — not the whole changeset. File-count and line-count thresholds drive the tier. — Cloudflare: 'if (fileCount > 50 || hasSecurityFiles) return "full"; if (totalLines <= 10 && fileCount <= 20) return "trivial";' — trivial reviews use 2 agents, lite 4, full all 7 specialized reviewers. Crucially: 'The system doesn't embed full diffs in the prompt. Instead, it writes per-file patch files to a diff_directory and passes the path. Sub-reviewers read only relevant patch files per domai [https://blog.cloudflare.com/ai-code-review/] (relevance: Directly answers Q1 and Q5. The best primary batching unit is NOT 'split files into chunks then a generic reviewer per chunk' — it is (a) tier-gate by changeset size so a 5-line phase doesn't spawn a )
- When a single changeset/diff genuinely exceeds the context budget, the canonical open-source approach (Qodo PR-Agent) is a deterministic two-phase compression then size-based CHUNKING into multiple model calls — chunks sized to a token budget, with a fixed number of findings requested per chunk so output scales with PR size. — PR-Agent algorithm (DeepWiki, source: pr_agent/algo/pr_processing.py): Files are sorted by main repo language first, then by token count descending; binary/auto-generated files (e.g. package-lock.json) excluded. Phase 1 pr_generate_extended_diff() includes max context; Phase 2 pr_generate_compressed_diff() kicks in over budget — removes deletion-only hunks via handle_patch_deletions(), includes on [https://deepwiki.com/qodo-ai/pr-agent/3.4-diff-processing-and-token-management] (relevance: Answers Q1 and Q2 with hard numbers. Gives Quantis a deterministic fallback ladder: (1) drop deletion-only hunks, (2) drop binary/generated files, (3) clip surrounding context, (4) only then split int)
- Leading reviewers review the DIFF, not full files — but augment each diff with SELECTIVELY RETRIEVED cross-file context (code graph, dependency traces, callers/callees), because a diff-only reviewer is blind to cross-file breakage, and dumping whole files wastes the budget. Whole-file is reserved for verification of specific assumptions. — CodeRabbit: 'a 10-line change can quietly alter a shared helper used by multiple services... reviewers who only see the diff are flying without instruments'; it 'packs a 1:1 ratio of code-to-context' and '80-90% of token usage goes into context enrichment, not the final review'; verification is on-demand: 'CodeRabbit generates shell/Python checks (think grep, ast-grep) to confirm an assumption or  [https://www.coderabbit.ai/blog/how-coderabbit-delivers-accurate-ai-code-reviews-on-massive-codebases] (relevance: Answers Q2 definitively: diff-as-primary-unit + retrieved cross-file context, NOT whole-file by default. Whole-file (or grep/AST proof) is pulled in only when a hypothesis needs confirming (dead-code,)
- Aggregation is done by a single COORDINATOR/synthesis agent that dedupes by keeping a shared finding once in its best-fit section, RE-CATEGORIZES misclassified issues, runs a 'reasonableness' verification reading source to kill speculative findings, then ranks into 3 fixed severity tiers. Volume is deliberately low (~1.2 findings/review). — Cloudflare coordinator does three things: 'Deduplication: If the same issue is flagged by both the security reviewer and the code quality reviewer, it gets kept once in the section where it fits best'; re-categorization (moving misclassified issues); and a 'reasonableness filter where the coordinator reads source code to verify speculative findings.' Severity is structured XML with three tiers: cr [https://blog.cloudflare.com/ai-code-review/] (relevance: Answers Q3. The synthesis step is a distinct final subagent (or the orchestrator) whose job is: collect per-reviewer findings -> dedupe (same issue from two lanes kept once) -> re-categorize -> re-rea)
- The #1 false-positive reduction levers are: (a) explicit NEGATIVE prompting ('What NOT to Flag'), (b) per-finding CONFIDENCE SCORING with a high posting threshold, and (c) an LLM verification/filter pass that re-reads surrounding code before posting. Confidence thresholds materially cut noise; target FP rate <10% (ideally <5%). — Cloudflare: each agent gets strict 'What NOT to Flag' sections — 'Defense-in-depth suggestions when primary defenses are adequate... Issues in unchanged code that this MR doesn't affect... "Consider using library X" style suggestions.' Claude Code Review plugin: 'Each finding is scored on a 0-100 confidence scale, and only high-confidence issues (default threshold: 80) are posted, dramatically red [https://claude.com/plugins/code-review] (relevance: Answers Q4. Three concrete, prompt-level mechanisms Quantis can adopt with zero infra: (1) give every reviewer subagent an explicit 'Do NOT flag' list (unchanged code, defense-in-depth, library-swap s)
- The proven map-reduce shape is MAP = N specialized reviewers run in parallel (each in its own context window, each returning only a condensed 1-2k-token summary), REDUCE = one orchestrator synthesizes. Anthropic explicitly recommends this: deep exploration stays isolated in subagents; the lead keeps only the plan and synthesizes distilled results. — Anthropic context-engineering guidance: 'each subagent might explore extensively, using tens of thousands of tokens or more, but returns only a condensed, distilled summary of its work (often 1,000-2,000 tokens)'; 'the lead agent maintains a high-level plan while subagents perform deep technical work,' and the orchestrator 'focuses on synthesizing and analyzing the results.' Claude Code subagents  [https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents] (relevance: Answers Q5 and is the architectural backbone. The fan-out dimension that works is BY ASPECT (correctness, security, perf, simplification/dead-code, tests, convention/CLAUDE.md compliance), with file-b)
- Empirically, the aspect-fan-out + synthesis design lands near human-reviewer usefulness and keeps noise tolerable, validating it as the target for a 'senior' review tier. — Augment Code: 45% true-positive rate (comments addressed by authors) vs ~50% for humans, and 1.03 bugs fixed per PR vs 0.54 for humans; quality is tuned by choosing 'high signal-to-noise OR thorough' and naming categories to avoid. HAMY (9 Claude subagents): '~75% useful suggestions, much better than the <50% I saw previously,' achieved partly by making agents skip 'formatting, naming nitpicks, an [https://www.augmentcode.com/blog/how-we-built-high-quality-ai-code-review-agent] (relevance: Validates the recommended architecture and sets measurable acceptance targets for ISSUE 5 (whole-phase senior review): aim for >50% addressed-finding rate and <10% false-positive rate, and explicitly )
Recommendations: Architecture for Quantis 'senior-code-review' (ISSUE 5): adopt a MAP-REDUCE / coordinator-fan-out shape. MAP = dispatch N read-only reviewer subagents (Antigravity 'research' subagent type via invoke_subagent), fanning out BY ASPECT (correctness/bugs, security, performance, dead-code & simplification, tests, convention/CLAUDE.md compliance) — NOT one generic reviewer per file. REDUCE = one synthesizer (the orchestrator or a final subagent) dedupes, re-categorizes, verifies, and ranks. This is read-only and parallel, so it does NOT conflict with the established 'SDD never runs parallel implementers' rule. | Batching unit, in priority order: (1) RISK-TIER GATE FIRST — compute fileCount, total changed lines, and whether security-sensitive or migration files are touched; skip review for trivial changes (e.g. <=10 lines, <=a few files), run a light 2-agent pass for small, full multi-aspect pass for large/sensitive (Cloudflare's tier function is the template). (2) Within a lane, the unit is the DIFF HUNK with ~3-10 lines of surrounding context, grouped per file. (3) Only when a single lane's diff still overflows, fall back to Qodo-style token chunking: sort files by language then token-count, drop deletion-only hunks and binary/generated files, fit greedily into ~32k-token chunks, request a fixed N findings per chunk, and emit an explicit OMITTED-files list so nothing silently vanishes. | Diff vs whole-file: default to DIFF + on-demand cross-file context, never pre-load whole files. Give each reviewer subagent the hunks plus tool access (view_file / grep_search) to pull callers, callees, and definitions only as it forms a hypothesis — this is how CodeRabbit/Greptile catch cross-file breakage without blowing the budget. Reserve whole-file reads for the specific checks that require them (dead-code/unused-symbol detection, cross-file edge cases), and have the agent confirm with grep/AST-style proof before reporting. | Synthesis/dedup (the REDUCE step): a single coordinator collects per-lane findings and (a) dedupes — a finding raised by two lanes is kept once in its best-fit section, (b) re-categorizes misclassified items, (c) runs a 'reasonableness' verification re-reading the cited source lines to kill speculative findings, (d) outputs ONE report ranked into exactly three tiers: Critical (breaks/exploitable) > Warning (measurable regression) > Suggestion. Bias the verdict toward 'approve unless there is real production risk.' Keep total volume low — production tools average ~1.2 findings/review. | False-positive controls (the #1 complaint, and the crux of ISSUES 5/6): bake in THREE prompt-level mechanisms. (1) Give every reviewer an explicit 'What NOT to Flag' list: unchanged code the phase didn't touch, defense-in-depth when primary defense is adequate, 'consider library X' swaps, and any style/naming/formatting nit a linter already catches. (2) Require each finding to carry a 0-100 confidence; default to a 'high-confidence only' mode that drops anything below ~80, with a 'thorough' mode that surfaces lower-confidence items — map this onto the harness's existing /code-review low/medium/high/max effort levels. (3) Add the verification re-read pass in the synthesizer. Acceptance targets to stress-test against: >50% addressed-finding rate and <10% (ideally <5%) false-positive rate. | For ISSUE 6 (stress-test depth) and ISSUE 7 (broader subagent integration): the same fan-out-by-aspect + read-only-subagent + condensed-1-2k-token-summary pattern from Anthropic's context-engineering guidance generalizes beyond review — use it as the standard Quantis 'dispatch N specialists, synthesize' primitive. Reviewer subagents must return only distilled summaries (1-2k tokens), never raw exploration, so the orchestrator's context survives many lanes. This keeps the senior-review capability consistent with how stress-test and other parallel-dispatch workflows should be built. | Integration with existing Quantis flow: position senior-code-review as a PHASE-LEVEL capability that runs AFTER the per-task SDD reviewers and the existing single final whole-implementation reviewer — it should CONSUME the per-task SUMMARY.md / reviewer outputs as its high-level map (so it knows intent), then fan out aspect-reviewers over the cumulative phase diff. This avoids re-deriving context from scratch and respects the existing SDD sequence; it is an additive REDUCE layer over work already done, not a replacement.
