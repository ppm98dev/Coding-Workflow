---
phase: 3.1
plan: methodology-overrides-and-rules
wave: 2
gap_closure: false
---

# Phase 3.1: Methodology Scope Overrides + Rules Contradictions — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development or executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Kill phantom modes, rogue terminal states, and rules-layer contradictions — Fixes 5 and 6 from the SPEC.

**Architecture:** Prose edits to 13 files. Fix 5 reconciles the methodology chain with wf-* orchestration (including the bootstrap-consistency files in Task 5). Fix 6 aligns PROJECT_RULES, QUANTIS-STYLE, and the 3-strike protocol. Wave 2 because some files overlap with Wave 1 edits.

**Tech Stack:** Markdown

---

### Task 1: Fix wf-discuss-phase + brainstorming (phantom modes, Planning Lock bypass)

**Files:**
- Modify: `.agents/skills/wf-discuss-phase/SKILL.md`
- Modify: `.agents/skills/brainstorming/SKILL.md`

- [ ] **Step 1: Delete "discuss/explore mode" from wf-discuss-phase**

In `.agents/skills/wf-discuss-phase/SKILL.md`, find the brainstorming invocation (around line 42):

Current text (approximately):
```
**Read and follow `.agents/skills/brainstorming/SKILL.md`** in discuss/explore mode.
```

Replace with:
```
**Read and follow `.agents/skills/brainstorming/SKILL.md`, checklist items 1–8 in full** — including writing the spec to the phase directory and the user review gate. **Override:** do NOT execute item 9 (invoking writing-plans); this workflow ends at Step 4 below, suggesting `/plan {N}`.
```

- [ ] **Step 2: Add SPEC existence gate to wf-discuss-phase completion**

Before the completion banner in wf-discuss-phase, add:

```markdown
**Gate:** `test -f "$PHASE_DIR/SPEC.md"` — if SPEC.md was not created during brainstorming, the discussion is incomplete. Do not display the completion banner.

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 Quantis ► PHASE {N} DISCUSSED ✓
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Spec: $PHASE_DIR/SPEC.md ✓
▶ /stress-test {N} — adversarial spec review (recommended)
▶ /plan {N} — create execution plans
```
```

- [ ] **Step 3: Add caller-override clause to brainstorming**

In `.agents/skills/brainstorming/SKILL.md`, find the end of the checklist (around step 9, line 32) and add:

```markdown
**If invoked by a workflow command** (`/discuss-phase`, `/stress-test`, etc.): the caller may override step 9 with its own terminal action, but steps 1–8 always apply. A design discussion that ends without a written, committed SPEC.md is incomplete regardless of any scope restriction the caller specifies.
```

- [ ] **Step 4: Add FINALIZED stamp to brainstorming's user approval**

Find brainstorming's user-review gate (where the user approves the spec). On approval, add:

```markdown
On user approval: set `Status: FINALIZED` in the spec's header and commit. This satisfies the Planning Lock — plans cannot be created until the spec is FINALIZED.
```

- [ ] **Step 5: Commit**

```bash
git add .agents/skills/wf-discuss-phase/SKILL.md .agents/skills/brainstorming/SKILL.md
git commit -m "fix(phase-3.1): kill phantom modes, add SPEC gate, close Planning Lock bypass"
```

Run: `grep -c "discuss/explore mode" .agents/skills/wf-discuss-phase/SKILL.md`
Expected: 0

Run: `grep -c "FINALIZED" .agents/skills/brainstorming/SKILL.md`
Expected: At least 1

---

### Task 2: Fix wf-execute methodology scoping + wf-stress-test phantom mode

**Files:**
- Modify: `.agents/skills/wf-execute/SKILL.md` (methodology selection)
- Modify: `.agents/skills/subagent-driven-development/SKILL.md` (fallback alignment + checkpoint exception)
- Modify: `.agents/skills/executing-plans/SKILL.md` (terminal step scoping)
- Modify: `.agents/skills/wf-stress-test/SKILL.md` (phantom mode removal)

- [ ] **Step 1: Align no-subagent fallback (pick SDD inline mode)**

In `.agents/skills/wf-execute/SKILL.md`, update the methodology selection (lines 72-76):

Current text:
```
2. **Select execution methodology (automatic — do NOT ask the user):**
   - Check if `invoke_subagent` tool is available in your current environment
   - **If available:** Read and follow `.agents/skills/subagent-driven-development/SKILL.md`. Announce: "I'm using Subagent-Driven Development to execute this plan."
   - **If NOT available:** Read and follow `.agents/skills/executing-plans/SKILL.md`.
   - **This is NOT a choice. Do not present a menu. Do not ask the user.**
```

Replace with (**keep the `2.` numbering — this is item 2 under "For each plan", after "1. Load plan context"; do not renumber to `1.`**):
```
2. **Select execution methodology (automatic — do NOT ask the user):**
   - **Read and follow `.agents/skills/subagent-driven-development/SKILL.md`** — it handles platform detection internally.
   - SDD auto-selects: `invoke_subagent` available → dispatch real subagents; not available → inline SDD mode (self-execute with two-stage review gates).
   - **This is NOT a choice. Do not present a menu. Do not ask the user.**
   - **When dispatched by `/execute`, skip SDD's "When to Use" decision tree** — the workflow already chose.
```

This aligns the fallback: SDD inline mode is now the single fallback path, not executing-plans.

- [ ] **Step 1b: Remove the remaining "executing-plans as fallback" signals in wf-execute (audit H1)**

The grep below currently finds `executing-plans` at lines 7, 9, 75, and 142. Step 1 removed line 75 (the "If NOT available" delegation). Fix the rest so wf-execute no longer presents executing-plans as the inline fallback:

Line 7 — replace:
```
# /execute → executing-plans skill
```
with:
```
# /execute → subagent-driven-development skill
```

Line 9 — replace:
```
> **Skill-powered workflow.** Execution methodology is powered by `subagent-driven-development` (CLI-first default) or `executing-plans` (inline fallback). This workflow adds Quantis orchestration (wave management, verification, state tracking).
```
with:
```
> **Skill-powered workflow.** Execution methodology is powered by `subagent-driven-development`, which self-selects real subagents (when `invoke_subagent` is available) or inline mode (when it is not) — no separate fallback skill, no menu. This workflow adds Quantis orchestration (wave management, verification, state tracking).
```

`<related>` table (line ~142) — recharacterize the executing-plans row from "Execution methodology (delegated)" to:
```
| `executing-plans` | Standalone inline execution (only when invoked outside `/execute`) |
```

- [ ] **Step 2: Add checkpoint exception to SDD**

In `.agents/skills/subagent-driven-development/SKILL.md`, find the continuous execution rule (around line 14):

Current text (approximately):
```
**Continuous execution:** Do not pause to check in with your human partner between tasks...
```

Add after "all tasks complete":
```
…or a task explicitly typed `checkpoint:*` — those exist to stop you; present them to the user and wait for their response.
```

- [ ] **Step 3: Add scope note to SDD and executing-plans terminal steps**

In `.agents/skills/subagent-driven-development/SKILL.md`, find the finishing-a-development-branch reference and add:

```markdown
> **When invoked from `/execute`:** Skip the final whole-implementation review and finishing-a-development-branch steps. `/execute` Steps 5–6 own phase-level verification and completion. Only run these terminal steps when SDD is invoked standalone (not from a workflow).
```

In `.agents/skills/executing-plans/SKILL.md`, find Step 3 (Complete Development) and add the same note:

```markdown
> **When invoked from `/execute`:** Skip this step. `/execute` owns phase-level completion. Only run finishing-a-development-branch when executing-plans is invoked standalone.
```

- [ ] **Step 4: Fix wf-stress-test phantom mode references**

In `.agents/skills/wf-stress-test/SKILL.md`:

Find and delete "adversarial critique mode" references (lines 7, 30, 44). Replace line 7:
```
# /stress-test → brainstorming skill (critique mode)
```
with:
```
# /stress-test → adversarial spec review
```

Replace line 44:
```
**Read `.agents/skills/brainstorming/SKILL.md`** and adopt adversarial critique mode.
```
with:
```
**Adopt an adversarial mindset.** Your job is to BREAK the spec, not validate it. The 7-dimension framework below is self-contained — do not reference brainstorming modes.
```

- [ ] **Step 4b: Persist stress-test findings + stamp the spec (SPEC Fix 5 — currently unimplemented)**

The phantom-mode removal above is only half of SPEC Fix 5's stress-test bullet. Also make the findings durable:

In `wf-stress-test/SKILL.md` Step 4 ("Generate Report"), prepend: "Write the full findings report to disk before continuing — `$PHASE_DIR/STRESS-TEST.md` for a phase target, else `.quantis/STRESS-TEST.md`. **Gate:** `test -f` the report before continuing; findings that exist only in chat are lost at session end."

In Step 5 ("Update Spec"), change the heading from "Update Spec (if applicable)" to "Update Spec (mandatory when findings exist)" and append: "After recording unresolved questions, stamp `Stress-tested: {date}` on its own line under the spec's status header. If any 🔴 Critical finding is unresolved, STOP — fix it before `/plan`."

In Step 6, add the report to the commit: `git add` the STRESS-TEST.md path alongside STATE.md.

> Stress-test *integration into `/plan`* (auto-invoke, plan mode) remains out of scope → Phase 3.5. This step only makes the existing standalone command produce a durable artifact, which SPEC Fix 5 already requires.

Run: `grep -c "STRESS-TEST.md" .agents/skills/wf-stress-test/SKILL.md`
Expected: At least 1

Run: `grep -c "Stress-tested:" .agents/skills/wf-stress-test/SKILL.md`
Expected: At least 1

- [ ] **Step 5: Commit**

```bash
git add .agents/skills/wf-execute/SKILL.md .agents/skills/subagent-driven-development/SKILL.md .agents/skills/executing-plans/SKILL.md .agents/skills/wf-stress-test/SKILL.md
git commit -m "fix(phase-3.1): methodology scope overrides — aligned fallback, checkpoint exception, terminal scoping"
```

Run: `grep -c "adversarial critique mode" .agents/skills/wf-stress-test/SKILL.md`
Expected: 0

Run: `grep -nE "Read and follow.*executing-plans|inline fallback" .agents/skills/wf-execute/SKILL.md`
Expected: **0** — wf-execute no longer delegates to executing-plans as a fallback (SDD handles inline internally).

> Do NOT assert `grep -c "executing-plans" = 0` — the `<related>` table legitimately still names it as the standalone alternative. Match only the fallback-delegation phrasing above.

---

### Task 3: Fix rules-layer contradictions

**Files:**
- Modify: `.agents/rules/PROJECT_RULES.md`
- Modify: `.agents/rules/QUANTIS-STYLE.md`
- Modify: `.agents/skills/context-health-monitor/SKILL.md`
- Modify: `.agents/skills/systematic-debugging/SKILL.md`

- [ ] **Step 1: Fix wave table — ordering, not parallelism**

In `.agents/rules/PROJECT_RULES.md`, find the Wave Execution table (around lines 62-68):

Current text:
```
| 1 | Foundation tasks, no dependencies | Run in parallel |
| 2 | Depends on Wave 1 | Wait for Wave 1, then parallel |
| 3 | Depends on Wave 2 | Wait for Wave 2, then parallel |
```

Replace with:
```
| 1 | Foundation tasks, no dependencies | Execute sequentially |
| 2 | Depends on Wave 1 | Wait for Wave 1 complete, then execute |
| 3 | Depends on Wave 2 | Wait for Wave 2 complete, then execute |

> **Note:** Waves define *ordering constraints*, not parallelism. Implementation is always sequential (one subagent per task). Parallel dispatch is reserved for read-only investigation (see dispatching-parallel-agents skill).
```

- [ ] **Step 2: Add token-efficiency exemptions**

In `.agents/rules/PROJECT_RULES.md`, find the Loading Rules table (around lines 206-213) and add after the table:

```markdown
**Exemptions:** The following are always read in full regardless of size:
- SKILL.md files invoked via "Read and follow" directives
- `.quantis/` state files (STATE.md, ROADMAP.md, SPEC.md)
- Any file a skill explicitly says to read completely
- The >5-file cap applies to source code exploration only, not skill/state loading.
```

- [ ] **Step 3: Add delegation pattern and scoped Decision Gates to QUANTIS-STYLE**

In `.agents/rules/QUANTIS-STYLE.md`, add a new section after "UX Patterns" (before "Summary"):

```markdown
## Delegation Pattern

When a workflow delegates to a skill:
1. **"Read and follow `path/to/SKILL.md`"** as a numbered process step — never in `<context>`.
2. **Subagent branches paste content, not paths** — subagents don't inherit context, so the orchestrator must read the skill file and paste its full text into the subagent prompt.
3. **Platform Check block** (when present): Step 0, MUST dispatch when `invoke_subagent` is available, include "then continue at Step N" to prevent orphaned tail steps.
```

Update the Decision Gates section (lines 251-261) to add scope:

```markdown
### Decision Gates

When user input is genuinely needed for a decision the system cannot resolve:
```
⚠️ DECISION REQUIRED

Option A: {description}
Option B: {description}

Which do you prefer?
```

**Never use Decision Gates for:** execution mode selection, tool availability checks, platform detection, or any choice a skill resolves automatically.
```

- [ ] **Step 3b: Reconcile QUANTIS-STYLE task format with checkbox plans (audit C2)**

QUANTIS-STYLE still canonicalizes the XML `<task type="...">` structure (lines ~67-103, 181), which contradicts the checkbox plan format that `writing-plans` produces and `/execute` now parses (Fix 1). Without this, `/plan`'s output format and QUANTIS-STYLE disagree.

In `.agents/rules/QUANTIS-STYLE.md`, at the top of the "Task Structure" / XML `<task>` section, add:

```markdown
> **Plan task format:** Implementation plans use the **checkbox** task format defined in `writing-plans` (`### Task N` with `- [ ]` steps, `Run:`/`Expected:` verification). The XML `<task type="...">` structure below is retained ONLY for (a) the task-type/effort vocabulary (`auto`, `checkpoint:human-verify`, `checkpoint:decision`, effort levels) referenced by `/plan`, and (b) gap-closure fix plans generated by `/verify`. Do not author normal plans in XML.
```

Run: `grep -c "checkbox" .agents/rules/QUANTIS-STYLE.md`
Expected: At least 1

- [ ] **Step 4: Reconcile 3-strike protocol**

In `.agents/skills/context-health-monitor/SKILL.md`, find the 3-Strike Rule (around lines 27-37) and update:

```markdown
### Rule 1: The 3-Strike Rule

If debugging the same issue fails 3 times:

1. **Auto-save state** — Document in `.quantis/STATE.md`: what was tried, what errors occurred, current hypothesis
2. **Assess the pattern:**
   - If failures suggest an architectural problem (same area, same class of error) → **discuss with user** before attempting more fixes (see systematic-debugging Phase 4.5 — the file has Four Phases plus a "Phase 4.5"; there is no "Phase 5")
   - Otherwise → **recommend `/pause`** for a fresh session with fresh context
3. **Do NOT** continue with attempt #4 without either user discussion or fresh session

> **Cross-reference:** systematic-debugging § Phase 4.5 (architectural discussion), wf-execute § context_hygiene (/pause recommendation). This protocol supersedes both when they conflict.
```

Apply the same cross-reference note to:
- `systematic-debugging/SKILL.md` (around lines 192-213): Add `> **Cross-reference:** See context-health-monitor Rule 1 for the unified 3-strike protocol.`
- `wf-execute/SKILL.md` (lines 109-114): Add `> **Cross-reference:** See context-health-monitor Rule 1 for the unified 3-strike protocol.`

- [ ] **Step 5: Commit**

```bash
git add .agents/rules/PROJECT_RULES.md .agents/rules/QUANTIS-STYLE.md .agents/skills/context-health-monitor/SKILL.md .agents/skills/systematic-debugging/SKILL.md
git commit -m "fix(phase-3.1): rules-layer contradictions — waves, tokens, delegation, 3-strike"
```

Run: `grep -c "Run in parallel" .agents/rules/PROJECT_RULES.md`
Expected: 0

Run: `grep -c "Cross-reference.*3-strike" .agents/skills/context-health-monitor/SKILL.md .agents/skills/systematic-debugging/SKILL.md .agents/skills/wf-execute/SKILL.md`
Expected: 1 per file (3 total)

---

### Task 4: ROADMAP hygiene rider

**Files:**
- Modify: `.quantis/ROADMAP.md`

- [ ] **Step 1: Update Phase 3.1 deliverables**

Read `.quantis/ROADMAP.md`. Update Phase 3.1's deliverables to use `wf-*` paths (not the old `.agent/workflows/` paths deleted by Phase 3.2). Check off the two items 3.2 already shipped. Resolve the 3.2-depends-on-3.1 inversion note.

- [ ] **Step 2: Commit**

```bash
git add .quantis/ROADMAP.md
git commit -m "docs(phase-3.1): ROADMAP hygiene — updated paths, checked shipped items"
```

---

### Task 5: Bootstrap & adapter consistency (using-quantis, executing-plans, antigravity-tools) — audit H1

**Why this task exists:** SPEC Fix 5 explicitly lists `using-quantis` ("fix /execute and /stress-test rows; add project-SPEC row; Flexible-skill note"), and audit H1 lists `executing-plans:14` and `antigravity-tools.md:21`. None of these were covered by Tasks 1–4. `using-quantis` is read at session start, so a stale `/execute → executing-plans` row re-seeds Issue 2 system-wide even after every other fix lands.

**Files:**
- Modify: `.agents/skills/using-quantis/SKILL.md`
- Modify: `.agents/skills/executing-plans/SKILL.md`
- Modify: `.agents/skills/using-quantis/references/antigravity-tools.md`

- [ ] **Step 1: Fix the using-quantis command table + File Conventions**

In `.agents/skills/using-quantis/SKILL.md`:

Line 109 — replace:
```
| `/execute {N}` | Execute phase plans | executing-plans |
```
with:
```
| `/execute {N}` | Execute phase plans | subagent-driven-development (auto: real subagents or inline; executing-plans only standalone) |
```

Line 112 — replace:
```
| `/stress-test` | Adversarial spec/plan review | brainstorming (critique) |
```
with:
```
| `/stress-test` | Adversarial spec/plan review | — (self-contained 7-dimension framework) |
```

In the File Conventions table, add a row distinguishing the project spec from the phase spec (audit Issue-1 namespace):
```
| Project spec (Planning Lock) | `.quantis/SPEC.md` |
| Phase design spec | `.quantis/phases/{N}.{M}-{slug}/SPEC.md` |
```

In the "Skill Types" section, append to the "Flexible" line: "— but required output artifacts (e.g. brainstorming's SPEC.md) are never skippable; 'flexible' applies to dialogue style, not deliverables."

- [ ] **Step 2: Fix executing-plans' false "natively supported" note (audit H1)**

In `.agents/skills/executing-plans/SKILL.md` line 14 — replace:
```
**Note:** When subagents are available (Antigravity 2.0 supports them natively), use subagent-driven-development instead of this skill for better quality via two-stage review.
```
with:
```
**Note:** If the `invoke_subagent` tool is in your session's tool list (CLI `agy`, Standalone — not the IDE), use subagent-driven-development instead. If `/execute` routed you here, that check already failed — proceed with this skill and do not ask the user about execution modes.
```

- [ ] **Step 3: Fix antigravity-tools.md subagent-support claim (audit H1)**

In `.agents/skills/using-quantis/references/antigravity-tools.md` (~line 21), replace the blanket "Antigravity 2.0 supports subagents natively" claim with:
```
Antigravity supports subagents via `invoke_subagent` on CLI (`agy`) and Standalone only — the IDE does not have this tool. If `invoke_subagent` is not in your tool list, that is normal, not an error: skills fall back to inline execution automatically, and you must never ask the user whether subagents are available.
```

- [ ] **Step 4: Commit**

```bash
git add .agents/skills/using-quantis/SKILL.md .agents/skills/executing-plans/SKILL.md .agents/skills/using-quantis/references/antigravity-tools.md
git commit -m "fix(phase-3.1): bootstrap & adapter consistency — using-quantis rows, executing-plans + antigravity-tools subagent claims"
```

- [ ] **Step 5: Verify**

Run: `grep -c "executing-plans |" .agents/skills/using-quantis/SKILL.md`
Expected: 0 (the `/execute` row no longer maps solely to executing-plans)

Run: `grep -c "natively" .agents/skills/executing-plans/SKILL.md .agents/skills/using-quantis/references/antigravity-tools.md`
Expected: 0 for both (false "natively supported" claims removed)
