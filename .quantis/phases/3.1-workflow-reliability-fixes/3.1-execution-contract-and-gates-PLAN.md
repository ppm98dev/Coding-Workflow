---
phase: 3.1
plan: execution-contract-and-gates
wave: 1
gap_closure: false
---

# Phase 3.1: Execution Contract + STOP Gates — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development or executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix the execution contract (plan format, discovery, SUMMARY naming) and add STOP semantics to all gates — Fixes 1 and 3 from the SPEC.

**Architecture:** Prose edits to 6 SKILL.md files + 1 rules file. No code. Every change is a text replacement in an existing file. The execution contract and gate fixes are coupled because the zero-plans gate (Fix 1) needs the STOP pattern (Fix 3).

**Tech Stack:** Markdown, bash pseudocode

---

### Task 1: Fix writing-plans plan format contract

**Files:**
- Modify: `.agents/skills/writing-plans/SKILL.md:18-19,45-61,134-153`

- [ ] **Step 1: Add required frontmatter to plan header**

In `.agents/skills/writing-plans/SKILL.md`, replace the Plan Document Header section (lines 45-61).

Current text (lines 18-19):
```
**Save plans to:** `.quantis/phases/{N}.{M}-{slug}/{N}.{M}-PLAN.md`
- (User preferences for plan location override this default)
```

Replace with:
```
**Save plans to:** `.quantis/phases/{N}.{M}-{slug}/{N}.{M}-{plan-slug}-PLAN.md`
- Filename MUST end in `-PLAN.md` — `/execute` discovers plans by this suffix.
- Multiple plans per phase: use descriptive slugs (e.g., `3.1-execution-contract-PLAN.md`, `3.1-gate-semantics-PLAN.md`).
- (User preferences for plan location override this default)
```

Current text (lines 47-61):
```markdown
**Every plan MUST start with this header:**

\`\`\`markdown
# Phase {N}.{M}: {Description} - Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development (recommended) or executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** [One sentence describing what this builds]

**Architecture:** [2-3 sentences about approach]

**Tech Stack:** [Key technologies/libraries]

---
\`\`\`
```

Replace with:
```markdown
**Every plan MUST start with this header:**

\`\`\`markdown
---
phase: {N}.{M}
plan: {plan-slug}
wave: {W}
gap_closure: false
---

# Phase {N}.{M}: {Description} - Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development or executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** [One sentence describing what this builds]

**Architecture:** [2-3 sentences about approach]

**Tech Stack:** [Key technologies/libraries]

---
\`\`\`

The YAML frontmatter is REQUIRED — `/execute` reads `wave` for ordering and `gap_closure` for filtering. Default `wave: 1` if only one plan. Default `gap_closure: false` for normal plans.
```

Run: `grep -c "^wave:" .agents/skills/writing-plans/SKILL.md`
Expected: 1 (the template has the wave field)

- [ ] **Step 2: Replace execution handoff menu with auto-select**

Current text (lines 134-153):
```markdown
## Execution Handoff

After saving the plan, offer execution choice:

**"Plan complete and saved to `.quantis/phases/{N}.{M}-{slug}/{N}.{M}-PLAN.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?"**

**If Subagent-Driven chosen:**
- **REQUIRED SUB-SKILL:** Use subagent-driven-development
- Fresh subagent per task + two-stage review

**If Inline Execution chosen:**
- **REQUIRED SUB-SKILL:** Use executing-plans
- Batch execution with checkpoints for review
```

Replace with:
```markdown
## Execution Handoff

After saving the plan, announce the path and STOP:

**"Plan complete and saved to `{path}`. Run `/execute {N}` to implement."**

Do NOT ask the user to choose an execution mode — `/execute` auto-selects based on platform capabilities (`invoke_subagent` available → subagent-driven-development; else → executing-plans). Never present a menu.

If invoked directly (not from `/plan`), check `invoke_subagent` availability and proceed automatically:
- **If available:** Read and follow `.agents/skills/subagent-driven-development/SKILL.md`
- **If NOT available:** Read and follow `.agents/skills/executing-plans/SKILL.md`
```

Run: `grep -c "Which approach" .agents/skills/writing-plans/SKILL.md`
Expected: 0

- [ ] **Step 3: Add FINALIZED precondition (closes Planning Lock bypass — audit H9)**

In `.agents/skills/writing-plans/SKILL.md`, in the Overview/Scope Check section (near the top, before the File Structure section), add:

```markdown
**Planning Lock precondition:** Before writing any plan, confirm the spec contains `Status: FINALIZED` (the phase spec `.quantis/phases/{N}.{M}-{slug}/SPEC.md` if it exists, otherwise the project `.quantis/SPEC.md`). If it is not FINALIZED, STOP — return to brainstorming/`/discuss-phase` to finalize and approve the spec first. This mirrors `/plan`'s Planning Lock so the brainstorming → writing-plans path cannot bypass it.
```

Run: `grep -c "FINALIZED" .agents/skills/writing-plans/SKILL.md`
Expected: At least 1

- [ ] **Step 4: Commit**

```bash
git add .agents/skills/writing-plans/SKILL.md
git commit -m "fix(phase-3.1): writing-plans format contract + remove execution menu + FINALIZED precondition"
```

---

### Task 2: Fix wf-execute plan discovery, format parsing, and SUMMARY naming

**Files:**
- Modify: `.agents/skills/wf-execute/SKILL.md:45-50,53,77-79,96-105,116-136`

- [ ] **Step 1: Fix plan discovery (zero plans = error, not completion)**

In `.agents/skills/wf-execute/SKILL.md`, replace lines 45-50.

Current text:
```
ls "$PHASE_DIR"/*-PLAN.md 2>/dev/null
ls "$PHASE_DIR"/*-SUMMARY.md 2>/dev/null
```
Build list of **incomplete plans** (PLAN without matching SUMMARY).
If `--gaps-only`: filter to plans with `gap_closure: true` in frontmatter.
If no incomplete plans: phase already complete, skip to step 6.
```

Replace with:
```
ls "$PHASE_DIR"/*-PLAN.md 2>/dev/null
ls "$PHASE_DIR"/*-SUMMARY.md 2>/dev/null
```

**If NO `*-PLAN.md` files exist: STOP.** "No plans found for phase {N}. Run `/plan {N}` first." Do not touch ROADMAP.md or STATE.md.

Build list of **incomplete plans** — a plan is incomplete if `X-PLAN.md` has no matching `X-SUMMARY.md` (same filename prefix before `-PLAN.md` → before `-SUMMARY.md`).

If `--gaps-only`: filter to plans with `gap_closure: true` in YAML frontmatter.
If every PLAN has a matching SUMMARY: skip to Step 5 (Verify Phase Goal) — **never** straight to Step 6.
```

- [ ] **Step 2: Fix wave parsing fallback and task format**

Current text (line 53):
```
Read `wave` field from each plan's YAML frontmatter. Group plans by wave number. Lower waves first.
```

Replace with:
```
Read `wave` field from each plan's YAML frontmatter. If a plan has no `wave` field, treat it as wave 1 — do not error, do not skip. Group plans by wave number. Lower waves first.
```

Current text (lines 77-79):
```
3. Follow `<task>` blocks in order, run `<verify>` commands
4. Commit per task: `git commit -m "feat(phase-$PHASE): {task-name}"`
5. After all tasks in plan: create `$PHASE-SUMMARY.md` inside `$PHASE_DIR/` documenting what was done
```

Replace with:
```
3. Execute the plan's `### Task N` sections in order. For each task:
   - Run each step's `Run:` command and check output against `Expected:`.
   - (Legacy `<task>` XML plans from gap closure: run `<verify>` per block.)
   - **GATE:** Each task's verification must RUN and PASS (read the output) before that task is committed.
   - On failure → apply systematic-debugging. After 3 failed attempts on the same task → record BLOCKED in STATE.md, continue with other plans, do not mark the wave complete.
4. Commit per task (ONLY after verification passes): `git commit -m "feat(phase-$PHASE): {task-name}"`
5. After all tasks in plan: create per-plan summary — `X-PLAN.md` → `X-SUMMARY.md` (same prefix) inside `$PHASE_DIR/` documenting what was done.

> **Scope note:** The methodology skill (`subagent-driven-development`, which self-selects real subagents or inline mode by platform) executes ONE plan. Skip its terminal steps (final whole-implementation review, finishing-a-development-branch) — Steps 5–6 below own phase-level verification and completion. (Do not name `executing-plans` here — Plan 4 makes SDD the single execution path inside `/execute`.)
```

- [ ] **Step 3: Fix Step 6 banner to offer /verify instead of /execute {N+1}**

Current text (lines 116-124):
```
<offer_next>

**Phase complete, more phases:**
\```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 Quantis ► PHASE {N} COMPLETE ✓
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
▶ /plan {N+1} or /execute {N+1}
\```
```

Replace with:
```
<offer_next>

**Phase complete, more phases:**
\```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 Quantis ► PHASE {N} EXECUTION COMPLETE ✓
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
▶ /verify {N} — independent verification (recommended)
▶ /plan {N+1} — plan the next phase
\```
```

- [ ] **Step 4: Commit**

```bash
git add .agents/skills/wf-execute/SKILL.md
git commit -m "fix(phase-3.1): execution contract — discovery, format, SUMMARY, gates"
```

---

### Task 3: Fix wf-plan Step 5 checklist + all remaining STOP gates

**Files:**
- Modify: `.agents/skills/wf-plan/SKILL.md:50-54,166-174`
- Modify: `.agents/skills/wf-execute/SKILL.md:29-35` (environment validation gate — audit C5)
- Modify: `.agents/skills/wf-new-milestone/SKILL.md:15-21,91-110`
- Modify: `.agents/skills/wf-complete-milestone/SKILL.md:14-26`

- [ ] **Step 1: Fix wf-plan Planning Lock gate + Step 5 checklist**

In `.agents/skills/wf-plan/SKILL.md`:

Current text (lines 50-54):
```
## 1. Planning Lock
\```bash
grep -q "FINALIZED" ".quantis/SPEC.md" || echo "Error: SPEC.md must be FINALIZED"
test -f ".agents/rules/CONSTITUTION.md" || echo "⚠️ No .agents/rules/CONSTITUTION.md"
\```
```

Replace with:
```
## 1. Planning Lock
\```bash
grep -q "FINALIZED" ".quantis/SPEC.md" || { echo "❌ STOP: SPEC.md must be FINALIZED before planning."; exit 1; }
test -f ".agents/rules/CONSTITUTION.md" || echo "⚠️ No .agents/rules/CONSTITUTION.md — quality standards not enforced"
\```

**If the SPEC check fails: STOP.** Do not continue to Step 2. Tell the user to finalize the SPEC first. (⚠️ warnings are non-blocking.)
```

Current text (lines 166-174):
```
## 5. Plan Checker (max 3 iterations)
For each plan, verify:
- [ ] All referenced files exist or will be created
- [ ] Actions are specific (no vague "implement X")
- [ ] `<verify>` commands are executable
- [ ] `<done>` criteria are measurable
- [ ] Tests verify real behavior (no tautological asserts, no mock-everything)

**If issues found:** Fix and re-verify. Max 3 iterations.
```

Replace with:
```
## 5. Plan Checker (max 3 iterations)
For each plan, verify:
- [ ] All referenced files exist or will be created
- [ ] Actions are specific (no vague "implement X")
- [ ] Every task has `Run:` commands with `Expected:` output
- [ ] Every task's final step has measurable acceptance criteria
- [ ] Tests verify real behavior (no tautological asserts, no mock-everything)
- [ ] Plan has YAML frontmatter with `phase`, `plan`, `wave`, `gap_closure` fields
- [ ] Filename ends in `-PLAN.md`

**If issues found:** Fix and re-verify. Max 3 iterations. If unresolved after 3 iterations → STOP and ask the user.
```

- [ ] **Step 2: Fix wf-new-milestone SPEC check gate + archive guard**

In `.agents/skills/wf-new-milestone/SKILL.md`:

Find the SPEC check (around lines 15-21) and add STOP semantics:
After the `echo "Error: SPEC.md required..."` add: `**If SPEC.md is missing: STOP. Do not gather milestone information.**`

Find the truncation block (around lines 91-110) and add archive guard:
Before the truncation, add:
```
**Before resetting:** Verify the current DECISIONS.md and JOURNAL.md have been archived.
\```bash
# Check if archive exists for current milestone
if [ -f ".quantis/DECISIONS.md" ] && [ "$(wc -l < .quantis/DECISIONS.md)" -gt 5 ]; then
    if ! find .quantis/milestones -name "DECISIONS.md" -newer .quantis/DECISIONS.md 2>/dev/null | grep -q .; then
        echo "❌ STOP: DECISIONS.md has unarchived content. Run /complete-milestone first."
        exit 1
    fi
fi
\```
```

- [ ] **Step 3: Fix wf-complete-milestone incomplete phase check**

In `.agents/skills/wf-complete-milestone/SKILL.md`:

Find the incomplete phases check (around lines 14-26) and replace with:
```
\```bash
INCOMPLETE=$(grep -c -E "Status.*Not Started|Status.*In Progress" ".quantis/ROADMAP.md" || true)
if [ "$INCOMPLETE" -gt 0 ]; then
    echo "❌ STOP: Cannot complete milestone — $INCOMPLETE phases incomplete."
    echo "Run /progress to see status."
    exit 1
fi
\```

**If ANY phase is incomplete: STOP.** Do not proceed to verification or archiving.
```

- [ ] **Step 4: Fix wf-execute environment validation gate (audit C5)**

In `.agents/skills/wf-execute/SKILL.md`, replace the Step 1 validation block (lines 29-35).

Current text:
```bash
test -f ".quantis/ROADMAP.md" || echo "Error: run /plan first"
test -f ".quantis/STATE.md" || echo "Error: run /plan first"
test -f ".agents/rules/CONSTITUTION.md" || echo "⚠️ No .agents/rules/CONSTITUTION.md — quality standards not enforced"
```

Replace with:
```bash
test -f ".quantis/ROADMAP.md" || { echo "❌ STOP: No ROADMAP.md — run /plan first."; exit 1; }
test -f ".quantis/STATE.md"   || { echo "❌ STOP: No STATE.md — run /plan first."; exit 1; }
test -f ".agents/rules/CONSTITUTION.md" || echo "⚠️ No .agents/rules/CONSTITUTION.md — quality standards not enforced"
```

After the block add: `**If a STOP line printed: halt. Do not continue to Step 2.** (The ⚠️ CONSTITUTION line is a non-blocking warning.)`

> **Note:** the `⚠️` CONSTITUTION warning intentionally stays as `|| echo` — it is non-blocking. Only the two blocking checks convert to `|| { …; exit 1; }`.

- [ ] **Step 5: Commit all gate fixes**

```bash
git add .agents/skills/wf-plan/SKILL.md .agents/skills/wf-execute/SKILL.md .agents/skills/wf-new-milestone/SKILL.md .agents/skills/wf-complete-milestone/SKILL.md
git commit -m "fix(phase-3.1): STOP semantics for all gates"
```

- [ ] **Step 6: Verify all blocking gates STOP (warnings may remain)**

Run: `grep -nE '\|\| echo "(Error|❌)' .agents/skills/wf-plan/SKILL.md .agents/skills/wf-execute/SKILL.md .agents/skills/wf-new-milestone/SKILL.md .agents/skills/wf-complete-milestone/SKILL.md`
Expected: **Zero matches** — every blocking error gate now uses `|| { echo …; exit 1; }`, not bare `|| echo`.

> Do NOT grep for bare `|| echo` and expect zero — non-blocking `⚠️` warnings legitimately keep that form (wf-plan CONSTITUTION line, wf-execute CONSTITUTION line). The check above targets only blocking gates.

Run: `grep -c "STOP" .agents/skills/wf-plan/SKILL.md .agents/skills/wf-execute/SKILL.md .agents/skills/wf-new-milestone/SKILL.md .agents/skills/wf-complete-milestone/SKILL.md`
Expected: Each file has at least 1 STOP instruction
