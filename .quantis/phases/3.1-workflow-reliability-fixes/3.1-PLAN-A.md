---
phase: 3.1
plan: skill-loading-fixes
wave: 1
---

# Phase 3.1: Skill Loading Fixes - Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development (recommended) or executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix discuss-phase.md and verify.md so they reliably load their delegated skills and produce required artifacts.

**Architecture:** Direct edits to two workflow markdown files. No code, no tests — verification is reading the file and confirming the instructions are unambiguous.

**Tech Stack:** Markdown workflow files

---

### Task 1: Fix discuss-phase.md — Enforce Full Brainstorming Checklist

**Files:**
- Modify: `.agent/workflows/discuss-phase.md:28-36`

- [ ] **Step 1: Replace the "discuss/explore mode" instruction in Step 2**

Replace the current Step 2 content (lines 28-36):

```markdown
## 2. Brainstorm
**Read and follow `.agents/skills/brainstorming/SKILL.md`** in discuss/explore mode.

Present structured discussion points:
- **Scope**: What's in, what's out?
- **Concerns**: What could go wrong?
- **Dependencies**: What must exist before this works?
- **Risks**: What's the biggest unknown?
- **Alternatives**: Is there a simpler way?
```

With this:

```markdown
## 2. Brainstorm
**Read and follow `.agents/skills/brainstorming/SKILL.md` — complete the full checklist** including:
- Exploring context & asking clarifying questions
- Proposing 2-3 approaches with trade-offs
- Presenting design sections for approval
- **Writing SPEC.md** to `.quantis/phases/{N}.{M}-{slug}/SPEC.md`
- Running spec self-review
- Getting user approval on the written spec

Use the structured discussion points below to guide the brainstorming conversation:
- **Scope**: What's in, what's out?
- **Concerns**: What could go wrong?
- **Dependencies**: What must exist before this works?
- **Risks**: What's the biggest unknown?
- **Alternatives**: Is there a simpler way?

<HARD-GATE>
The brainstorming skill MUST produce a SPEC.md file. Do NOT skip the spec-writing, self-review, or user-review steps. If brainstorming completes without a SPEC.md in the phase directory, the workflow has failed.
</HARD-GATE>
```

- [ ] **Step 2: Verify the edit**

Run: `grep -c "discuss/explore mode" .agent/workflows/discuss-phase.md`
Expected: `0` (the phrase should no longer appear)

Run: `grep -c "HARD-GATE" .agent/workflows/discuss-phase.md`
Expected: `1` (the gate should exist)

Run: `grep -c "SPEC.md" .agent/workflows/discuss-phase.md`
Expected: At least `2` (referenced in the checklist and the hard gate)

- [ ] **Step 3: Commit**

```bash
git add .agent/workflows/discuss-phase.md
git commit -m "fix(discuss-phase): enforce full brainstorming checklist and SPEC.md creation"
```

---

### Task 2: Fix verify.md — Explicitly Load Verification Skill in Process Steps

**Files:**
- Modify: `.agent/workflows/verify.md:57-59`

- [ ] **Step 1: Add a new Step 1.5 between "Load Verification Context" and "Extract Must-Haves"**

After the current Step 1 block (which ends at the `---` on line 57), insert a new step before Step 2:

```markdown
## 1.5. Load Verification Methodology

**Read and follow `.agents/skills/verification-before-completion/SKILL.md`** for the full verification methodology. This skill defines:
- The Iron Law: no completion claims without fresh verification evidence
- The Gate Function: identify → run → read → verify → claim
- Common failure patterns and required evidence types

Apply this methodology to every must-have verification in Step 3.

---
```

- [ ] **Step 2: Verify the edit**

Run: `grep -c "Read and follow.*verification-before-completion" .agent/workflows/verify.md`
Expected: `1` (the explicit skill-loading instruction in Step 1.5)

Run: `grep -n "## 1.5" .agent/workflows/verify.md`
Expected: Shows a line number between Step 1 and Step 2, confirming correct placement

- [ ] **Step 3: Commit**

```bash
git add .agent/workflows/verify.md
git commit -m "fix(verify): add explicit step to load verification-before-completion skill"
```
