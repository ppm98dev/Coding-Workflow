---
phase: 3.1
plan: lifecycle
wave: 2
gap_closure: false
---

# Phase 3.1 · Lifecycle Workflows — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: subagent-driven-development (or executing-plans). Steps use checkbox (`- [ ]`).

**Goal:** Pause/resume/progress/milestones/context-health/debugging — state, gates, error paths.

**Architecture:** **One task per file** — every edit for that file is applied in the order listed (top-to-bottom), each anchored to the live file (or the result of the prior edit in the same task). This is the full, conflict-free set: all 150 edits across 32 files were simulated in-order and verified to apply with zero collisions. No file is edited by more than one task. Prose edits only.

---

## Task 1: `.agents/skills/wf-pause/SKILL.md`  (4 edits)

_sources: new, original_

- [ ] **1. Insert a new '## 2.5. Reconcile ROADMAP.md' section between '## 2. Add Journal Entry' and '## 3. Commit State' (after the JOURNAL.md fenced template, before the '---' that precedes '## 3. Commit State')** `[original:completion-ownership Task3 Step1]`

Find (current text):
````
### Handoff Notes
{Critical info for resuming}
```

---

## 3. Commit State
````

Replace with:
````
### Handoff Notes
{Critical info for resuming}
```

---

## 2.5. Reconcile ROADMAP.md

Check for phase-state drift between STATE.md/phase directories and ROADMAP.md:

For each non-Complete phase that has a directory in `.quantis/phases/`:
- If `VERIFICATION.md` exists and contains `verdict: PASS` → mark `✅ Complete` in ROADMAP (idempotent)
- If all `*-PLAN.md` files have matching `*-SUMMARY.md` but no `VERIFICATION.md` → leave status, set Next Steps to `/verify {N}`
- Otherwise → leave as-is

**Never** mark a phase Complete based on SUMMARYs alone — require a verdict-based VERIFICATION.md.

---

## 3. Commit State
````

- [ ] **2. Section '## 3. Commit State'; add ROADMAP.md to the git add line** `[original:completion-ownership Task3 Step2]`

Find (current text):
````
git add .quantis/STATE.md .quantis/JOURNAL.md
git commit -m "docs: pause session - {brief reason}"
````

Replace with:
````
git add .quantis/STATE.md .quantis/JOURNAL.md .quantis/ROADMAP.md
git commit -m "docs: pause session - {brief reason}"
````

- [ ] **3. <proactive_state_save> ### When to Auto-Save table, first row (~line 155)** `[new:Replace unmeasurable context-% trigger w]`

Find (current text):
````
| Context usage reaches ~50-70% | Write lightweight state snapshot to `.quantis/STATE.md` |
````

Replace with:
````
| Observable proxies — any of: 20+ tool calls this session, 10+ files read in full, the platform reports a context warning, or you notice yourself re-reading files you already summarized | Write lightweight state snapshot to `.quantis/STATE.md` |
````

- [ ] **4. ~149, Proactive Auto-Save > Solution line** `[new:Harden wf-pause proactive auto-save to M]`

Find (current text):
````
**Solution:** The agent should auto-save state BEFORE limits are hit.
````

Replace with:
````
**Solution:** The agent MUST auto-save state BEFORE limits are hit. When any trigger in the table fires, writing the snapshot to `.quantis/STATE.md` is mandatory and comes BEFORE any message to the user.
````

- [ ] **Verify:**
  - `grep -c '~50-70%' .agents/skills/wf-pause/SKILL.md` → 0
  - `grep -c '20+ tool calls this session' .agents/skills/wf-pause/SKILL.md` → 1
  - `grep -c 'The agent MUST auto-save state BEFORE limits are hit' .agents/skills/wf-pause/SKILL.md` → 1
  - `grep -c 'should auto-save state BEFORE limits' .agents/skills/wf-pause/SKILL.md` → 0

- [ ] **Commit:** `git add .agents/skills/wf-pause/SKILL.md && git commit -m "fix(phase-3.1): wf-pause"`

---

## Task 2: `.agents/skills/wf-resume-session/SKILL.md`  (2 edits)

_sources: new_

- [ ] **1. Step 1 Load Saved State — the single line under the heading** `[new:wf-resume-session: missing/stub STATE.md]`

Find (current text):
````
## 1. Load Saved State

Read `.quantis/STATE.md` completely.
````

Replace with:
````
## 1. Load Saved State

Read `.quantis/STATE.md` completely.

**If STATE.md is missing or a stub (no `## Current Position` section):** do not invent a position. Reconstruct from durable evidence and say you are doing so:
1. `.quantis/ROADMAP.md` — the lowest non-Complete phase is the likely current phase.
2. `git log --oneline -15` — recent `feat(phase-N)`/`docs(phase-N)` commits show what was last worked on.
3. `.quantis/phases/` — a `*-PLAN.md` without a matching `*-SUMMARY.md` indicates an in-progress phase.
Present the reconstructed position to the user for confirmation before continuing. If nothing can be reconstructed, STOP and suggest `/progress` or `/new-project`.
````

- [ ] **2. Step 4 Check for Conflicts — the "If changes found" warning block** `[new:wf-resume-session: missing/stub STATE.md]`

Find (current text):
````
**If changes found:**
```
⚠️ UNCOMMITTED CHANGES DETECTED

{list of modified files}

These may be from the previous session.
Review before proceeding.
```
````

Replace with:
````
**If changes found, run this procedure — do not just print a warning and continue:**
1. Compare each dirty file against STATE.md's In-Progress / "Files modified" list.
2. **Files listed in STATE.md** (expected work-in-progress): summarize `git diff` for each, then offer the user a choice — (a) commit as `wip(phase-N): resume checkpoint`, or (b) leave uncommitted and continue the task.
3. **Files NOT listed in STATE.md** (unexpected): show them and ASK the user what they are before doing anything — do not commit or revert on your own.
4. Wait for the user's decision before marking the session active (Step 5).
```
⚠️ UNCOMMITTED CHANGES DETECTED

{list of modified files}

Resolve via the procedure above before continuing.
```
````

- [ ] **Verify:**
  - `grep -n "Reconstruct from durable evidence\|wip(phase-N): resume checkpoint\|do not just print a warning" .agents/skills/wf-resume-session/SKILL.md` → Three matches: the reconstruction branch, the wip-commit option, and the procedure intro. "Review before proceeding" no longer appears as a terminal instruction.

- [ ] **Commit:** `git add .agents/skills/wf-resume-session/SKILL.md && git commit -m "fix(phase-3.1): wf-resume-session"`

---

## Task 3: `.agents/skills/wf-progress/SKILL.md`  (2 edits)

_sources: new_

- [ ] **1. ## 2. Calculate Progress, count list (~lines 24-29)** `[new:Fix wf-progress status legend (⏸ = CLOSE]`

Find (current text):
````
Count phases:
- Total phases
- Completed phases (✅)
- In progress (🔄)
- Blocked (⏸️)
- Not started (⬜)
````

Replace with:
````
Count phases:
- Total phases
- Completed phases (✅)
- In progress (🔄)
- Not started (⬜)
- Closed / superseded (⏸) — excluded from the active total
````

- [ ] **2. ~87, Suggest Action table** `[new:Fix wf-progress Celebrate cell to route ]`

Find (current text):
````
| All phases complete | Celebrate! 🎉 |
````

Replace with:
````
| All phases complete | `/complete-milestone` |
````

- [ ] **Verify:**
  - `grep -c 'Blocked (⏸️)' .agents/skills/wf-progress/SKILL.md` → 0
  - `grep -c 'Closed / superseded (⏸)' .agents/skills/wf-progress/SKILL.md` → 1
  - `grep -c 'In progress (🔄)' .agents/skills/wf-progress/SKILL.md` → 1
  - `grep -c 'Celebrate' .agents/skills/wf-progress/SKILL.md` → 0
  - `grep -c '`/complete-milestone`' .agents/skills/wf-progress/SKILL.md` → 1

- [ ] **Commit:** `git add .agents/skills/wf-progress/SKILL.md && git commit -m "fix(phase-3.1): wf-progress"`

---

## Task 4: `.agents/skills/wf-new-milestone/SKILL.md`  (4 edits)

_sources: new, original_

- [ ] **1. Section '## 1. Validate SPEC Exists'; add STOP semantics to the missing-SPEC echo and add a STOP prose line after the bash block** `[original:execution-contract-and-gates Task3 Step2]`

Find (current text):
````
```bash
if [ ! -f ".quantis/SPEC.md" ]; then
    echo "Error: SPEC.md required. Run /new-project first." >&2
fi
```
````

Replace with:
````
```bash
if [ ! -f ".quantis/SPEC.md" ]; then
    echo "❌ STOP: SPEC.md required. Run /new-project first." >&2
    exit 1
fi
```

**If SPEC.md is missing: STOP. Do not gather milestone information.**
````

- [ ] **2. Section '## 5b. Reset Session Files (if starting fresh)'; insert the archive guard before the truncation bash block, after the section's intro sentence** `[original:execution-contract-and-gates Task3 Step2]`

Find (current text):
````
If DECISIONS.md or JOURNAL.md contain entries from a previous milestone, reset them to prevent monolithic growth:

```bash
if [ -f ".quantis/DECISIONS.md" ] && [ "$(wc -l < .quantis/DECISIONS.md)" -gt 5 ]; then
````

Replace with:
````
If DECISIONS.md or JOURNAL.md contain entries from a previous milestone, reset them to prevent monolithic growth:

**Before resetting:** Verify the current DECISIONS.md and JOURNAL.md have been archived.
```bash
# Check if archive exists for current milestone
if [ -f ".quantis/DECISIONS.md" ] && [ "$(wc -l < .quantis/DECISIONS.md)" -gt 5 ]; then
    if ! find .quantis/milestones -name "DECISIONS.md" -newer .quantis/DECISIONS.md 2>/dev/null | grep -q .; then
        echo "❌ STOP: DECISIONS.md has unarchived content. Run /complete-milestone first."
        exit 1
    fi
fi
```

```bash
if [ -f ".quantis/DECISIONS.md" ] && [ "$(wc -l < .quantis/DECISIONS.md)" -gt 5 ]; then
````

- [ ] **3. ## 4. Update ROADMAP.md heading + intro (~lines 53-55)** `[new:Make wf-new-milestone Step 4 append, not]`

Find (current text):
````
## 4. Update ROADMAP.md

```markdown
# ROADMAP.md
````

Replace with:
````
## 4. Append to ROADMAP.md

Do NOT overwrite the file. Update the `> **Current Milestone**:` line at the top, then append the section below as a new milestone — preserve all previous milestone sections.

```markdown
# ROADMAP.md
````

- [ ] **4. Step '## 5. Update STATE.md' heading + intro (line 80-82)** `[new:wf-new-milestone: edit STATE.md Current ]`

Find (current text):
````
## 5. Update STATE.md

```markdown
## Current Position
- **Milestone**: {name}
- **Phase**: Not started
- **Status**: Milestone planned
```
````

Replace with:
````
## 5. Update STATE.md

Edit `.quantis/STATE.md` IN PLACE — set the fields inside the existing `## Current Position` section (canonical schema in `.quantis/templates/state.md`); leave all other sections intact:

```markdown
## Current Position
- **Phase**: Not started — milestone {name} planned
- **Task**: —
- **Status**: Milestone planned
```
````

- [ ] **Verify:**
  - `grep -c '## 4. Append to ROADMAP.md' .agents/skills/wf-new-milestone/SKILL.md` → 1
  - `grep -c 'preserve all previous milestone sections' .agents/skills/wf-new-milestone/SKILL.md` → 1
  - `grep -c 'IN PLACE' /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-new-milestone/SKILL.md` → 1 (in-place STATE edit rule added)

- [ ] **Commit:** `git add .agents/skills/wf-new-milestone/SKILL.md && git commit -m "fix(phase-3.1): wf-new-milestone"`

---

## Task 5: `.agents/skills/wf-complete-milestone/SKILL.md`  (6 edits)

_sources: new, original_

- [ ] **1. Step 1 Verify All Phases Complete — milestone-scoped incomplete check + STOP (merged: orig STOP gate + new milestone-scoping)** `[original:execution-contract-and-gates Task3 Step3]`

Find (current text):
````
```bash
# Check ROADMAP.md for incomplete phases
grep -E "Status.*Not Started|Status.*In Progress" ".quantis/ROADMAP.md"
```

**If incomplete phases found:**
```
⚠️ Cannot complete milestone — {N} phases incomplete

Run /progress to see status.
```
````

Replace with:
````
```bash
# Scope to the CURRENT milestone only — archived (⏸ CLOSED/SUPERSEDED) sections above contain
# stale "Not Started"/"In Progress" text that would false-positive against the whole file.
INCOMPLETE=$(sed -n '/Current Milestone/,$p' ".quantis/ROADMAP.md" | grep -c -E "Status.*Not Started|Status.*In Progress" || true)
if [ "$INCOMPLETE" -gt 0 ]; then
    echo "❌ STOP: Cannot complete milestone — $INCOMPLETE phases incomplete."
    echo "Run /progress to see status."
    exit 1
fi
```

**If ANY phase is incomplete: STOP.** Do not proceed to verification or archiving.
````

- [ ] **2. Step 2, ## 2. Run Final Verification — append GATE after the bullet list, before the --- separator** `[new:wf-complete-milestone: final-verificatio]`

Find (current text):
````
Verify all must-haves from ROADMAP.md:
- Run verification commands
- Capture evidence
- Create VERIFICATION.md if not exists

---
````

Replace with:
````
Verify all must-haves from ROADMAP.md:
- Run verification commands
- Capture evidence
- Create VERIFICATION.md if not exists

**GATE:** If any must-have FAILS verification, STOP. Do not generate the summary (Step 3), archive (Step 4 — destructive), reset (Step 5), or tag (Step 6). Report which must-haves failed and route to `/verify {N}` or `/execute {N} --gaps-only`, then re-run /complete-milestone once they pass.

---
````

- [ ] **3. Step 4, ## 4. Archive Current State — add post-mv verification inside the bash block before the closing ```** `[new:wf-complete-milestone: final-verificatio]`

Find (current text):
````
# Archive decisions and journal (prevent monolithic growth across milestones)
[ -f ".quantis/DECISIONS.md" ] && cp ".quantis/DECISIONS.md" ".quantis/milestones/{name}/DECISIONS.md"
[ -f ".quantis/JOURNAL.md" ] && cp ".quantis/JOURNAL.md" ".quantis/milestones/{name}/JOURNAL.md"
```
````

Replace with:
````
# Archive decisions and journal (prevent monolithic growth across milestones)
[ -f ".quantis/DECISIONS.md" ] && cp ".quantis/DECISIONS.md" ".quantis/milestones/{name}/DECISIONS.md"
[ -f ".quantis/JOURNAL.md" ] && cp ".quantis/JOURNAL.md" ".quantis/milestones/{name}/JOURNAL.md"

# GATE: confirm the archive received the files before any reset
if [ -z "$(ls -A ".quantis/milestones/{name}/" 2>/dev/null)" ] || [ -n "$(ls -A .quantis/phases 2>/dev/null)" ]; then
    echo "❌ STOP: archive incomplete — .quantis/phases still has contents or the milestone dir is empty."
    echo "Do not proceed to Step 5 (reset). Investigate the mv/mkdir before continuing."
    exit 1
fi
```

**If the STOP line printed: halt.** Step 5 resets ROADMAP.md, DECISIONS.md, and JOURNAL.md — never reset until this guard confirms the archive received the files.
````

- [ ] **4. ~117, Step 5c item 3** `[new:Fix wf-complete-milestone STACK.md path ]`

Find (current text):
````
3. **Update STACK.md** — refresh technology and dependency information
````

Replace with:
````
3. **Update `.quantis/STACK.md`** (if it exists) — refresh technology and dependency information; skip if the project doesn't maintain one
````

- [ ] **5. ~146, step heading** `[new:Fix wf-complete-milestone STACK.md path ]`

Find (current text):
````
## 7. Celebrate
````

Replace with:
````
## 7. Display Completion
````

- [ ] **6. Step '## 5. Reset for Next Milestone', the 'Update STATE.md to show milestone complete.' line (line 87)** `[new:wf-complete-milestone: give 'Update STAT]`

Find (current text):
````
Clear ROADMAP.md phases section (keep header).
Update STATE.md to show milestone complete.
````

Replace with:
````
Clear ROADMAP.md phases section (keep header).
Edit `.quantis/STATE.md` IN PLACE (canonical schema in `.quantis/templates/state.md`) — set:
```markdown
## Current Position
- **Phase**: —
- **Task**: Milestone {name} complete and archived
- **Status**: Milestone complete — ready for /new-milestone
```
````

- [ ] **Verify:**
  - `grep -c 'GATE:' .agents/skills/wf-complete-milestone/SKILL.md` → At least 2 (one in Step 2 final-verification, one in Step 4 archive guard)
  - `grep -n 'STOP' .agents/skills/wf-complete-milestone/SKILL.md` → Matches in both Step 2 (final-verification failure) and Step 4 (post-mv archive guard)
  - `grep -c '`.quantis/STACK.md`' .agents/skills/wf-complete-milestone/SKILL.md` → 1
  - `grep -c '## 7. Celebrate' .agents/skills/wf-complete-milestone/SKILL.md` → 0
  - `grep -c 'Milestone {name} complete and archived' /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-complete-milestone/SKILL.md` → 1 (concrete STATE field list added)
  - `grep -n "sed -n '/Current Milestone/,\$p'\|GATE: verify the archive succeeded\|mv incomplete" .agents/skills/wf-complete-milestone/SKILL.md` → Three matches: the scoped grep slice, the archive-verification gate comment, and the STOP condition.

- [ ] **Commit:** `git add .agents/skills/wf-complete-milestone/SKILL.md && git commit -m "fix(phase-3.1): wf-complete-milestone"`

---

## Task 6: `.agents/skills/context-health-monitor/SKILL.md`  (6 edits)

_sources: new, original_

- [ ] **1. Section '### Rule 1: The 3-Strike Rule'; replace the four-step body with the reconciled protocol plus cross-reference note (before '### Rule 2: Circular Detection')** `[original:methodology-overrides-and-rules Task3 Step4]`

Find (current text):
````
### Rule 1: The 3-Strike Rule

If debugging the same issue fails 3 times:

1. **STOP** attempting fixes
2. **Document** in `.quantis/STATE.md`:
   - What was tried
   - What errors occurred
   - Current hypothesis
3. **Recommend** user start fresh session
4. **Do NOT** continue with more attempts
````

Replace with:
````
### Rule 1: The 3-Strike Rule

If debugging the same issue fails 3 times:

1. **Auto-save state** — Document in `.quantis/STATE.md`: what was tried, what errors occurred, current hypothesis
2. **Assess the pattern:**
   - If failures suggest an architectural problem (same area, same class of error) → **discuss with user** before attempting more fixes (see systematic-debugging Phase 4.5 — the file has Four Phases plus a "Phase 4.5"; there is no "Phase 5")
   - Otherwise → **recommend `/pause`** for a fresh session with fresh context
3. **Do NOT** continue with attempt #4 without either user discussion or fresh session

> **Cross-reference:** systematic-debugging § Phase 4.5 (architectural discussion), wf-execute § context_hygiene (/pause recommendation). This protocol supersedes both when they conflict.
````

- [ ] **2. ~105, Integration list** `[new:Fix context-health-monitor GEMINI.md ref]`

Find (current text):
````
- Rule 3 in `GEMINI.md` — Context Hygiene enforcement
````

Replace with:
````
- `.agents/rules/PROJECT_RULES.md` § Context Management — Context Hygiene rules
````

- [ ] **3. ~14, When This Skill Activates** `[new:Fix context-health-monitor GEMINI.md ref]`

Find (current text):
````
The agent should self-monitor for these warning signs:
````

Replace with:
````
The agent MUST self-monitor for these warning signs, and MUST take the listed Action the moment a threshold is hit — these are not suggestions:
````

- [ ] **4. Rule 3: Uncertainty Logging — DECISIONS.md write list (lines 52-57)** `[new:context-health-monitor: edit STATE.md in]`

Find (current text):
````
2. **Document** in `.quantis/DECISIONS.md`:
   - The uncertain decision
   - Why it's uncertain
   - Alternatives considered
````

Replace with:
````
2. **Document** in `.quantis/DECISIONS.md` using the canonical `D-{NNN}` format (see `.quantis/templates/decisions.md`; `NNN` = next integer after the highest existing `D-` ID):
   - **Decision:** the uncertain decision
   - **Rationale:** why it's uncertain + alternatives considered
````

- [ ] **5. State Dump Format intro line (line 61) and the fenced block heading** `[new:context-health-monitor: edit STATE.md in]`

Find (current text):
````
When triggered, write to `.quantis/STATE.md`:

```markdown
## Context Health: State Dump
````

Replace with:
````
When triggered, edit `.quantis/STATE.md` IN PLACE — update the Phase/Task/Status fields in the existing `## Current Position` section and record the detail under `## Context Dump` (canonical schema in `.quantis/templates/state.md`; never replace the file or add a competing top-level section):

```markdown
## Context Dump → Context Health
````

- [ ] **6. Auto-Save Protocol → Steps, item 1 (line 92)** `[new:context-health-monitor: edit STATE.md in]`

Find (current text):
````
1. **Write** a state snapshot to `.quantis/STATE.md` immediately when a threshold is hit
````

Replace with:
````
1. **Write** a state snapshot to `.quantis/STATE.md` immediately when a threshold is hit — edit the canonical `## Current Position` fields in place (schema in `.quantis/templates/state.md`); do not overwrite other sections
````

- [ ] **Verify:**
  - `grep -c 'GEMINI.md' .agents/skills/context-health-monitor/SKILL.md` → 0
  - `grep -c 'MUST self-monitor' .agents/skills/context-health-monitor/SKILL.md` → 1
  - `grep -c 'canonical `D-{NNN}` format' /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/context-health-monitor/SKILL.md` → 1 (Rule 3 pinned to D-{NNN})
  - `grep -c 'IN PLACE\|in place' /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/context-health-monitor/SKILL.md` → 2 (State Dump + Auto-Save both edit in place)

- [ ] **Commit:** `git add .agents/skills/context-health-monitor/SKILL.md && git commit -m "fix(phase-3.1): context-health-monitor"`

---

## Task 7: `.agents/skills/systematic-debugging/SKILL.md`  (1 edits)

_sources: original_

- [ ] **1. Section '5. If 3+ Fixes Failed: Question Architecture' (around lines 199-213); add the cross-reference note after 'This is NOT a failed hypothesis - this is a wrong architecture.', before '## Red Flags'** `[original:methodology-overrides-and-rules Task3 Step4]`

Find (current text):
````
   This is NOT a failed hypothesis - this is a wrong architecture.

## Red Flags - STOP and Follow Process
````

Replace with:
````
   This is NOT a failed hypothesis - this is a wrong architecture.

> **Cross-reference:** See context-health-monitor Rule 1 for the unified 3-strike protocol.

## Red Flags - STOP and Follow Process
````

- [ ] **Commit:** `git add .agents/skills/systematic-debugging/SKILL.md && git commit -m "fix(phase-3.1): systematic-debugging"`

---
