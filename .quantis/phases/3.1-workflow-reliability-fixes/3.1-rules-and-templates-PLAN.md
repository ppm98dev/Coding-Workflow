---
phase: 3.1
plan: rules-and-templates
wave: 2
gap_closure: false
---

# Phase 3.1 · Rules & Templates — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: subagent-driven-development (or executing-plans). Steps use checkbox (`- [ ]`).

**Goal:** Rules layer + the document templates (state-schema unification, dead refs, CONSTITUTION).

**Architecture:** **One task per file** — every edit for that file is applied in the order listed (top-to-bottom), each anchored to the live file (or the result of the prior edit in the same task). This is the full, conflict-free set: all 150 edits across 32 files were simulated in-order and verified to apply with zero collisions. No file is edited by more than one task. Prose edits only.

---

## Task 1: `.agents/rules/PROJECT_RULES.md`  (7 edits)

_sources: new, original_

- [ ] **1. Section '## Wave Execution'; replace the three wave-table rows (lines 66-68) and append the ordering note** `[original:methodology-overrides-and-rules Task3 Step1]`

Find (current text):
````
| 1 | Foundation tasks, no dependencies | Run in parallel |
| 2 | Depends on Wave 1 | Wait for Wave 1, then parallel |
| 3 | Depends on Wave 2 | Wait for Wave 2, then parallel |
````

Replace with:
````
| 1 | Foundation tasks, no dependencies | Execute sequentially |
| 2 | Depends on Wave 1 | Wait for Wave 1 complete, then execute |
| 3 | Depends on Wave 2 | Wait for Wave 2 complete, then execute |

> **Note:** Waves define *ordering constraints*, not parallelism. Implementation is always sequential (one subagent per task). Parallel dispatch is reserved for read-only investigation (see dispatching-parallel-agents skill).
````

- [ ] **2. Section '## Token Efficiency Rules' → '### Loading Rules'; insert exemptions after the Loading Rules table (after the '>5 files needed' row), before '### Budget Thresholds'** `[original:methodology-overrides-and-rules Task3 Step2]`

Find (current text):
````
| >5 files needed | Stop, reconsider approach |

### Budget Thresholds
````

Replace with:
````
| >5 files needed | Stop, reconsider approach |

**Exemptions:** The following are always read in full regardless of size:
- SKILL.md files invoked via "Read and follow" directives
- `.quantis/` state files (STATE.md, ROADMAP.md, SPEC.md)
- Any file a skill explicitly says to read completely
- The >5-file cap applies to source code exploration only, not skill/state loading.

### Budget Thresholds
````

- [ ] **3. ## Token Efficiency Rules > ### Budget Thresholds table (~lines 217-221)** `[new:Replace unmeasurable budget thresholds w]`

Find (current text):
````
| Usage | Action Required |
|-------|-----------------|
| 0-50% | Proceed normally |
| 50-70% | Switch to outline mode, compress context |
| 70%+ | State dump required, recommend fresh session |
````

Replace with:
````
Models cannot introspect token usage, so gate on observable proxies instead.

| Observed signal | Action Required |
|-----------------|-----------------|
| Fewer than ~10 files read in full | Proceed normally |
| 10+ files read in full, or 20+ tool calls this session | Switch to outline mode, compress context, write a lightweight STATE.md snapshot (see wf-pause Proactive Auto-Save) |
| Platform reports a context warning, or you are re-reading files you already summarized | State dump required, recommend fresh session |
````

- [ ] **4. ~122-125, Model Independence > Adapter Pattern** `[new:Fix PROJECT_RULES repo tree paths and em]`

Find (current text):
````
**Adapter Pattern:**
```
adapters/
```

Each adapter must begin with:
````

Replace with:
````
**Adapter Pattern:**

The only adapter today is `adapters/ANTIGRAVITY.md`. Each adapter must begin with:
````

- [ ] **5. ~158-161, Repository Structure tree (top)** `[new:Fix PROJECT_RULES repo tree paths and em]`

Find (current text):
````
```
PROJECT_RULES.md          # ← This file (canonical rules, in .agents/rules/)
QUANTIS-STYLE.md              # Style and conventions (in .agents/rules/)

.agents/
└── skills/               # Workflows (wf-*) + methodology skills
````

Replace with:
````
```
.agents/
├── rules/
│   ├── PROJECT_RULES.md  # ← This file (canonical rules)
│   ├── QUANTIS-STYLE.md  # Style and conventions
│   └── CONSTITUTION.md   # Project quality standards
└── skills/               # Workflows (wf-*) + methodology skills
````

- [ ] **6. ~175, Repository Structure tree (.quantis/ subtree)** `[new:Fix PROJECT_RULES repo tree paths and em]`

Find (current text):
````
├── templates/            # Document templates
└── examples/             # Usage examples
````

Replace with:
````
├── templates/            # Document templates
└── milestones/           # Archived milestone summaries
````

- [ ] **7. Commit Conventions section — after the Rules bullet list, before the closing horizontal rule** `[new:PROJECT_RULES: add Commit Failure Rule r]`

Find (current text):
````
**Rules:**
- One task = one commit
- Verify before commit
- Scope = phase number for phase work (e.g., `feat(phase-1): ...`)
````

Replace with:
````
**Rules:**
- One task = one commit
- Verify before commit
- Scope = phase number for phase work (e.g., `feat(phase-1): ...`)

**Commit Failure Rule** (applies to every workflow that commits — wf-execute, wf-plan, wf-pause, wf-verify, wf-new-milestone, wf-complete-milestone): after each `git commit`, confirm it succeeded (exit code 0, e.g. `git log -1` shows the new commit). On failure, route by cause — do not silently proceed:
- **Pre-commit hook rejection** — fix the reported issues, then re-commit. Never bypass with `--no-verify`.
- **Merge/rebase in progress** (`.git/MERGE_HEAD` / rebase dir exists) — STOP and ask the user before touching the conflict.
- **Detached HEAD** — STOP; ask the user to check out a branch before committing.
- **Nothing to commit** — not an error: report it and continue (the intended files may already be committed).
````

- [ ] **Verify:**
  - `grep -c '| 70%+ | State dump required' .agents/rules/PROJECT_RULES.md` → 0
  - `grep -c 'Models cannot introspect token usage' .agents/rules/PROJECT_RULES.md` → 1
  - `grep -c 'examples/' .agents/rules/PROJECT_RULES.md` → 0
  - `grep -A2 '\*\*Adapter Pattern:\*\*' .agents/rules/PROJECT_RULES.md | grep -c 'ANTIGRAVITY.md'` → 1
  - `grep -c '│   ├── PROJECT_RULES.md' .agents/rules/PROJECT_RULES.md` → 1
  - `grep -n "Commit Failure Rule\|Merge/rebase in progress\|Detached HEAD\|Nothing to commit" .agents/rules/PROJECT_RULES.md` → Four matches covering the rule heading and all three blocking/non-blocking failure cases.

- [ ] **Commit:** `git add .agents/rules/PROJECT_RULES.md && git commit -m "fix(phase-3.1): PROJECT_RULES.md"`

---

## Task 2: `.agents/rules/QUANTIS-STYLE.md`  (5 edits)

_sources: new, original_

- [ ] **1. Insert a new '## Delegation Pattern' section after '## UX Patterns' (after its Decision Gates fenced block) and before '## Summary: Core Meta-Patterns'** `[original:methodology-overrides-and-rules Task3 Step3]`

Find (current text):
````
### Decision Gates

When user input needed:
```
⚠️ DECISION REQUIRED

Option A: {description}
Option B: {description}

Which do you prefer?
```

---

## Summary: Core Meta-Patterns
````

Replace with:
````
### Decision Gates

When user input is genuinely needed for a decision the system cannot resolve:
```
⚠️ DECISION REQUIRED

Option A: {description}
Option B: {description}

Which do you prefer?
```

**Never use Decision Gates for:** execution mode selection, tool availability checks, platform detection, or any choice a skill resolves automatically.

---

## Delegation Pattern

When a workflow delegates to a skill:
1. **"Read and follow `path/to/SKILL.md`"** as a numbered process step — never in `<context>`.
2. **Subagent branches paste content, not paths** — subagents don't inherit context, so the orchestrator must read the skill file and paste its full text into the subagent prompt.
3. **Platform Check block** (when present): Step 0, MUST dispatch when `invoke_subagent` is available, include "then continue at Step N" to prevent orphaned tail steps.

---

## Summary: Core Meta-Patterns
````

- [ ] **2. At the top of the '### Task Structure' XML section under '## XML Tag Conventions'; insert the checkbox plan-format note before the xml code fence** `[original:methodology-overrides-and-rules Task3 Step3b]`

Find (current text):
````
### Task Structure

```xml
<task type="auto" effort="medium">
````

Replace with:
````
### Task Structure

> **Plan task format:** Implementation plans use the **checkbox** task format defined in `writing-plans` (`### Task N` with `- [ ]` steps, `Run:`/`Expected:` verification). The XML `<task type="...">` structure below is retained ONLY for (a) the task-type/effort vocabulary (`auto`, `checkpoint:human-verify`, `checkpoint:decision`, effort levels) referenced by `/plan`, and (b) gap-closure fix plans generated by `/verify`. Do not author normal plans in XML.

```xml
<task type="auto" effort="medium">
````

- [ ] **3. ~3, header blockquote** `[new:Fix QUANTIS-STYLE orphaned line and SSoT]`

Find (current text):
````
> **Comprehensive reference.** Core rules auto-load from `.gemini/GEMINI.md`. This document provides deep explanations and examples for when you need the full picture.
````

Replace with:
````
> **Comprehensive reference.** Canonical rules live in `.agents/rules/PROJECT_RULES.md` (the Single Source of Truth, auto-discovered on all platforms; `.gemini/GEMINI.md` is a Gemini-only adapter). This document provides deep explanations and examples for when you need the full picture.
````

- [ ] **4. ~37-40, Templates section** `[new:Fix QUANTIS-STYLE orphaned line and SSoT]`

Find (current text):
````
Reusable document structures. Copy, don't reference.


Read-only documentation and examples.
````

Replace with:
````
Reusable document structures. Copy, don't reference.
````

- [ ] **5. Immediately before the '## UX Patterns' heading (line 229), insert a new top-level section.** `[new:QUANTIS-STYLE: add canonical Platform Ch]`

Find (current text):
````
## UX Patterns

### Banners
````

Replace with:
````
## Subagent Dispatch & Delegation

### Canonical Platform Check Block

Every workflow that can offload work to a subagent opens with `## 0. Platform Check` (inside `<process>`). State it identically everywhere using this block, filling the bracketed slots:

```markdown
## 0. Platform Check

**If `invoke_subagent` is available** (CLI `agy`, Standalone): **you MUST dispatch** — do not do this work inline. Dispatch a [`self` | `research`] subagent (for parallel fan-out: read `.agents/skills/dispatching-parallel-agents/SKILL.md`, then invoke all subagents together). The subagent prompt MUST contain, **pasted in full** (subagents do NOT inherit your context — paste CONTENTS, never paths):
- [each input the subagent needs: skill text, spec text, templates, the exact task]
**Required return format:** [the exact structure/file the subagent must produce].
When the subagent(s) return, **continue at Step {N}** (review their output, then run the remaining inline steps yourself).

**If `invoke_subagent` is NOT available** (IDE): proceed inline from Step 1.

**If a dispatch fails or returns unusable output** (missing file, empty report, non-empirical evidence): re-dispatch once with explicit feedback on what was wrong; on a second failure, fall back to the inline procedure and say so.

> Detection is automatic. Never ask the user which mode to use.
```

**Subagent types** (defined in `.agents/skills/using-quantis/references/antigravity-tools.md`): `research` = read-only codebase navigation/exploration; `self` = clone of the calling agent with the same capabilities; `browser` = sandboxed web operations. Name the type explicitly at every dispatch site.

### Paste, Don't Reference

A subagent starts with an empty context. Name-dropping a skill ("the writing-plans skill", "brainstorming in critique mode") is unresolvable — the subagent has no access to your files unless you give it the **text** or tell it the **exact path to read**. Default to pasting CONTENTS. The only inputs that may be passed as a path are ones you instruct the subagent to read itself, and you must say "read this path".

### Delegation as a Numbered Process Step

When a workflow delegates to a methodology skill, the load instruction is a **numbered process step**, bolded: `**Read and follow .agents/skills/{skill}/SKILL.md exactly.**` — never buried in `<context>` or a metadata blockquote. Subagent branches paste the skill's text into the dispatch instead.

---

## UX Patterns

### Banners
````

- [ ] **Verify:**
  - `grep -c 'Read-only documentation and examples' .agents/rules/QUANTIS-STYLE.md` → 0
  - `grep -c 'auto-load from `.gemini/GEMINI.md`' .agents/rules/QUANTIS-STYLE.md` → 0
  - `grep -c 'Single Source of Truth' .agents/rules/QUANTIS-STYLE.md` → 1
  - `grep -c 'Canonical Platform Check Block\|Paste, Don.t Reference\|continue at Step {N}' /Users/piemelel/GitHub/Coding-Workflow/.agents/rules/QUANTIS-STYLE.md` → 3 (the new section headings and the orphan-prevention clause are present)
  - `grep -n 'platform detection' /Users/piemelel/GitHub/Coding-Workflow/.agents/rules/QUANTIS-STYLE.md` → A match inside the Decision Gates subsection (template now scoped away from auto-resolvable choices)

- [ ] **Commit:** `git add .agents/rules/QUANTIS-STYLE.md && git commit -m "fix(phase-3.1): QUANTIS-STYLE.md"`

---

## Task 3: `.agents/rules/CONSTITUTION.md`  (1 edits)

_sources: new_

- [ ] **1. Governance section — after the three existing bullets, before the Version line** `[new:CONSTITUTION: rule that unfilled [FILL] ]`

Find (current text):
````
- This constitution supersedes personal coding habits during this project.
- All plans and code must comply. Violations require documented justification in the plan.
- Amendments: update this file, document rationale, commit with `docs: amend constitution`.
````

Replace with:
````
- This constitution supersedes personal coding habits during this project.
- All plans and code must comply. Violations require documented justification in the plan.
- Amendments: update this file, document rationale, commit with `docs: amend constitution`.
- **Unfilled placeholders:** if an article still contains a `[FILL: ...]` choice at plan/execute time, treat the FIRST listed option as the active default (e.g. `[FILL: fail-fast / graceful degradation / hybrid]` → fail-fast; `[FILL: mandatory TDD / ...]` → mandatory TDD) and note in the plan that the constitution default was assumed. Workflows MUST NOT stall on unfilled placeholders. A constitution that still contains `[FILL:` is not fully ratified, but defaults keep work moving.
````

- [ ] **Verify:**
  - `grep -n "Unfilled placeholders:\|treat the FIRST listed option as the active default" .agents/rules/CONSTITUTION.md` → Two matches: the Governance bullet heading and the first-option default rule.

- [ ] **Commit:** `git add .agents/rules/CONSTITUTION.md && git commit -m "fix(phase-3.1): CONSTITUTION.md"`

---

## Task 4: `.agents/skills/using-quantis/references/antigravity-tools.md`  (1 edits)

_sources: original_

- [ ] **1. Section '## Subagent Support' (line 21); replace the blanket 'supports subagents natively' sentence** `[original:methodology-overrides-and-rules Task5 Step3]`

Find (current text):
````
Antigravity 2.0 supports subagents natively via `invoke_subagent`. Use `define_subagent` to register named subagent types at session start.
````

Replace with:
````
Antigravity supports subagents via `invoke_subagent` on CLI (`agy`) and Standalone only — the IDE does not have this tool. If `invoke_subagent` is not in your tool list, that is normal, not an error: skills fall back to inline execution automatically, and you must never ask the user whether subagents are available. Use `define_subagent` to register named subagent types at session start.
````

- [ ] **Commit:** `git add .agents/skills/using-quantis/references/antigravity-tools.md && git commit -m "fix(phase-3.1): antigravity-tools.md"`

---

## Task 5: `.quantis/ROADMAP.md`  (1 edits)

_sources: original_

- [ ] **1. Milestone v3.3 → 'Phase 3.1: Workflow Reliability Fixes' deliverables list; rewrite to use wf-* paths and check off the two items v3.2 already shipped (deterministic SDD on execute, browser fallback on verify)** `[original:methodology-overrides-and-rules Task4 Step1]`

Find (current text):
````
**Deliverables:**

- [ ] `discuss-phase.md` — Enforce full brainstorming checklist (always produce SPEC.md)
- [ ] `execute.md` — Deterministically use SDD on Antigravity (no user menu)
- [ ] `verify.md` — Explicitly load verification-before-completion skill in process steps
- [ ] `pause.md` — Add roadmap sync check before pausing
````

Replace with:
````
**Deliverables:**

- [ ] `wf-discuss-phase/SKILL.md` — Enforce full brainstorming checklist (always produce SPEC.md)
- [x] `wf-execute/SKILL.md` — Deterministically use SDD on Antigravity (no user menu) — shipped in Phase 3.2
- [ ] `wf-verify/SKILL.md` — Explicitly load verification-before-completion skill in process steps
- [ ] `wf-pause/SKILL.md` — Add roadmap sync check before pausing

> **Note:** Phase 3.2 (CLI-First Migration) shipped ahead of 3.1, resolving the original 3.2-depends-on-3.1 inversion. The deterministic-SDD and `browser_subagent` graceful-fallback items landed there; remaining 3.1 work targets the `wf-*` skills directly.
````

- [ ] **Commit:** `git add .quantis/ROADMAP.md && git commit -m "fix(phase-3.1): ROADMAP.md"`

---

## Task 6: `.quantis/templates/state.md`  (2 edits)

_sources: new_

- [ ] **1. Replace the entire fenced File Template block (lines 9-58), from the opening ```markdown through the closing ```** `[new:Make wf-pause's STATE.md schema canonica]`

Find (current text):
````
```markdown
---
updated: [ISO timestamp]
---

# Project State

## Current Position

**Milestone:** {name}
**Phase:** {N} - {name}
**Status:** {planning | executing | verifying | blocked}
**Plan:** {current plan if executing}

## Last Action

{What was just completed}

## Next Steps

1. {Immediate next action}
2. {Following action}
3. {Third action if known}

## Active Decisions

Decisions made that affect current work:

| Decision | Choice | Made | Affects |
|----------|--------|------|---------|
| {what} | {choice} | {date} | {phases/plans} |

## Blockers

{None if clear}

- [ ] {Blocker 1}: {resolution approach}
- [ ] {Blocker 2}: {resolution approach}

## Concerns

Things to watch but not blocking:

- {Concern 1}
- {Concern 2}

## Session Context

{Any context the next session needs to know}
```
````

Replace with:
````
```markdown
## Current Position
- **Phase**: {current phase number and name}
- **Task**: {specific task in progress, if any}
- **Status**: {planning | executing | verifying | blocked | paused at {timestamp}}

## Last Session Summary
{What was accomplished this session}

## In-Progress Work
{Any uncommitted changes or partial work}
- Files modified: {list}
- Tests status: {passing/failing/not run}

## Blockers
{What was preventing progress, if anything — "None" if clear}

## Context Dump
{Critical context that would be lost}:

### Decisions Made
- {Decision 1}: {rationale}  (full records live in DECISIONS.md; reference D-{NNN} IDs here if relevant)

### Approaches Tried
- {Approach 1}: {outcome}

### Current Hypothesis
{Best guess at solution/issue}

### Files of Interest
- `{file1}`: {what's relevant}

## Next Steps
1. {Specific first action for next session}
2. {Second priority}
3. {Third priority}
```

> **Canonical schema.** This is the same schema `/pause` writes and `/resume-session` reads. Every other writer (wf-verify, wf-new-milestone, context-health-monitor, SDD/executing-plans auto-save) edits these fields IN PLACE — never replaces the file or invents a new layout. Decisions are NOT stored here; they live in `.quantis/DECISIONS.md`.
````

- [ ] **2. Update Rules section — the 'What to update' list referencing the removed Last Action / Active Decisions fields (lines 70-74)** `[new:Make wf-pause's STATE.md schema canonica]`

Find (current text):
````
**What to update:**
- `updated` timestamp
- Current Position
- Last Action
- Next Steps
````

Replace with:
````
**What to update (edit these fields in place — never replace the file):**
- Current Position (Phase / Task / Status)
- Last Session Summary
- Next Steps
````

- [ ] **Verify:**
  - `grep -c 'Active Decisions' /Users/piemelel/GitHub/Coding-Workflow/.quantis/templates/state.md` → 0 (the unused Active Decisions table is gone)
  - `grep -c 'Last Session Summary\|In-Progress Work\|Context Dump' /Users/piemelel/GitHub/Coding-Workflow/.quantis/templates/state.md` → 3 (canonical wf-pause schema sections present)
  - `grep -c 'Canonical schema' /Users/piemelel/GitHub/Coding-Workflow/.quantis/templates/state.md` → 1 (canonical note added)

- [ ] **Commit:** `git add .quantis/templates/state.md && git commit -m "fix(phase-3.1): state.md"`

---

## Task 7: `.quantis/templates/decisions.md`  (1 edits)

_sources: new_

- [ ] **1. Replace the fenced Template block (lines 7-27)** `[new:Make D-{NNN} format canonical in templat]`

Find (current text):
````
```markdown
## [DECISION-XXX] Title

**Date**: YYYY-MM-DD
**Status**: Proposed | Accepted | Deprecated | Superseded

### Context
What is the issue we're facing?

### Decision
What have we decided to do?

### Rationale
Why did we make this decision?

### Consequences
What are the trade-offs?

### Alternatives Considered
What other options were evaluated?
```
````

Replace with:
````
```markdown
### D-{NNN}: {Decision Title}
**Decision:** {what was decided}
**Rationale:** {why}
```

**ID allocation:** `NNN` is the next integer after the highest existing `D-` ID across `.quantis/DECISIONS.md`, zero-padded to 3 digits (e.g. `D-001`, `D-042`). Optional fields — add only when they carry weight: `**Date:** YYYY-MM-DD`, `**Status:** Proposed | Accepted | Superseded`, `**Alternatives:** {options considered}`.

This is the canonical decision format. All writers (`/discuss-phase`, `/stress-test`, context-health-monitor) use it.
````

- [ ] **Verify:**
  - `grep -c 'DECISION-XXX' /Users/piemelel/GitHub/Coding-Workflow/.quantis/templates/decisions.md` → 0 (old format removed)
  - `grep -c 'D-{NNN}' /Users/piemelel/GitHub/Coding-Workflow/.quantis/templates/decisions.md` → 1 (canonical D-{NNN} format present)

- [ ] **Commit:** `git add .quantis/templates/decisions.md && git commit -m "fix(phase-3.1): decisions.md"`

---

## Task 8: `.quantis/templates/roadmap.md`  (1 edits)

_sources: new_

- [ ] **1. Inside the File Template fenced block — the Progress Summary and Timeline tables (lines 67-85)** `[new:Delete unused roadmap.md template fields]`

Find (current text):
````
## Progress Summary

| Phase | Status | Plans | Complete |
|-------|--------|-------|----------|
| 1 | ⬜ | 0/2 | — |
| 2 | ⬜ | 0/2 | — |
| 3 | ⬜ | 0/1 | — |
| 4 | ⬜ | 0/1 | — |

---

## Timeline

| Phase | Started | Completed | Duration |
|-------|---------|-----------|----------|
| 1 | — | — | — |
| 2 | — | — | — |
| 3 | — | — | — |
| 4 | — | — | — |
```
````

Replace with:
````
```

> Progress Summary and Timeline tables are intentionally omitted — `/progress` derives phase status live from this file; no workflow writes summary/timeline tables.

````

- [ ] **Verify:**
  - `grep -cE 'Progress Summary|Timeline' /Users/piemelel/GitHub/Coding-Workflow/.quantis/templates/roadmap.md` → 0 (both unused tables removed)

- [ ] **Commit:** `git add .quantis/templates/roadmap.md && git commit -m "fix(phase-3.1): roadmap.md"`

---

## Task 9: `.quantis/templates/SUMMARY.md`  (2 edits)

_sources: new_

- [ ] **1. Frontmatter inside the File Template block — the duration_minutes line (line 14)** `[new:Delete unused SUMMARY.md template durati]`

Find (current text):
````
completed_at: [ISO timestamp]
duration_minutes: {N}
status: complete | partial | failed
````

Replace with:
````
completed_at: [ISO timestamp]
status: complete | partial | failed
````

- [ ] **2. The trailing Metadata block inside the File Template (lines 76-84), from the separator through the closing fence** `[new:Delete unused SUMMARY.md template durati]`

Find (current text):
````
---

## Metadata

- **Started:** {timestamp}
- **Completed:** {timestamp}
- **Duration:** {N} minutes
- **Context Usage:** ~{N}%
```
````

Replace with:
````
```
````

- [ ] **Verify:**
  - `grep -cE 'duration_minutes|## Metadata|Context Usage' /Users/piemelel/GitHub/Coding-Workflow/.quantis/templates/SUMMARY.md` → 0 (unused duration/metadata fields removed)

- [ ] **Commit:** `git add .quantis/templates/SUMMARY.md && git commit -m "fix(phase-3.1): SUMMARY.md"`

---

## Task 10: `.quantis/templates/VERIFICATION.md`  (1 edits)

_sources: new_

- [ ] **1. Lines 46-48, inside the template code block — insert '## Code Review' before '## Gap Closure Required'** `[new:Add '## Code Review' section to the VERI]`

Find (current text):
````
## Gap Closure Required

{If verdict is FAIL or PARTIAL, list what needs fixing}
````

Replace with:
````
## Code Review

**Scope:** {N} files changed, {BASE_SHA}..{HEAD_SHA}
**Mode:** {subagent | inline}

### Critical
{findings with file:line, or "None"}

### Important
{findings with file:line, or "None"}

### Minor
{findings with file:line, or "None"}

## Gap Closure Required

{If verdict is FAIL or PARTIAL, list what needs fixing}
````

- [ ] **Verify:**
  - `grep -n '## Code Review' /Users/piemelel/GitHub/Coding-Workflow/.quantis/templates/VERIFICATION.md` → One match: the Code Review section now exists in the template.
  - `grep -nE '^### (Critical|Important|Minor)' /Users/piemelel/GitHub/Coding-Workflow/.quantis/templates/VERIFICATION.md` → Three matches (Critical, Important, Minor) — the severity subsections are present.

- [ ] **Commit:** `git add .quantis/templates/VERIFICATION.md && git commit -m "fix(phase-3.1): VERIFICATION.md"`

---
