# ISSUE ANALYSES

## Issue 1 — rootCause=mixed conf=high
WHY: Primarily a design flaw in the orchestration layer, compounded by instruction-clarity gaps; it is NOT model non-compliance. Design flaw: the thin orchestrator the model actually executes (wf-discuss-phase/SKILL.md) never names the phase SPEC.md as an output — its process steps deliver only DECISIONS.md entries (:51-57) and a STATE.md update (:59-60), and its completion banner (:69 '{X} decisions documented') can be truthfully printed with no spec…
CONF-REASON: The original failure mode is fully explained by absence: the orchestrator the model actually reads (wf-discuss-phase) never names the artifact, contradicts the delegated checklist's tail, and has no gate. The fixes attack that with triple redundancy …
- Q: Does wf-discuss-phase explicitly require SPEC.md creation?
  A: No. The phase spec file .quantis/phases/{N}.{M}-{slug}/SPEC.md is never mentioned anywhere in wf-discuss-phase/SKILL.md. The workflow's own 5-step process names exactly two output artifacts: DECISIONS.md entries (Step 3) and a STATE.md update (Step 4). The completion banner reports only '{X} decisions documented'. The only SPEC.md the file mentions is the PROJECT-level .quantis/SPEC.md, listed as …
- Q: Does the brainstorming skill checklist (step 6) make SPEC.md creation mandatory and unambiguous?
  A: In isolation it is fairly strong; in the context it is actually invoked from, no. The checklist preamble says 'You MUST create a task for each of these items and complete them in order' and step 6 is 'Write design doc — save to .quantis/phases/{N}.{M}-{slug}/SPEC.md and commit'. But four things undermine it when reached via /discuss-phase: (1) checklist steps 8-9 ('Transition to implementation — i…
- Q: Is the spec file path clear and unambiguous?
  A: No, on three counts. (1) Namespace collision: wf-discuss-phase line 17 lists .quantis/SPEC.md (project spec) as required context, while brainstorming says write .quantis/phases/{N}.{M}-{slug}/SPEC.md — a model can conclude 'the SPEC.md already exists' and skip writing. The only spec template (.quantis/templates/spec.md) is the project-level one ('SPEC.md — Project Specification' with Planning Lock…
- Q: Is there a verification step that checks the SPEC.md was actually written?
  A: No, nowhere in the pipeline. wf-discuss-phase Step 4 is just 'Update STATE.md. Suggest /plan {N}' — no test -f, and the completion banner has no spec field to force acknowledgment. brainstorming's 'Spec Self-Review' (lines 116-124) reviews content quality but only runs if the spec was already written; it is not an existence check. Downstream, wf-plan's 'Planning Lock' greps only the PROJECT-level …
FIX 1.1 [.agents/skills/wf-discuss-phase/SKILL.md @ line 42, Step 2 heading area ('## 2. Brainstorm')]
  NOW: ## 2. Brainstorm **Read and follow `.agents/skills/brainstorming/SKILL.md`** in discuss/explore mode.
  CHANGE: ## 2. Brainstorm **Read and follow `.agents/skills/brainstorming/SKILL.md`**, checklist steps 1–8 in full (explore context through user review of the written spec). Step 9 of that checklist (invoke writing-plans) does NOT apply here — this workflow ends by suggesting `/plan {N}` instead (Step 5 below). All other steps apply, including writing the spec file.
  RISK: Hard-codes brainstorming's checklist numbering; if that checklist is ever renumbered, this cross-reference silently points at the wrong steps. Mitigate by also naming the carve-out…
FIX 1.2 [.agents/skills/wf-discuss-phase/SKILL.md @ lines 51-52, Step 3 heading ('## 3. Document Decisions')]
  NOW: ## 3. Document Decisions Record outcomes in `.quantis/DECISIONS.md` using format:
  CHANGE: ## 3. Write Phase SPEC.md (REQUIRED — do not skip) The discussion is NOT complete until the phase spec file exists. This is the primary deliverable of `/discuss-phase`. Note: this is a NEW file in the phase directory — separate from the project-level `.quantis/SPEC.md` you read for reference. Resolve the phase directory (same recipe `/plan` uses, so the directories match): ```bash PHASE_TITLE=$(grep -i "Phase $PHASE" .quantis/ROADMAP.md | head -n…
  RISK: Duplicates the slug bash from wf-plan — the two copies can drift if one is edited. The grep-based PHASE_TITLE extraction fails on unconventional ROADMAP.md formatting, yielding an …
FIX 1.3 [.agents/skills/wf-discuss-phase/SKILL.md @ lines 59-60, Step 4 ('## 4. Update State + Next Steps')]
  NOW: ## 4. Update State + Next Steps Update STATE.md. Suggest `/plan {N}` or `/stress-test`.
  CHANGE: ## 5. Verify Outputs + Update State **Gate — run this before declaring the discussion complete:** ```bash test -f "$PHASE_DIR/SPEC.md" && echo "✓ Phase spec exists: $PHASE_DIR/SPEC.md" || echo "✗ BLOCKER: $PHASE_DIR/SPEC.md missing — go back to Step 3" ``` If the file is missing, you are not done — write it now, then re-run the check. Do not print the completion banner until this check passes. Update STATE.md (record the spec path). Suggest `/pla…
  RISK: A model that skipped Step 3 could also skip the gate; the echo is advisory, not enforced by tooling. Renumbering Step 4→5 requires the matching 'Document Decisions' renumber from t…
FIX 1.4 [.agents/skills/wf-discuss-phase/SKILL.md @ lines 66-69, offer_next banner]
  NOW: Quantis ► DISCUSSION COMPLETE ✓ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ {X} decisions documented
  CHANGE: Quantis ► DISCUSSION COMPLETE ✓ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Spec: {PHASE_DIR}/SPEC.md ✓ {X} decisions documented
  RISK: Minimal. A model could in theory fabricate the path without writing the file, but combined with the Step 5 gate this is unlikely; worst case the banner is cosmetically wrong on leg…
FIX 1.5 [.agents/skills/brainstorming/SKILL.md @ line 32, end of Checklist section (immediately before '## Process Flow')]
  NOW: 9. **Transition to implementation** — invoke writing-plans skill to create implementation plan
  CHANGE: 9. **Transition to implementation** — invoke writing-plans skill to create implementation plan **If invoked by a workflow command (e.g. `/discuss-phase`):** the calling workflow may replace step 9 (it decides what happens after the spec), but steps 1–8 still apply in full. Step 6 is the deliverable — a design discussion that ends without a written, committed SPEC.md is incomplete, regardless of any 'mode' the caller names. Exception: critique and…
  RISK: The exception clause is the dangerous part — a model could classify a discuss session as 'research-ish' to skip steps 6–9. Mitigated by enumerating the exact commands. Also adds ca…
FIX 1.6 [.agents/skills/brainstorming/SKILL.md @ lines 111-112, 'After the Design > Documentation']
  NOW: - Write the validated design (spec) to `.quantis/phases/{N}.{M}-{slug}/SPEC.md` - (User preferences for spec location override this default)
  CHANGE: - Write the validated design (spec) to `.quantis/phases/{N}.{M}-{slug}/SPEC.md` - `{N}.{M}` is the phase number (e.g. `3.1`); `{slug}` is the phase title from ROADMAP.md, lower-cased, with non-alphanumeric runs replaced by hyphens (e.g. `3.1-workflow-reliability-fixes`). If a directory for this phase already exists under `.quantis/phases/`, write into it — never create a second directory with a different slug. - This file is distinct from the pro…
  RISK: Low. The worked example bakes in the current naming convention; if the convention changes (e.g. phases without sub-numbers), the example needs updating. Slightly longer skill file.
FIX 1.7 [.agents/skills/using-quantis/SKILL.md @ line 81, 'Skill Types' section]
  NOW: **Flexible** (patterns, brainstorming): Adapt principles to context.
  CHANGE: **Flexible** (patterns, brainstorming): Adapt the dialogue and questioning style to context — but required output artifacts (e.g. brainstorming's written SPEC.md) are rigid. "Flexible" never means skipping a deliverable file.
  RISK: Very low. Could marginally over-rigidify brainstorming in edge cases where no design is reached (user aborts) — but the deliverable requirement only applies once a design is approv…
FIX 1.8 [.agents/skills/wf-plan/SKILL.md @ line 63, Step 2 bash block (after PHASE_DIR assignment)]
  NOW: PHASE_DIR=".quantis/phases/${PHASE}-${PHASE_SLUG}"
  CHANGE: PHASE_DIR=".quantis/phases/${PHASE}-${PHASE_SLUG}" test -f "$PHASE_DIR/SPEC.md" || echo "⚠️ No phase spec at $PHASE_DIR/SPEC.md — run /discuss-phase $PHASE first, or confirm with the user that planning from ROADMAP.md alone is intended"
  RISK: It is a warning, not a hard block — deliberately, because /plan legitimately supports planning a phase straight from ROADMAP.md without a prior /discuss-phase. A model might ignore…

## Issue 2 — rootCause=mixed conf=medium
WHY: Primarily a design flaw, compounded by an instruction-clarity gap; not plain model non-compliance, because the system gives models genuinely contradictory signals plus a sanctioned menu template to express the resulting confusion. (1) Contradictory fallback specifications: wf-execute/SKILL.md:75 says the no-subagent path is \"Read and follow `.agents/skills/executing-plans/SKILL.md`\" while subagent-driven-development/SKILL.md:22-26 says the no-s…
CONF-REASON: The fixes remove every conflicting signal found (choice-framed header, contradictory fallback definitions, the unconditional "natively supports subagents" claim, the /execute→executing-plans table mapping, and the unscoped Decision Gates template) an…
- Q: 1. Is the "do NOT ask the user" instruction strong enough? Check exact wording.
  A: The prohibition wording itself is strong — it appears twice in the selection block: the heading "Select execution methodology (automatic — do NOT ask the user)" and the closing line "This is NOT a choice. Do not present a menu. Do not ask the user." The weakness is not the prohibition but the detection step it guards: "Check if `invoke_subagent` tool is available in your current environment" gives…
- Q: 2. Does the SDD skill Platform Detection section reinforce the auto-detect behavior?
  A: Yes on the no-asking rule, but it actively CONTRADICTS wf-execute on what the fallback is. SDD line 28 reinforces: "Do not ask the user which mode to use. Check tool availability and proceed automatically." However SDD lines 22-26 say the no-subagent fallback is "inline SDD mode" (execute yourself with self-review gates), while wf-execute line 75 says the no-subagent fallback is to "Read and follo…
- Q: 3. Is there conflicting language elsewhere that could confuse the model?
  A: Yes, five places. (a) wf-execute's own header frames it as a choice before the no-menu rule is ever seen: "powered by `subagent-driven-development` (CLI-first default) or `executing-plans` (inline fallback)" (line 9). (b) The fallback skill executing-plans tells IDE models that subagents should exist: "When subagents are available (Antigravity 2.0 supports them natively), use subagent-driven-devel…
- Q: 4. Could the issue be that the model is not actually reading the SDD skill (i.e., the delegation instruction is too weak…
  A: Partially, but in a specific structural way: the delegation wording ("Read and follow `.agents/skills/subagent-driven-development/SKILL.md`", wf-execute:74) is adequately direct — the problem is that delegation is CONDITIONAL on the very detection the model struggles with. The menu failure happens at wf-execute Step 4 point 2, BEFORE any skill is read: at that moment the model has only wf-execute'…
FIX 2.1 [.agents/skills/wf-execute/SKILL.md @ line 9 (header blockquote)]
  NOW: > **Skill-powered workflow.** Execution methodology is powered by `subagent-driven-development` (CLI-first default) or `executing-plans` (inline fallback). This workflow adds Quantis orchestration (wa…
  CHANGE: > **Skill-powered workflow.** Execution methodology is `subagent-driven-development`; when the `invoke_subagent` tool is absent from your session, the fallback is `executing-plans`. Which one applies is determined by your tool list in Step 4 — never by asking the user. This workflow adds Quantis orchestration (wave management, verification, state tracking).
  RISK: Low. Pure rewording; no behavioral change. Minor risk that "determined by your tool list in Step 4" reads oddly if Step 4 is later renumbered.
FIX 2.2 [.agents/skills/wf-execute/SKILL.md @ lines 72-76 (Step 4, point 2)]
  NOW: 2. **Select execution methodology (automatic — do NOT ask the user):** - Check if `invoke_subagent` tool is available in your current environment - **If available:** Read and follow `.agents/skills/su…
  CHANGE: 2. **Select execution methodology (automatic — do NOT ask the user):** - The check: does a tool named `invoke_subagent` appear in the tool list you were given for this session? Inspect your own tool list. Do not run commands to find out, and do not ask the user. - **If `invoke_subagent` is in your tool list:** Read and follow `.agents/skills/subagent-driven-development/SKILL.md`. Announce: "I'm using Subagent-Driven Development to execute this pl…
  RISK: Low-medium. A model on a platform that genuinely cannot enumerate its tools will now silently choose inline fallback even when invoke_subagent exists, losing SDD quality gates on s…
FIX 2.3 [.agents/skills/subagent-driven-development/SKILL.md @ lines 16-28 (Platform Detection section)]
  NOW: ## Platform Detection Before starting SDD, check your environment: 1. **Check if `invoke_subagent` tool is available** in your current toolset 2. **If available** (CLI `agy`, Standalone): Proceed with…
  CHANGE: ## Platform Detection Before starting SDD, check your environment: 1. **The check:** does a tool named `invoke_subagent` appear in the tool list you were given for this session? Inspect your own tool list. If you are not sure, treat it as NOT available — uncertainty is never a reason to ask the user. 2. **If available** (CLI `agy`, Standalone): Proceed with **full SDD** — dispatch real subagents via `invoke_subagent` for implementation, spec revi…
  RISK: Medium. The scoping note adds a sentence of routing logic to a methodology skill, slightly coupling it to wf-execute; if wf-execute's Step 4 is later changed to delegate unconditio…
FIX 2.4 [.agents/skills/using-quantis/SKILL.md @ line 109 (Workflow Commands table)]
  NOW: | `/execute {N}` | Execute phase plans | executing-plans |
  CHANGE: | `/execute {N}` | Execute phase plans | subagent-driven-development (auto-detected; inline fallback: executing-plans) |
  RISK: Low. Table cell grows slightly; no behavioral logic changes. Must keep the wording in sync if the methodology pairing ever changes.
FIX 2.5 [.agents/skills/using-quantis/references/antigravity-tools.md @ line 21 (Subagent Support section, first paragraph)]
  NOW: Antigravity 2.0 supports subagents natively via `invoke_subagent`. Use `define_subagent` to register named subagent types at session start.
  CHANGE: Antigravity supports subagents via `invoke_subagent` on CLI (`agy`) and Standalone only — the IDE does not have this tool. If `invoke_subagent` is not in your tool list, that is normal, not an error: skills fall back to inline execution automatically, and you must never ask the user whether subagents are available. When `invoke_subagent` is available, use `define_subagent` to register named subagent types at session start.
  RISK: Low. Pure documentation correction matching the already-shipped adapter text. Only risk is divergence if a future platform gains invoke_subagent and only one of the two files is up…
FIX 2.6 [.agents/skills/executing-plans/SKILL.md @ line 14 (Overview note)]
  NOW: **Note:** When subagents are available (Antigravity 2.0 supports them natively), use subagent-driven-development instead of this skill for better quality via two-stage review.
  CHANGE: **Note:** If the `invoke_subagent` tool is in your session's tool list, use subagent-driven-development instead of this skill for better quality via two-stage review. If it is not (e.g., on IDE), this skill is the correct choice — proceed without asking the user about execution modes.
  RISK: Low. Wording only. Slight redundancy with wf-execute's Step 4, but redundancy is desirable here since this skill can also be invoked directly outside /execute.
FIX 2.7 [.agents/rules/QUANTIS-STYLE.md @ lines 251-253 (Decision Gates section, trigger sentence)]
  NOW: ### Decision Gates When user input needed:
  CHANGE: ### Decision Gates Only for genuine product or design decisions the user must own (architecture trade-offs, scope cuts, irreversible choices). Never use a decision gate for anything a skill resolves automatically — execution mode, subagent vs inline, tool availability, platform detection. When user input is genuinely needed:
  RISK: Low-medium. If the exclusion list is read too broadly a model might under-ask on a genuinely ambiguous judgment call; mitigated by the explicit "product or design decisions" positi…

## Issue 3 — rootCause=mixed conf=high
WHY: Mixed: design-flaw + instruction-clarity; explicitly NOT model-compliance. Design-flaw: wf-verify violates the system's own thin-orchestrator architecture — instead of delegating, its <process> steps 2-4 (.agents/skills/wf-verify/SKILL.md:74-180) and <evidence_requirements> (:315-338) re-implement a thinned-down copy of the methodology inline (the Forbidden Phrases and Required Evidence tables at :317-337 mirror verification-before-completion/SKI…
CONF-REASON: The proposed fix replicates, verbatim in style, the delegation pattern already used by the three sibling workflows (bold 'Read and follow <path> exactly' as the first action of a numbered <process> step) — a pattern the repo treats as its working sta…
- Q: 1. Does wf-verify contain an explicit "Read and follow" instruction for the verification skill?
  A: Only in a weakened, misplaced form. Line 38 contains the words 'read and follow' but as a lowercase, unbolded blockquote aside inside the <context> metadata block, referencing the skill by name rather than file path (the path is on separate line 36). Critically, there is NO 'Read and follow' instruction anywhere inside the <process> block (lines 54-313), which is what a model actually executes ste…
- Q: 2. Is the skill reference only in the <context> block (metadata, not a process step)?
  A: Yes, and worse: the <context> block is malformed. <context> opens at line 28 and is NEVER closed — grep confirms no </context> tag exists anywhere in the file (tags found: role 9/20, objective 22/26, context 28/none, process 54/313, evidence_requirements 315/338, related 340/355). So the skill reference (line 36), the read-and-follow blockquote (line 38), AND the entire '## 0. Platform Check' sect…
- Q: 3. Compare the delegation pattern across discuss-phase, plan, execute, verify. Are they consistent?
  A: No — wf-verify is the odd one out on every axis. (a) Banner: wf-discuss-phase:9, wf-plan:9, and wf-execute:9 all open with '> **Skill-powered workflow.** ... powered by `X`'; wf-verify:7 is just '# /verify Workflow' with no banner. (b) Process-step imperative: discuss-phase Step 2 (line 42) '**Read and follow `.agents/skills/brainstorming/SKILL.md`** in discuss/explore mode.'; plan Step 4 (line 15…
FIX 3.1 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md @ Line 7 (title, immediately after frontmatter)]
  NOW: # /verify Workflow
  CHANGE: # /verify → verification-before-completion skill > **Skill-powered workflow.** Verification methodology is powered by `verification-before-completion`. This workflow adds Quantis phase context, evidence collection, and gap-closure planning.
  RISK: Very low. Cosmetic/priming change. Slight redundancy with the <role> block at lines 9-20; no script parses the H1 title (validate-workflows.sh checks only frontmatter, description,…
FIX 3.2 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md @ Lines 36-40 (end of <context> block through '## 0. Platform Check' heading)]
  NOW: **Skill reference:** `.agents/skills/verification-before-completion/SKILL.md` > Before starting verification, read and follow the `verification-before-completion` skill for the full methodology. ## 0.…
  CHANGE: **Skill reference:** `.agents/skills/verification-before-completion/SKILL.md` — loaded in Step 1 </context> <process> ## 0. Platform Check
  RISK: Medium if applied alone: deleting the blockquote without applying Fix 3 would remove the file's ONLY read-and-follow language, making the issue worse. Also temporarily creates a du…
FIX 3.3 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md @ Lines 52-58 (end of Step 0 through start of Step 1, including the old <process> …]
  NOW: **If `invoke_subagent` is NOT available**, run verification inline (proceed to Step 1). <process> ## 1. Load Verification Context ```bash
  CHANGE: **If `invoke_subagent` is NOT available**, run verification inline (proceed to Step 1). ## 1. Load Verification Context **Read and follow `.agents/skills/verification-before-completion/SKILL.md` exactly.** Its Gate Function (IDENTIFY → RUN → READ → VERIFY → only then claim) governs every must-have check in Steps 2–5. Do not record any PASS/FAIL verdict without fresh command output produced in this session. Where the skill and this workflow overla…
  RISK: Low. Adds ~80 tokens of context per /verify run (the skill file is ~140 lines — acceptable). Residual risk: a model could read the skill, then still mechanically follow only wf-ver…
FIX 3.4 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md @ Lines 42-45 (Step 0 'Platform Check', subagent dispatch bullets)]
  NOW: **If `invoke_subagent` is available**, dispatch a `self` subagent with: - The SPEC.md must-haves list - The verification-before-completion skill instructions - The phase directory and codebase access
  CHANGE: **If `invoke_subagent` is available**: first read `.agents/skills/verification-before-completion/SKILL.md`, then dispatch a `self` subagent with: - The SPEC.md must-haves list - The full text of `.agents/skills/verification-before-completion/SKILL.md`, pasted into the subagent prompt (do not assume the subagent can locate or load skills itself) - The phase directory and codebase access
  RISK: Low. Subagent prompts grow by ~140 lines of skill text — negligible against typical context budgets and consistent with the system's fresh-context-per-subagent philosophy. Minor du…

## Issue 4 — rootCause=mixed conf=medium
WHY: Both design flaw and compliance decay, with design dominant. Design: the only authoritative 'mark phase ✅ Complete' instruction in the entire system sits at the tail of the longest workflow (.agents/skills/wf-execute/SKILL.md:96-105), reachable only via the PASS branch of its internal verification (:92-94); the dedicated Auditor workflow that the rest of the system treats as the real completion gate (wf-progress/SKILL.md:85 'Phase done, not verif…
CONF-REASON: The /verify and /pause fixes are mechanical, located in short workflows where instructions are read close to execution, and the staging self-check in /verify Step 6 gives the model a concrete failure signal — those should comply reliably. The residua…
- Q: 1. Does /execute Step 6 clearly mandate ROADMAP.md update?
  A: Yes, when read in isolation the mandate is clear, but it is structurally fragile in three ways. (a) It is the final step of the longest workflow — by the time the model has executed all waves (Step 4) and verified the phase goal (Step 5), context is at its most degraded, and nothing re-anchors the requirement: the 'PHASE {N} COMPLETE' banner in <offer_next> can be printed without any gate confirmi…
- Q: 2. Does /pause check if there are completed-but-unsynced phases?
  A: No. wf-pause never reads, mentions, or stages ROADMAP.md anywhere. Step 1 writes only STATE.md, Step 2 only JOURNAL.md, and Step 3 commits exactly those two files ('git add .quantis/STATE.md .quantis/JOURNAL.md'). The proactive auto-save section (lines 144-177) also touches only STATE.md. So when /pause is invoked right after a phase finishes (or mid-/execute via the 3-strike rule), it can write '…
- Q: 3. Does /verify update ROADMAP.md on PASS?
  A: No. The PASS branch of Step 5 updates only STATE.md ('Phase: {N} (verified) / Status: ✅ Complete and verified', lines 228-233). ROADMAP.md is read as input (lines 33, 68) but never written. Worse, Step 6 'Commit Verification' stages ONLY VERIFICATION.md (line 309), so even the STATE.md edit /verify just made in Step 5 is left uncommitted — consistent with the currently-modified-uncommitted .quanti…
- Q: 4. Is there a redundancy issue (multiple workflows could update ROADMAP, so none does)?
  A: Yes — a diffusion-of-responsibility pattern, in the worst configuration: redundancy where it does not matter and a gap where it does. Deliverable check-offs are mentioned in three methodology layers (executing-plans/SKILL.md:91-94 'Check off deliverables as they're completed', subagent-driven-development/SKILL.md:314 'Check off deliverable if the completed task satisfies one', using-quantis/SKILL.…
FIX 4.1 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md @ Lines 226-228, Step '## 5. Handle Results', start of the PASS branch]
  NOW: ### If PASS (all must-haves verified): Update `.quantis/STATE.md`:
  CHANGE: ### If PASS (all must-haves verified): **Update `.quantis/ROADMAP.md` — REQUIRED, do not skip:** 1. Find this phase's entry and set its `**Status**:` line to `✅ Complete ({YYYY-MM-DD})` 2. Check off the phase's plan/deliverable checkboxes (`- [x]`) 3. Update the Progress Summary and Timeline tables, if the file has them A phase is not verified-complete until ROADMAP.md says so. If `/execute` already marked it `✅ Complete`, leave it and continue. …
  RISK: If a model runs /verify on an old, already-archived phase it could touch historical milestone sections; the idempotency clause mitigates this. Real ROADMAP files vary in structure …
FIX 4.2 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md @ Lines 306-311, Step '## 6. Commit Verification']
  NOW: ## 6. Commit Verification ```bash git add "$PHASE_DIR/VERIFICATION.md" git commit -m "docs(phase-$PHASE): verification report" ```
  CHANGE: ## 6. Commit Verification Commit the report together with the state updates made in Step 5: ```bash git add "$PHASE_DIR/VERIFICATION.md" .quantis/ROADMAP.md .quantis/STATE.md git commit -m "docs(phase-$PHASE): verification report + state sync" ``` Self-check: if the verdict was PASS, `git show --stat HEAD` must list `.quantis/ROADMAP.md`. If it does not, the Step 5 ROADMAP update was skipped — go back and do it, then `git commit --amend --no-edit…
  RISK: If verdict was FAIL, ROADMAP.md legitimately has no changes and `git add` of an unchanged file is a harmless no-op — but a model might misread the self-check as applying to FAIL to…
FIX 4.3 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-pause/SKILL.md @ Lines 96-101, Step '## 3. Commit State']
  NOW: ## 3. Commit State ```bash git add .quantis/STATE.md .quantis/JOURNAL.md git commit -m "docs: pause session - {brief reason}" ```
  CHANGE: ## 3. Reconcile ROADMAP.md Catch completed-but-unsynced phases before handing off: ```bash grep -nE "Status.*(In Progress|🔄|Not Started|⬜)" .quantis/ROADMAP.md ``` For each phase listed that has a directory under `.quantis/phases/`, check the evidence on disk: - `VERIFICATION.md` exists with `verdict: PASS` → set that phase's ROADMAP.md status to `✅ Complete` and check off its plans - Every `*-PLAN.md` has a matching `*-SUMMARY.md` but there is n…
  RISK: Adds work at the moment the user wants to stop quickly — kept cheap (one grep plus file existence checks). A model could over-eagerly mark phases Complete from SUMMARY files alone …
FIX 4.4 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-pause/SKILL.md @ Line 105, heading '## 4. Display Handoff']
  NOW: ## 4. Display Handoff
  CHANGE: ## 5. Display Handoff
  RISK: None beyond merge friction; must be applied atomically with the previous fix.
FIX 4.5 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md @ Lines 96-105, Step '## 6. Update State']
  NOW: ## 6. Update State **ROADMAP.md:** Mark phase `✅ Complete` **STATE.md:** Update position, last session summary, next steps **REQUIREMENTS.md** (if exists): Cross-reference completed tasks with require…
  CHANGE: ## 6. Update State — REQUIRED GATE The phase is not complete until this commit exists. Never print the `PHASE {N} COMPLETE` banner before it. **ROADMAP.md:** Set this phase's `**Status**:` line to `✅ Complete ({YYYY-MM-DD})`, check off its plan checkboxes, and update the Progress Summary / Timeline tables if present **STATE.md:** Update position, last session summary, next steps **REQUIREMENTS.md** (if exists): Cross-reference completed tasks wit…
  RISK: The grep self-check can false-negative on heading-format variations (e.g. 'Phase 3:' vs 'Phase 3.2:') and send the model back to re-edit a correct file — the pattern was chosen to …
FIX 4.6 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md @ Lines 67-69, Step '## 4. Execute Waves', opening lines]
  NOW: ## 4. Execute Waves For each wave in order, for each plan in the wave:
  CHANGE: ## 4. Execute Waves Before the first wave: set this phase's status in `.quantis/ROADMAP.md` to `🔄 In Progress` (single-line edit; it rides along with the first task commit). For each wave in order, for each plan in the wave:
  RISK: The ROADMAP edit lands inside the first `feat(phase-N)` task commit rather than a docs commit — slightly noisy history. If execution is aborted before any task commit, ROADMAP is l…

## Issue 5 — rootCause=design-flaw conf=medium
WHY: The instructions are followed faithfully — they simply contain no code review step in the phase verification path, so no degree of model compliance can produce one. Evidence: (1) wf-verify/SKILL.md:14-19 scopes the verifier to must-haves only ('Extract testable deliverables... Collect empirical evidence... Generate fix plans'), and grep over wf-verify and wf-execute SKILL.md files returns zero occurrences of 'code review', 'code-quality', or 'req…
CONF-REASON: A model reading the updated wf-verify would very likely execute the new Step 4: it mirrors two patterns the file already uses (platform-branch on invoke_subagent at the top, gap-closure plans on failure at the bottom), reuses a fully-written reviewer…
- Q: 1. Should this be a new step in /verify or a separate workflow?
  A: A new step in /verify (inserted between Step 3 'Verify Each Must-Have' and the current Step 4 'Create Verification Report'), not a separate workflow. Three reasons grounded in the system's own design: (a) /verify already owns the phase quality gate and the remediation loop the review findings need — FAIL produces gap closure plans routed to '/execute {N} --gaps-only' (wf-verify/SKILL.md:254-302). …
- Q: 2. Should it use subagents (dispatch a code reviewer subagent per file batch)?
  A: Yes where invoke_subagent is available, with an inline file-by-file fallback for the IDE. Fresh-context review is the system's stated rationale for review quality: 'The reviewer gets precisely crafted context for evaluation — never your session's history' (requesting-code-review/SKILL.md:8). wf-verify already platform-branches on invoke_subagent (wf-verify/SKILL.md:40-52), so the same pattern fits…
- Q: 3. How should it interact with the existing SDD code-quality-reviewer?
  A: Complementary layers, not replacement — and the /verify review is a critical backstop because SDD's reviews frequently never happen. The SDD code-quality-reviewer is task-scoped: it reviews 'Task N from [plan-file]' over the diff '[commit before task]..[current commit]' (code-quality-reviewer-prompt.md:9-17) and gates each task during /execute. SDD also names a 'final code reviewer subagent for en…
- Q: 4. What is the right scope: only files changed in the phase, or full codebase?
  A: Files changed in the phase, derived from git, with the SUMMARY.md 'Files Changed' tables as fallback — never the full codebase. The diff range is derivable because every phase commit is tagged: 'git commit -m "feat(phase-$PHASE): {task-name}"' (wf-execute/SKILL.md:78) and 'docs(phase-$PHASE)' (wf-execute/SKILL.md:104, wf-verify/SKILL.md:310), so BASE_SHA = parent of the first commit matching grep …
FIX 5.1 [.agents/skills/wf-verify/SKILL.md @ lines 14-19, <role> block]
  NOW: **Core responsibilities:** - Extract testable deliverables from phase - Walk through each requirement - Collect empirical evidence (commands, screenshots) - Create verification report - Generate fix p…
  CHANGE: **Core responsibilities:** - Extract testable deliverables from phase - Walk through each requirement - Collect empirical evidence (commands, screenshots) - Run senior code review on all code changed during the phase - Create verification report - Generate fix plans if issues found
  RISK: Minimal. A model might attempt code review before reading Step 4's mechanics, but the numbered process below constrains the order.
FIX 5.2 [.agents/skills/wf-verify/SKILL.md @ lines 47-50, '## 0. Platform Check' section]
  NOW: The subagent verifies each must-have and produces VERIFICATION.md. The orchestrator then: 1. Reviews the verification results 2. Checks that evidence is empirical (not claims) 3. Presents results to t…
  CHANGE: The subagent verifies each must-have and returns the results. The orchestrator then: 1. Reviews the verification results 2. Checks that evidence is empirical (not claims) 3. Runs the senior code review (Step 4) — never skipped 4. Writes VERIFICATION.md combining must-have results and review findings (Step 5) 5. Presents results to the user with PASS/FAIL summary
  RISK: Changes who writes VERIFICATION.md in subagent mode (orchestrator instead of subagent). If the self subagent still writes a file out of habit, the orchestrator overwrites it — beni…
FIX 5.3 [.agents/skills/wf-verify/SKILL.md @ lines 184-186, between Step 3 and current Step 4]
  NOW: ## 4. Create Verification Report Write `"$PHASE_DIR/VERIFICATION.md"`:
  CHANGE: ## 4. Senior Code Review Must-haves prove the phase does what the spec asked. This step proves the code is well-built: no bugs, dead code, or unhandled edge cases. It is never skipped. Review findings are evidence-backed (file:line references), not impressions — this does not conflict with the forbidden-phrases rule, which bans unverified success claims. ### 4a. Determine Review Scope Review the code changed during this phase — never the full cod…
  RISK: Largest fix, three risks: (1) the git --grep derivation fails silently if commits were made off-convention — the SUMMARY.md fallback mitigates but depends on SUMMARY accuracy; (2) …
FIX 5.4 [.agents/skills/wf-verify/SKILL.md @ line 224 and line 306, section headings]
  NOW: ## 5. Handle Results
  CHANGE: ## 6. Handle Results (and at line 306, change "## 6. Commit Verification" to "## 7. Commit Verification")
  RISK: Purely mechanical, but a missed renumber leaves a dangling cross-reference; both replacements are unique strings so they are safe to apply with exact matching.
FIX 5.5 [.agents/skills/wf-verify/SKILL.md @ lines 215-219, inside the VERIFICATION.md report format code block in 'Create Ve…]
  NOW: ## Verdict {PASS | FAIL | PARTIAL} ## Gap Closure Required {If FAIL, list what needs to be fixed}
  CHANGE: ## Code Review **Scope:** {N} files changed, {BASE_SHA}..{HEAD_SHA} **Mode:** {subagent | inline} ### Critical {findings with file:line, or "None"} ### Important {findings with file:line, or "None"} ### Minor {findings with file:line, or "None"} ## Verdict {PASS | FAIL | PARTIAL} PASS requires every must-have verified AND no unresolved Critical/Important review findings. ## Gap Closure Required {If FAIL, list what needs to be fixed}
  RISK: Reports get longer; for phases with zero findings this adds boilerplate ('None' three times). Acceptable cost for auditability.
FIX 5.6 [.agents/skills/wf-verify/SKILL.md @ line 226, Handle Results PASS branch]
  NOW: ### If PASS (all must-haves verified):
  CHANGE: ### If PASS (all must-haves verified, no unresolved Critical/Important review findings):
  RISK: None meaningful; consistent with the verdict rule added to the report format.
FIX 5.7 [.agents/skills/wf-verify/SKILL.md @ lines 351-354, <related> Skills table]
  NOW: | Skill | Purpose | |-------|---------| | `verification-before-completion` | Detailed verification methodology | | `context-health-monitor` | Context budget monitoring |
  CHANGE: | Skill | Purpose | |-------|---------| | `verification-before-completion` | Detailed verification methodology | | `requesting-code-review` | Senior code review template (Step 4) | | `context-health-monitor` | Context budget monitoring |
  RISK: None.
FIX 5.8 [.quantis/templates/VERIFICATION.md @ lines 46-48]
  NOW: ## Gap Closure Required {If verdict is FAIL or PARTIAL, list what needs fixing}
  CHANGE: ## Code Review **Scope:** {N} files changed, {BASE_SHA}..{HEAD_SHA} **Mode:** {subagent | inline} ### Critical {findings with file:line, or "None"} ### Important {findings with file:line, or "None"} ### Minor {findings with file:line, or "None"} ## Gap Closure Required {If verdict is FAIL or PARTIAL, list what needs fixing}
  RISK: Template and wf-verify inline format must now be kept in sync manually — an existing maintenance burden this change extends, not creates (the must-haves sections are already duplic…

## Issue 6 — rootCause=design-flaw conf=medium
WHY: The inconsistent plan quality is overwhelmingly a design gap, not models ignoring clear instructions. Four structural absences: (1) Stress-test is an orphan command — nothing invokes it programmatically. wf-plan/SKILL.md:50-53's only gate is `grep -q "FINALIZED"`, while every reference to stress-test is an optional suggestion (wf-new-project/SKILL.md:259 "optional but recommended", wf-discuss-phase/SKILL.md:72 "(optional)"). The spec template eve…
CONF-REASON: The integration fixes mirror patterns the system already uses successfully (wf-plan Step 4 and wf-stress-test Step 0 already do invoke_subagent platform checks; wf-execute Step 4 already does forced auto-selection with 'This is NOT a choice'), so a m…
- Q: 1. Should /plan auto-invoke stress-test on the SPEC before planning?
  A: Yes, as a gated check with an escape hatch — not an unconditional re-run. Today stress-test is purely optional and lives outside /plan: /new-project suggests it as "optional but recommended" and /discuss-phase lists it as "(optional)". /plan's only gate is a grep for the literal string FINALIZED. This is incoherent with the spec template, which says the Unresolved Questions section is "Auto-genera…
- Q: 2. Should /plan auto-invoke stress-test on the PLAN after creation?
  A: Yes — this is the bigger gap, but wf-stress-test must first gain a plan mode or the auto-invoke will misfire. /plan currently goes self-checklist (Step 5) -> commit (Step 6) -> "Next: /execute {N}" with no adversarial pass on the plan. wf-stress-test is nominally a "spec/plan review" (argument-hint "[target-file]", Step 0 mentions "SPEC.md and/or PLAN.md content", using-quantis calls it "Adversari…
- Q: 3. Should stress-test use subagents for parallel dimension review?
  A: Qualified yes — grouped parallel subagents (3 agents covering 2-3 dimensions each), not 7. Today Step 0 dispatches ONE self subagent with "Instructions to attack across all 7 dimensions": a single context attacks all dimensions serially, so later dimensions (Security, Performance, Maintainability) get rushed once earlier findings fill the context, and a single perspective anchors after finding a f…
- Q: 4. Is the plan quality checker (Step 5 in /plan) sufficient, or does it need to be more adversarial?
  A: Insufficient on four counts. (1) Same-author review: Step 5 is run by the orchestrator that wrote the plan (inline path) or already reviewed it in Step 4 ("Reviews the plan output ... Checks for quality"); writing-plans' own Self-Review explicitly forbids fresh-context review ("a checklist you run yourself — not a subagent dispatch"). So the plan gets up to three self-checks by the same mind and z…
FIX 6.1 [.agents/skills/wf-plan/SKILL.md @ line 4 (frontmatter)]
  NOW: argument-hint: "[phase] [--research] [--skip-research] [--gaps]"
  CHANGE: argument-hint: "[phase] [--research] [--skip-research] [--gaps] [--skip-stress-test]"
  RISK: Minimal. Users could habitually pass --skip-stress-test and defeat the gate, but that is an explicit informed choice.
FIX 6.2 [.agents/skills/wf-plan/SKILL.md @ lines 18-21 (<context> Flags list)]
  NOW: **Flags:** - `--research` — Force re-research even if RESEARCH.md exists - `--skip-research` — Skip research entirely - `--gaps` — Gap closure mode (reads VERIFICATION.md, skips research)
  CHANGE: **Flags:** - `--research` — Force re-research even if RESEARCH.md exists - `--skip-research` — Skip research entirely - `--gaps` — Gap closure mode (reads VERIFICATION.md, skips research and the spec stress-test gate) - `--skip-stress-test` — Skip the Step 1.5 spec stress-test gate (spec already reviewed elsewhere)
  RISK: Minimal; documentation-only change paired with the Step 1.5 fix.
FIX 6.3 [.agents/skills/wf-plan/SKILL.md @ lines 50-54 (Step 1 'Planning Lock') — append new Step 1.5 after it]
  NOW: ## 1. Planning Lock ```bash grep -q "FINALIZED" ".quantis/SPEC.md" || echo "Error: SPEC.md must be FINALIZED" test -f ".agents/rules/CONSTITUTION.md" || echo "⚠️ No .agents/rules/CONSTITUTION.md" ```
  CHANGE: ## 1. Planning Lock ```bash grep -q "FINALIZED" ".quantis/SPEC.md" || echo "Error: SPEC.md must be FINALIZED" test -f ".agents/rules/CONSTITUTION.md" || echo "⚠️ No .agents/rules/CONSTITUTION.md" ``` ## 1.5 Spec Stress-Test Gate **Skip this step if** `--gaps` or `--skip-stress-test` was passed. ```bash grep -q "Stress-tested:" ".quantis/SPEC.md" && echo "Spec already stress-tested" || echo "Spec NOT stress-tested" ``` **If the spec has NOT been s…
  RISK: Adds latency and tokens to the first /plan of every project; small/simple projects pay a 7-dimension review they may not need (mitigated by --skip-stress-test). Marker detection is…
FIX 6.4 [.agents/skills/wf-plan/SKILL.md @ lines 166-174 (Step 5 'Plan Checker')]
  NOW: ## 5. Plan Checker (max 3 iterations) For each plan, verify: - [ ] All referenced files exist or will be created - [ ] Actions are specific (no vague "implement X") - [ ] `<verify>` commands are execu…
  CHANGE: ## 5. Adversarial Plan Review (max 3 iterations) The plan's author must not be its only reviewer. **If `invoke_subagent` is available:** dispatch a fresh `self` subagent that has NOT seen this planning conversation. Give it only: the PLAN.md file(s), `.quantis/SPEC.md`, and `.agents/skills/wf-stress-test/SKILL.md` with the instruction "run this skill in plan mode against these plans and return the findings report." **If `invoke_subagent` is NOT a…
  RISK: Subagent dispatch per planning run costs tokens; a reviewer without conversation context may flag intentional, user-approved decisions as findings, causing iteration churn (capped …
FIX 6.5 [.agents/skills/wf-stress-test/SKILL.md @ lines 28-38 (Step 0 'Platform Check', subagent branch)]
  NOW: **If `invoke_subagent` is available**, dispatch a `self` subagent with: - The SPEC.md and/or PLAN.md content - The brainstorming skill in adversarial critique mode - Instructions to attack across all …
  CHANGE: **If `invoke_subagent` is available**, dispatch THREE `self` subagents in parallel (pattern: `.agents/skills/dispatching-parallel-agents/SKILL.md`). Each gets the target file content, the brainstorming skill in adversarial critique mode, the review mode from Step 1.5 (spec or plan), and its dimension subset: - Subagent A: Dimensions 1-2 (Completeness, Consistency) - Subagent B: Dimensions 3-4 (Feasibility, Edge Cases) - Subagent C: Dimensions 5-7…
  RISK: Roughly 3x token cost versus the current single subagent; overlapping findings across groups require orchestrator dedup judgment; on platforms where invoke_subagent serializes, wal…
FIX 6.6 [.agents/skills/wf-stress-test/SKILL.md @ lines 40-41 (Step 1 'Load Context') — append new Step 1.5]
  NOW: ## 1. Load Context Read target file + SPEC.md + ROADMAP.md + DECISIONS.md.
  CHANGE: ## 1. Load Context Read target file + SPEC.md + ROADMAP.md + DECISIONS.md. ## 1.5 Determine Review Mode - Target filename matches `*PLAN.md` → **plan mode** - Anything else (including the default `.quantis/SPEC.md`) → **spec mode** **In spec mode**, apply the 7 dimensions in Step 3 as written. **In plan mode**, reinterpret each dimension for plans: 1. **Completeness** — Does every spec requirement map to a task? Does every task have test and veri…
  RISK: Mode branching is the most error-prone part of this fix set: a model could review a plan in spec mode if the target path is ambiguous. Mitigated by the mechanical filename test, bu…
FIX 6.7 [.agents/skills/wf-stress-test/SKILL.md @ lines 100-102 (Step 5 'Update Spec (if applicable)')]
  NOW: ## 5. Update Spec (if applicable) Add unresolved questions to SPEC.md's `## Unresolved Questions` section. Document decisions in DECISIONS.md.
  CHANGE: ## 5. Record Findings (mode-aware) **Spec mode:** Add unresolved questions to SPEC.md's `## Unresolved Questions` section, document decisions in DECISIONS.md, then append `Stress-tested: {YYYY-MM-DD}` on its own line under the spec's status header. **Plan mode:** Write the findings report to the phase directory as `STRESS-TEST.md` next to the plan. Do NOT modify SPEC.md. If invoked from /plan Step 5, return the findings to that step; otherwise fi…
  RISK: The marker is written once and never invalidated — a spec substantially edited after stress-testing still carries the marker and skips the gate. STRESS-TEST.md adds another per-pha…
FIX 6.8 [.agents/skills/wf-stress-test/SKILL.md @ lines 109-119 (<offer_next> block)]
  NOW: ▶ Fix critical issues, then /plan {N}
  CHANGE: ▶ Spec mode: fix critical issues, then /plan {N} ▶ Plan mode: fix critical issues in PLAN.md, then /execute {N}
  RISK: Minimal. Slightly busier output; model must pick the right line based on mode (consistent with the Step 1.5 mode it already determined).
FIX 6.9 [.agents/skills/wf-plan/SKILL.md @ lines 205-211 (<related> Skills table)]
  NOW: | Skill | Purpose | |-------|---------| | `writing-plans` | Planning methodology (delegated) | | `brainstorming` | Use for research phase |
  CHANGE: | Skill | Purpose | |-------|---------| | `writing-plans` | Planning methodology (delegated) | | `brainstorming` | Use for research phase | | `wf-stress-test` | Spec gate (Step 1.5) and adversarial plan review (Step 5) | | `receiving-code-review` | Triage external model feedback (Step 5.5) |
  RISK: None beyond table length.

## Issue 7 — rootCause=mixed conf=medium
WHY: Primarily design-flaw, compounded by instruction-clarity. Design flaws: (1) Subagent dispatch is a bolt-on conditional rather than an integral path — every dispatch site reads 'If invoke_subagent is available, dispatch...' and then falls through into numbered steps written exclusively for inline execution, with no routing for the dispatched case (wf-verify/SKILL.md:47 says the subagent 'produces VERIFICATION.md' while :186 tells the orchestrator …
CONF-REASON: The fixes convert weak conditionals into MUST directives, enumerate exactly what to paste into each subagent prompt (matching the system's own dispatching-parallel-agents quality bar), and add explicit post-dispatch routing ('skip Steps X-Y, resume a…
- Q: 1. Which workflows currently have subagent dispatch (Step 0)?
  A: Exactly four workflows have a conditional dispatch block, plus one indirect: (a) wf-discuss-phase Step 0 dispatches a single `research` subagent for context gathering; (b) wf-stress-test Step 0 dispatches a single `self` subagent to run all 7 dimensions; (c) wf-verify 'Step 0' dispatches a single `self` subagent to verify must-haves (note: this Step 0 sits INSIDE an unclosed `<context>` tag, befor…
- Q: 2. Are the dispatch instructions specific enough (what context to give the subagent)?
  A: No, except partially in wf-plan. Measured against the system's own quality bar in dispatching-parallel-agents (prompts must be 'Focused, Self-contained, Specific about output', lines 84-89), the Step 0 blocks fail: (a) wf-stress-test:31 says give the subagent 'Instructions to attack across all 7 dimensions below' — 'below' is in the ORCHESTRATOR's file; nothing tells the model to paste the dimensi…
- Q: 3. Could /discuss-phase dispatch a research subagent AND a brainstorming subagent?
  A: Partially. The research subagent already exists (Step 0, lines 24-30) and is correct. A FULL brainstorming subagent is architecturally impossible: brainstorming is an interactive dialogue with the user ('ask questions one at a time', 'One question at a time - Don't overwhelm', user approval gates), and subagents cannot talk to the user — so the dialogue must stay in the orchestrator. However, the …
- Q: 4. Could /stress-test dispatch 7 parallel subagents (one per dimension)?
  A: Yes, and it is the single clearest missed opportunity in the system. The 7 dimensions (Completeness, Consistency, Feasibility, Edge Cases, Security, Performance, Maintainability — lines 52-85) are independent, read-only analyses of the same static documents: no shared state, no file writes, no ordering dependency. This exactly matches dispatching-parallel-agents' 'Use when' criteria ('Each problem…
- Q: 5. Is the dispatching-parallel-agents skill being used anywhere?
  A: No. It is orphaned. Repo-wide grep finds it referenced only in MANIFEST.md:47 (inventory list), README.md:46 (catalog table row), and stale historical plans under .quantis/milestones/. Not one of the 30 wf-* workflow skills references it, and no methodology skill references it either. The only skill that discusses parallel dispatch at all is subagent-driven-development, which correctly FORBIDS it …
FIX 7.1 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-stress-test/SKILL.md @ Lines 26-38, section '## 0. Platform Check']
  NOW: ## 0. Platform Check **If `invoke_subagent` is available**, dispatch a `self` subagent with: - The SPEC.md and/or PLAN.md content - The brainstorming skill in adversarial critique mode - Instructions …
  CHANGE: ## 0. Platform Check **If `invoke_subagent` is available, you MUST dispatch subagents — do not run the critique inline.** Read `.agents/skills/dispatching-parallel-agents/SKILL.md`, then dispatch **7 parallel `self` subagents — one per dimension from Step 3**. The dimensions are independent, read-only analyses: invoke all 7 together, never sequentially. Each subagent prompt MUST contain, pasted in full (subagents do not inherit your context): 1. …
  RISK: Token cost: the full spec is pasted 7 times; on very large specs a model may truncate the paste, degrading subagent findings. Merging 7 reports is new orchestrator judgment work wh…
FIX 7.2 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md @ Lines 36-54, from '**Skill reference:**' through the '<process>' tag (also fixes…]
  NOW: **Skill reference:** `.agents/skills/verification-before-completion/SKILL.md` > Before starting verification, read and follow the `verification-before-completion` skill for the full methodology. ## 0.…
  CHANGE: **Skill reference:** `.agents/skills/verification-before-completion/SKILL.md` > Before starting verification, read and follow the `verification-before-completion` skill for the full methodology. </context> <process> ## 0. Platform Check **If `invoke_subagent` is available, you MUST dispatch a `self` subagent for the verification work — do not verify inline.** Do Steps 1-2 yourself first (load context, extract must-haves), then dispatch ONE `self`…
  RISK: The interleaved flow (inline 1-2, delegated 3-4, inline 5-6) is the most intricate routing of all the fixes; a model could still duplicate Step 4. The 'verify each must-have' subag…
FIX 7.3 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-discuss-phase/SKILL.md @ Lines 22-32, section '## 0. Platform Check']
  NOW: ## 0. Platform Check **If `invoke_subagent` is available**, dispatch a `research` subagent to: - Explore the codebase (file structure, recent commits, relevant code) - Read the phase definition from R…
  CHANGE: ## 0. Platform Check **If `invoke_subagent` is available, you MUST dispatch a `research` subagent for context gathering — do not explore the codebase inline.** The subagent prompt MUST contain, pasted in full (subagents do not inherit your context): 1. The phase number and the phase's complete text copied from `.quantis/ROADMAP.md` 2. Any entries from `.quantis/DECISIONS.md` that mention this phase 3. The task: "Explore the codebase: file structu…
  RISK: The 400-word cap could be too tight for very large phases, causing the brief to omit relevant files. The optional second subagent is a MAY, so it remains conditional — if the goal …
FIX 7.4 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md @ Lines 137-152, section '### Subagent Planning (when available)' inside Step 4]
  NOW: ### Subagent Planning (when available) **If `invoke_subagent` is available**, dispatch a `self` subagent with: - The SPEC.md content - The writing-plans skill instructions (`.agents/skills/writing-pla…
  CHANGE: ### Subagent Planning (when available) **If `invoke_subagent` is available, you MUST dispatch a `self` subagent to write the plans — do not write them inline.** The subagent prompt MUST contain, pasted in full (subagents do not inherit your context — paste file CONTENTS, not paths, except where noted): - The SPEC.md content - The full contents of `.agents/skills/writing-plans/SKILL.md` - The phase directory path (`$PHASE_DIR`) and the naming conv…
  RISK: Pasting SPEC.md + writing-plans skill + CONSTITUTION + RESEARCH + ARCHITECTURE into one prompt can be very large; on token-constrained platforms a model may summarize instead of pa…
FIX 7.5 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-research-phase/SKILL.md @ Lines 19-21, immediately after the '<process>' tag, before '## 1. Assess Discove…]
  NOW: <process> ## 1. Assess Discovery Level
  CHANGE: <process> ## 0. Platform Check **If `invoke_subagent` is available, you MUST dispatch research subagents for L1.5+ discovery — do not research inline.** First assess the discovery level (Step 1), then: - **L0/L1:** no subagent needed; handle inline. - **L1.5/L2/L3:** list the open research questions, then read `.agents/skills/dispatching-parallel-agents/SKILL.md` and dispatch **one `research` subagent per independent question, all in parallel**. …
  RISK: The Step 0 'first do Step 1' ordering inversion (level assessment must precede dispatch) could confuse a model that reads strictly sequentially — mitigated by stating it explicitly…
FIX 7.6 [/Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-map/SKILL.md @ Lines 20-21, section '## 2. Analyze Codebase']
  NOW: ## 2. Analyze Codebase **Read and follow `.agents/skills/codebase-mapper/SKILL.md` exactly.**
  CHANGE: ## 2. Analyze Codebase **Read and follow `.agents/skills/codebase-mapper/SKILL.md` exactly.** **If `invoke_subagent` is available, you MUST run the 5 analysis domains as parallel `research` subagents — do not analyze inline.** Read `.agents/skills/dispatching-parallel-agents/SKILL.md`, then dispatch one subagent per domain (Structure, Dependency, Pattern, Integration, Technical debt), all invoked together. Each prompt MUST contain: that domain's …
  RISK: The 5 domains are not perfectly independent (pattern analysis benefits from knowing structure), so 5 isolated agents may produce some redundant exploration and the orchestrator mus…

## Issue 8 — rootCause=mixed conf=medium
WHY: Predominantly a design flaw — the contracts BETWEEN workflows are mutually contradictory in the written artifacts themselves, so even a perfectly compliant model breaks the chain — compounded by instruction-clarity gaps. Hard evidence the system was never run end-to-end: (1) the executor's plan discovery glob `ls "$PHASE_DIR"/*-PLAN.md` (.agents/skills/wf-execute/SKILL.md:45) does not match the plans the system itself produced (`3.1-PLAN-A.md`, `…
CONF-REASON: The fixes close every concrete contract break found in the trace (plan discovery glob, silent-pass on empty dirs, summary naming, wave default, dual-format execution, the missing /execute→/verify handoff, gap-plan filenames, dir-resolution divergence…
- Q: 1. Are there any inter-workflow dependencies that could break (e.g., /plan expects output from /discuss-phase in a speci…
  A: Yes, at least six. (a) /discuss-phase delegates to brainstorming "in discuss/explore mode" — a mode that does not exist in brainstorming/SKILL.md; brainstorming's mandatory checklist instead writes a SPEC.md to the phase dir and terminates by invoking writing-plans directly, bypassing /plan's orchestration (planning lock, research, ecosystem discovery, plan checker, commit). (b) /plan's Planning L…
- Q: 2. Are file paths consistent across workflows (e.g., phase directory resolution)?
  A: No. Three different phase-dir resolution strategies and three plan/summary naming conventions coexist. Resolution: /plan CONSTRUCTS the dir from an unanchored `grep -i "Phase $PHASE" ROADMAP.md | head -n 1` plus slugification — in the real multi-milestone ROADMAP.md, "Phase 1" appears in 4 different milestones (lines 10, 26, 69, 130), so head -1 picks an ARCHIVED milestone's title and creates a wr…
- Q: 3. Are state files (STATE.md, JOURNAL.md) updated consistently?
  A: No. STATE.md has at least four competing formats: templates/state.md (frontmatter `updated:`, Milestone/Phase/Status/Plan, "## Last Action", "## Active Decisions"); wf-pause's full dump ("## Current Position / ## Last Session Summary / ## In-Progress Work / ## Blockers / ## Context Dump / ## Next Steps"); wf-verify's partial snippet (only Phase + Status); and executing-plans' per-task triple (Phas…
- Q: 4. Simulate: what artifacts exist after each step, and does the next workflow find exactly what it expects (names, paths…
  A: Step 1 /discuss-phase 3: if the model follows brainstorming's checklist literally, artifacts are `.quantis/phases/3.?-{slug}/SPEC.md` plus a direct jump into writing-plans (bypassing /plan); if it improvises the undefined "discuss mode", artifacts are DECISIONS.md entries and an unspecified STATE.md edit, and it suggests integer "/plan 3". Step 2 /plan 3: the unanchored ROADMAP grep resolves "Phas…
FIX 8.1 [.agents/skills/wf-execute/SKILL.md @ Step 2 "Discover Plans", lines 39-50]
  NOW: # Dynamically find the phase directory by prefix PHASE_DIR=$(find .quantis/phases -maxdepth 1 -name "${PHASE}-*" | head -n 1) if [ -z "$PHASE_DIR" ]; then echo "Error: phase directory starting with ${…
  CHANGE: # Dynamically find the phase directory by prefix (must match exactly one) MATCHES=$(find .quantis/phases -maxdepth 1 -name "${PHASE}-*" | sort) MATCH_COUNT=$(printf '%s\n' "$MATCHES" | grep -c .) if [ "$MATCH_COUNT" -eq 0 ]; then echo "Error: no phase directory for ${PHASE}. If phases use subphase numbers, pass the full number (e.g. /execute 3.1). Otherwise run /plan ${PHASE} first." exit 1 fi if [ "$MATCH_COUNT" -gt 1 ]; then echo "Error: multip…
  RISK: The wider glob could pick up stray files containing 'PLAN' in their name (e.g. PLAN-NOTES.md) and treat them as executable plans; the user-confirm step adds one interaction on legi…
FIX 8.2 [.agents/skills/wf-execute/SKILL.md @ Step 3 "Group by Wave" (line 53) and Step 4 items 3 and 5 (lines 77-79)]
  NOW: Read `wave` field from each plan's YAML frontmatter. Group plans by wave number. Lower waves first.
  CHANGE: Read `wave` field from each plan's YAML frontmatter. **If a plan has no frontmatter or no `wave` field, treat it as `wave: 1` — do not error and do not skip the plan.** Group plans by wave number. Lower waves first. Additionally replace line 77 "3. Follow `<task>` blocks in order, run `<verify>` commands" with: "3. Execute the plan's tasks in order. Plans come in two formats — handle both: `<task>` XML blocks (run each `<verify>` command, check `…
  RISK: Dual-format tolerance entrenches having two plan formats instead of forcing convergence; a model could mis-detect format on hybrid files (the real 3.1-PLAN-A.md is frontmatter + ch…
FIX 8.3 [.agents/skills/writing-plans/SKILL.md @ Line 18 ("Save plans to") and the "Plan Document Header" section, lines 45-49]
  NOW: **Save plans to:** `.quantis/phases/{N}.{M}-{slug}/{N}.{M}-PLAN.md` - (User preferences for plan location override this default)
  CHANGE: **Save plans to:** `.quantis/phases/{N}.{M}-{slug}/{N}.{M}-{plan-slug}-PLAN.md` (e.g. `3.1-skill-loading-fixes-PLAN.md`) - One file per plan. Multiple plans in the same phase directory get different `{plan-slug}` values. The filename MUST end in `-PLAN.md` — `/execute` discovers plans by that suffix. Never use letter suffixes after PLAN (not `3.1-PLAN-A.md`). - (User preferences for plan location override this default) Also, in the "Plan Document…
  RISK: writing-plans is an upstream-derived (Superpowers) skill; adding Quantis-specific frontmatter couples it to Quantis and complicates future upstream syncs. Existing phase dirs with …
FIX 8.4 [.agents/skills/wf-plan/SKILL.md @ Step 2 bash block, lines 59-64, and Step 4 line 164]
  NOW: # Resolve dynamic phase directory slug from ROADMAP.md PHASE_TITLE=$(grep -i "Phase $PHASE" .quantis/ROADMAP.md | head -n 1 | sed -E 's/.*Phase [0-9.]+:? (.*)/\1/' | tr -d '') PHASE_SLUG=$(echo "$PHAS…
  CHANGE: # Reuse an existing phase directory if one exists (same resolution as /execute and /verify) PHASE_DIR=$(find .quantis/phases -maxdepth 1 -name "${PHASE}-*" | head -n 1) if [ -z "$PHASE_DIR" ]; then # Derive slug from the phase HEADING only (anchored), taking the LAST match so archived # milestones earlier in ROADMAP.md with reused phase numbers are skipped PHASE_TITLE=$(grep -E "^#{2,4} Phase ${PHASE}: " .quantis/ROADMAP.md | tail -n 1 | sed -E '…
  RISK: tail -n 1 assumes the current milestone is last in ROADMAP.md — a reordered roadmap would still pick the wrong heading; the regex dot in subphase numbers (e.g. '3.1') is unescaped …
FIX 8.5 [.agents/skills/wf-execute/SKILL.md @ offer_next, lines 118-124 ("Phase complete, more phases")]
  NOW: **Phase complete, more phases:** ``` ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Quantis ► PHASE {N} COMPLETE ✓ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ▶ /plan {N+1} or /execut…
  CHANGE: **Phase complete, more phases:** ``` ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Quantis ► PHASE {N} COMPLETE ✓ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ▶ /verify {N} — independent verification with evidence (recommended) ▶ /plan {N+1} — plan the next phase ``` (Never offer `/execute {N+1}` here — the next phase has no plans until `/plan {N+1}` runs.)
  RISK: Slight redundancy: wf-execute step 5 already does a goal check, so users may feel /verify is duplicate work; if /verify's own STATE update then differs from execute's, the last wri…
FIX 8.6 [.agents/skills/wf-verify/SKILL.md @ Step 5 PASS output, lines 247-249, and FAIL gap-plan instruction, lines 256-258]
  NOW: ▶ Next Up /execute {N+1} — proceed to next phase
  CHANGE: ▶ Next Up /plan {N+1} — plan the next phase (phases need plans before /execute can run) Also replace lines 256-258 "**Create gap closure plans:**\n\nFor each failed must-have, create a fix plan in `\"$PHASE_DIR/\"`:" with: "**Create gap closure plans:**\n\nFor each failed must-have, create a fix plan in `\"$PHASE_DIR/\"` named `{N}-gap-{issue-slug}-PLAN.md`. The `-PLAN.md` suffix is REQUIRED — `/execute {N} --gaps-only` discovers gap plans by tha…
  RISK: Committing the whole $PHASE_DIR may sweep in unrelated scratch files (e.g. BROWSER-VERIFY.md mid-handoff); '/plan {N+1}' as the only PASS suggestion drops the (rare) case where N+1…
FIX 8.7 [.agents/skills/wf-discuss-phase/SKILL.md @ Step 2 "Brainstorm", line 42]
  NOW: **Read and follow `.agents/skills/brainstorming/SKILL.md`** in discuss/explore mode.
  CHANGE: **Read and follow `.agents/skills/brainstorming/SKILL.md`, applying ONLY checklist items 1-5** (explore context, offer visual companion, clarifying questions, propose 2-3 approaches, present design for approval). **STOP before checklist item 6:** do NOT write a SPEC.md and do NOT invoke writing-plans — in the Quantis cycle, spec and plans are produced later by `/plan {N}`. When discussion converges, return to Step 3 below and document the decisio…
  RISK: Truncating the checklist conflicts with brainstorming's own MUST language, so a model reading both files still sees a contradiction — it now has explicit precedence here, but brain…
FIX 8.8 [.agents/skills/wf-verify/SKILL.md @ Step 5 "If PASS", lines 228-233]
  NOW: Update `.quantis/STATE.md`: ```markdown ## Current Position - **Phase**: {N} (verified) - **Status**: ✅ Complete and verified ```
  CHANGE: Update `.quantis/STATE.md` **in place — edit only the fields below inside the existing `## Current Position` section and refresh `## Next Steps`; preserve all other sections (Last Session Summary, Blockers, Context Dump) so `/resume-session` still finds them**: ```markdown ## Current Position - **Phase**: {N} (verified) - **Status**: ✅ Complete and verified ## Next Steps 1. /plan {N+1} ```
  RISK: "Edit in place" assumes STATE.md already has the wf-pause section structure; on a fresh project whose STATE.md follows the divergent templates/state.md layout (Milestone/Last Actio…


# ISSUE-FIX VERDICTS (skeptic re-checks, unordered)

- citations=True analysis=True fixes=True | All verbatim quotes match the real files and the core analysis survives adversarial checking, but I found these minor problems: 1. COUNTING SLIP (answer 1): "A grep for 'Read and follow' across all wf-* skills returns 7 workflows" — verified grep returns 7 MATCHES across 6 workflows (wf-execute contributes two hits at lines 74 and 75). The parenthetical list itself correctly shows 6 files, so the data is right but the "7 workflows" label is wrong. 2. STEP/LINE MISMATCH (answer 3c and rootCauseEx…
- citations=True analysis=True fixes=True | All verbatim quotes verified against the real files (wf-execute/SKILL.md:9,72-76; subagent-driven-development/SKILL.md:16-28; using-quantis/SKILL.md:3,7,30,36,109; using-quantis/references/antigravity-tools.md:21; executing-plans/SKILL.md:12,14; QUANTIS-STYLE.md:251-261; SPEC.md:229-236,410-415; adapters/ANTIGRAVITY.md:42) and every proposed-fix currentText is present verbatim and uniquely applicable. Remaining problems, in rough order of severity: 1. MISSED CONFLICTING SIGNAL + RESIDUAL GAP IN …
- citations=True analysis=True fixes=True | All quoted texts and line refs verified against the real files (wf-discuss-phase:4,12,17,41-42,51-52,59-60,67-69; brainstorming:22,29,31-32,66,111-112,116-117; using-quantis:81,129; wf-research-phase:34; wf-stress-test:44; wf-plan:50-53,59-64,139-146,157-162; writing-plans:124; .quantis/templates/spec.md:1; scripts/*.sh contain no SPEC checks; phases 3.1/3.2 both contain SPEC.md). All eight currentText blocks are present verbatim and uniquely matchable (fix 8 confirmed via grep -F, 1 match; bann…
- citations=True analysis=True fixes=True | All quoted texts and line references check out (wf-execute/SKILL.md:96-105, :92-94, :109-114, :118-124; wf-pause/SKILL.md:98-100 (git add at :99), :22-65, :144-177; wf-verify/SKILL.md:226-233, :306-311, :249, :33, :68; templates/roadmap.md:32, :36-38, :67-74, :78-85; wf-complete-milestone/SKILL.md:18 grep gate; wf-progress/SKILL.md:24-29, :85; wf-resume-session/SKILL.md:106; executing-plans/SKILL.md:91-94; subagent-driven-development/SKILL.md:314; using-quantis/SKILL.md:97-100; STATE.md:2; ROADM…
- citations=True analysis=True fixes=True | All checked citations match the real files (verified ~25 quotes/line refs across /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md, wf-execute/SKILL.md, requesting-code-review/SKILL.md, requesting-code-review/code-reviewer.md, subagent-driven-development/SKILL.md, subagent-driven-development/code-quality-reviewer-prompt.md, executing-plans/SKILL.md, .quantis/templates/SUMMARY.md, .quantis/templates/VERIFICATION.md, .agents/rules/QUANTIS-STYLE.md:37, wf-audit-milestone/SKIL…
- citations=True analysis=True fixes=True | CITATIONS (all quotes verbatim-verified; only labeling imprecisions): (1) The quote "Add unresolved questions to SPEC.md's `## Unresolved Questions` section" is on wf-stress-test/SKILL.md:101 alone; :100 is the heading "## 5. Update Spec (if applicable)" — cited range :100-101 is generous but not a misquote. (2) Fix 5's location says "lines 28-38" but its currentText spans only 28-36 (line 38 is the inline-fallback line, deliberately untouched). (3) Fix 9's location "lines 205-211" labels the wh…

# DIMENSION FINDINGS

## delegation-consistency (17)
[CRITICAL] wf-discuss-phase delegation contract conflicts with brainstorming's mandatory checklist and terminal state | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-discuss-phase/SKILL.md:42-60 (vs brainstorming/SKILL.md lines 12-33, 66, 133-136)
  NOW: Step 2 says 'Read and follow .agents/skills/brainstorming/SKILL.md in discuss/explore mode'. Brainstorming has no 'discuss/explore mode'. Worse, brainstorming contains a mandatory checklist ('You MUST create a task for each of these items and complet…
  FIX: Per ROADMAP intent (option a): ```diff ## 2. Brainstorm -**Read and follow `.agents/skills/brainstorming/SKILL.md`** in discuss/explore mode. +**Read and follow `.agents/skills/brainstorming/SKILL.md` exactly.** Run the full +checklist, including writing the design doc to `.quantis/phases/{N}.{M}-{s…
[HIGH] wf-stress-test delegates to brainstorming in a non-existent 'adversarial critique mode' | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-stress-test/SKILL.md:7-9, 44 (vs brainstorming/SKILL.md lines 12-18, 21-33)
  NOW: Header (line 7) is '# /stress-test → brainstorming skill (critique mode)' and Step 2 (line 44) says '**Read `.agents/skills/brainstorming/SKILL.md`** and adopt adversarial critique mode.' Brainstorming contains zero critique content — it is a collabo…
  FIX: ```diff -# /stress-test → brainstorming skill (critique mode) +# /stress-test Workflow ... ## 2. Set Critique Mindset -**Read `.agents/skills/brainstorming/SKILL.md`** and adopt adversarial critique mode. +Adopt an adversarial mindset using the 7-dimension framework below. Do NOT load the +`brainsto…
[HIGH] wf-plan: explicit 'Read and follow' delegation exists only in the inline fallback; the primary subagent path reduces the skill to a context … | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:139-155
  NOW: Step 4 branches on invoke_subagent. The subagent branch (lines 139-151) — the path taken on CLI 'agy' and Standalone, the primary platforms per ROADMAP Phase 3.2 'CLI-First Migration' — lists 'The writing-plans skill instructions (`.agents/skills/wri…
  FIX: ```diff **If `invoke_subagent` is available**, dispatch a `self` subagent with: - The SPEC.md content -- The writing-plans skill instructions (`.agents/skills/writing-plans/SKILL.md`) +- The writing-plans methodology: **read `.agents/skills/writing-plans/SKILL.md` and + include its full text in the …
[HIGH] wf-verify: skill delegation buried in the <context> block as a blockquote, not a numbered process step — a gap the ROADMAP itself flags as u… | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:36-52
  NOW: The only load instruction for verification-before-completion is line 36 '**Skill reference:** `.agents/skills/verification-before-completion/SKILL.md`' plus a blockquote at line 38: '> Before starting verification, read and follow the `verification-b…
  FIX: ```diff -**Skill reference:** `.agents/skills/verification-before-completion/SKILL.md` - -> Before starting verification, read and follow the `verification-before-completion` skill for the full methodology. </context> ... <process> +## 1. Load Methodology +**Read and follow `.agents/skills/verificat…
[HIGH] Conflicting no-subagent fallbacks: wf-execute routes to executing-plans, SDD routes to 'inline SDD mode' | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:72-76 (vs subagent-driven-development/SKILL.md lines 16-28)
  NOW: wf-execute step 4.2 says: 'If NOT available: Read and follow .agents/skills/executing-plans/SKILL.md.' But subagent-driven-development's own 'Platform Detection' section says: 'If NOT available (IDE): Use inline SDD mode — execute each task yourself …
  FIX: Align wf-execute to SDD's inline mode (preserves review gates): ```diff - - **If NOT available:** Read and follow `.agents/skills/executing-plans/SKILL.md`. + - **If NOT available:** Read and follow + `.agents/skills/subagent-driven-development/SKILL.md` in its documented + **inline SDD mode** (exec…
[HIGH] writing-plans' Execution Handoff presents a user menu that wf-execute explicitly forbids, hijacking the /plan → /execute boundary | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/writing-plans/SKILL.md:134-153 (vs wf-execute/SKILL.md line 76, wf-plan/SKILL.md lines 166-181)
  NOW: wf-plan line 155 commands 'Read and follow .agents/skills/writing-plans/SKILL.md exactly.' Followed exactly, writing-plans ends with: 'After saving the plan, offer execution choice ... Two execution options ... Which approach?' — and then starts exec…
  FIX: ```diff ## Execution Handoff -After saving the plan, offer execution choice: -**"Plan complete and saved to `...`. Two execution options: ... Which approach?"** +If invoked from the /plan workflow: report "Plan complete and saved to `<path>`. +Run `/execute {N}` to implement — execution methodology …
[MEDIUM] wf-verify structural defect: <context> never closed, Step 0 lives outside <process> | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:28-54
  NOW: Verified by grep: <context> opens at line 28 and has no closing </context>; <process> opens at line 54 and closes at 313. As a result '## 0. Platform Check' (lines 40-52) and the verification-before-completion delegation blockquote (line 38) are stru…
  FIX: ```diff **Required files:** - `.quantis/phases/{phase}.{subphase}-{slug}/*-SUMMARY.md` — What was implemented +</context> + +<process> ## 0. Platform Check ... -<process> ## 1. Load Verification Context ```
[MEDIUM] wf-execute <related> table claims two methodology skills that the process never loads (verification-before-completion, context-health-monito… | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:85-94, 109-114, 138-146
  NOW: The <related> table lists 'verification-before-completion | Must-have verification methodology' (line 144) and 'context-health-monitor | 3-strike rule enforcement' (line 145). But Step 5 'Verify Phase Goal' (lines 85-94) re-implements verification in…
  FIX: ```diff ## 5. Verify Phase Goal After all waves: +0. **Read and follow `.agents/skills/verification-before-completion/SKILL.md`** for the evidence rules below. 1. Read phase goal from ROADMAP.md ... <context_hygiene> -**After 3 failed debugging attempts:** +**After 3 failed debugging attempts** (rea…
[MEDIUM] wf-plan <related> table claims brainstorming is 'Use for research phase' but Step 3 (Handle Research) never references or loads it | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:66-79, 205-211
  NOW: Related table row (line 210): '| `brainstorming` | Use for research phase |'. The actual research step (Step 3, lines 66-79) defines discovery levels L0-L3 and output files (DISCOVERY.md/RESEARCH.md) with no methodology skill reference at all. The br…
  FIX: ```diff <related> ### Skills | Skill | Purpose | |-------|---------| | `writing-plans` | Planning methodology (delegated) | -| `brainstorming` | Use for research phase | ``` (or add to Step 3: 'For L2/L3 research, **read and follow `.agents/skills/wf-research-phase/SKILL.md`**' if that is the intend…
[MEDIUM] wf-stress-test subagent branch passes 'the brainstorming skill' with no path and no read instruction; the explicit Step 2 load is bypassed o… | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-stress-test/SKILL.md:28-38
  NOW: Step 0: 'If `invoke_subagent` is available, dispatch a `self` subagent with: ... The brainstorming skill in adversarial critique mode'. No file path, no instruction to read the skill, no guidance on conveying it (full text vs. name-drop). Since the s…
  FIX: ```diff **If `invoke_subagent` is available**, dispatch a `self` subagent with: - The SPEC.md and/or PLAN.md content -- The brainstorming skill in adversarial critique mode -- Instructions to attack across all 7 dimensions below +- The full 7-dimension adversarial framework from Step 3 below (copy i…
[MEDIUM] No canonical delegation pattern exists; four distinct delegation styles are in use across the ten audited workflows | /Users/piemelel/GitHub/Coding-Workflow/.agents/rules/QUANTIS-STYLE.md:28-33
  NOW: QUANTIS-STYLE.md's Skills section says only 'Is referenced by parent workflows' — it never defines HOW. Result, full workflow→skill→style matrix from this audit: | Workflow | Skill | Delegation style | |---|---|---| | wf-discuss-phase | brainstorming…
  FIX: Add to QUANTIS-STYLE.md under 'Workflows': ```markdown ### Skill Delegation Pattern (canonical) Every methodology delegation MUST be a numbered <process> step of the exact form: **Read and follow `.agents/skills/<name>/SKILL.md`** [+ scope overrides if any] In subagent branches, the orchestrator MUS…
[MEDIUM] using-quantis command-to-skill map is stale: /execute row omits SDD (the documented CLI-first default) and lists modes that don't exist | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/using-quantis/SKILL.md:104-121
  NOW: The bootstrap skill (read at session start per its description) maps '/execute {N} | Execute phase plans | executing-plans' — but wf-execute line 9 declares 'Execution methodology is powered by subagent-driven-development (CLI-first default) or execu…
  FIX: ```diff -| `/execute {N}` | Execute phase plans | executing-plans | +| `/execute {N}` | Execute phase plans | subagent-driven-development (default) / executing-plans (no-subagent fallback) | -| `/stress-test` | Adversarial spec/plan review | brainstorming (critique) | +| `/stress-test` | Adversarial…
[MEDIUM] wf-complete-milestone re-implements final verification with no delegation to verification-before-completion | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-complete-milestone/SKILL.md:30-36
  NOW: Step 2 'Run Final Verification' says only: 'Verify all must-haves from ROADMAP.md: Run verification commands, Capture evidence, Create VERIFICATION.md if not exists.' This is the highest-stakes completion claim in the system (closing a milestone), ye…
  FIX: ```diff ## 2. Run Final Verification +**Read and follow `.agents/skills/verification-before-completion/SKILL.md`.** Verify all must-haves from ROADMAP.md: - Run verification commands - Capture evidence - Create VERIFICATION.md if not exists ```
[LOW] wf-pause references context-health-monitor as a parenthetical aside with no path and no load instruction | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-pause/SKILL.md:159-164
  NOW: The Auto-Save Protocol's detection step says: '1. **Detect** context health warning signals (see context-health-monitor skill)'. Verified by grep, this is the file's only skill reference: a parenthetical, no `.agents/skills/...` path, no 'read'. The …
  FIX: ```diff -1. **Detect** context health warning signals (see context-health-monitor skill) +1. **Detect** context health warning signals — read + `.agents/skills/context-health-monitor/SKILL.md` for the signal table and thresholds ```
[LOW] wf-verify <related> table lists context-health-monitor with no load instruction anywhere in the process | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:350-355
  NOW: Related table row: '| `context-health-monitor` | Context budget monitoring |'. No process step in wf-verify mentions context budgets or the skill — the reference is metadata-only with no corresponding behavior, the same dead-reference pattern as wf-e…
  FIX: ```diff ### Skills | Skill | Purpose | |-------|---------| | `verification-before-completion` | Detailed verification methodology | -| `context-health-monitor` | Context budget monitoring | ```
[LOW] wf-verify omits the 'Skill-powered workflow' header convention used by every other delegating workflow | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:7
  NOW: Delegating workflows signal their delegation in the H1 and an intro blockquote: wf-discuss-phase L7-9 ('# /discuss-phase → brainstorming skill' + '> **Skill-powered workflow.**...'), likewise wf-plan, wf-execute, wf-stress-test. wf-verify, which also…
  FIX: ```diff -# /verify Workflow +# /verify → verification-before-completion skill + +> **Skill-powered workflow.** Verification methodology is powered by +> `verification-before-completion`. This workflow adds Quantis phase context, +> evidence reporting, and gap-closure plan generation. ```
[LOW] wf-execute nests methodology selection (and skill read) inside the per-plan loop, implying re-selection and re-read for every plan | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:67-78
  NOW: Step 4 reads 'For each wave in order, for each plan in the wave: 1. Load plan context ... 2. **Select execution methodology** ... Read and follow `.agents/skills/subagent-driven-development/SKILL.md`'. Taken literally, the model re-checks invoke_suba…
  FIX: ```diff ## 4. Execute Waves +**Select execution methodology once (automatic — do NOT ask the user):** +- If `invoke_subagent` is available: **Read and follow + `.agents/skills/subagent-driven-development/SKILL.md`** for all plans below. +- If NOT: **Read and follow `.agents/skills/executing-plans/SK…

## instruction-clarity (39)
[CRITICAL] wf-execute requires a `wave` YAML frontmatter field that writing-plans never produces | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:53 (vs writing-plans/SKILL.md:47-61)
  NOW: wf-execute Step 3 says to group plans by the `wave` field in each plan's YAML frontmatter. But the delegated planning skill (writing-plans) defines a mandatory plan header with NO YAML frontmatter at all ('Every plan MUST start with this header' — a …
  FIX: In writing-plans/SKILL.md 'Plan Document Header', add before the H1: ```markdown --- phase: {N}.{M} wave: {wave number — plans in the same wave have no dependencies on each other} --- ``` AND in wf-execute Step 3 add: 'If a plan has no `wave` field, treat it as wave 1.'
[CRITICAL] wf-plan's checker and wf-execute reference XML <task>/<verify>/<done> blocks that writing-plans plans don't contain | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:166-174, 185-191 (and wf-execute/SKILL.md:77, QUANTIS-STYLE.md:67-103, writing-plans/SKILL.md:65-104)
  NOW: wf-plan Step 5 verifies '`<verify>` commands are executable' and '`<done>` criteria are measurable', and the <task_types> table defines `auto` / `checkpoint:human-verify` task types. wf-execute Step 4.3 says 'Follow `<task>` blocks in order, run `<ve…
  FIX: In wf-plan Step 5, replace: - '- [ ] `<verify>` commands are executable' → '- [ ] Every task has Run: commands with Expected: output' - '- [ ] `<done>` criteria are measurable' → '- [ ] Every task's final step has measurable acceptance (test passes, command exit 0)' In wf-execute Step 4.3 replace 'F…
[CRITICAL] Two contradictory SPEC.md locations: root .quantis/SPEC.md (Planning Lock) vs per-phase .quantis/phases/{N}.{M}-{slug}/SPEC.md | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/brainstorming/SKILL.md:29, 111 (vs wf-plan/SKILL.md:24,52; wf-verify/SKILL.md:32; PROJECT_RULES.md:15; using-quantis/SKILL.md:130)
  NOW: brainstorming instructs the model to save the spec to `.quantis/phases/{N}.{M}-{slug}/SPEC.md`, and using-quantis File Conventions agrees. But wf-plan's Planning Lock greps `.quantis/SPEC.md` for FINALIZED, wf-verify requires `.quantis/SPEC.md — Orig…
  FIX: In brainstorming line 29 and 111, change to: 'save to `.quantis/phases/{N}.{M}-{slug}/SPEC.md` (phase design doc). If `.quantis/SPEC.md` (project spec) does not exist or is not FINALIZED, also create/update it — /plan's Planning Lock checks the project-level file.' In wf-verify <context>, clarify: '…
[CRITICAL] wf-discuss-phase invokes brainstorming 'in discuss/explore mode' — a mode that does not exist — and brainstorming's HARD-GATE flow contradic… | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-discuss-phase/SKILL.md:42, 51-60 (vs brainstorming/SKILL.md:12-14, 21-33, 66)
  NOW: Step 2 says 'Read and follow `.agents/skills/brainstorming/SKILL.md` in discuss/explore mode' — but brainstorming defines no modes (verified by grep: no 'discuss', 'explore mode', or 'critique mode' in the file). brainstorming instead imposes a manda…
  FIX: Replace line 42 with: '**Read `.agents/skills/brainstorming/SKILL.md` and follow checklist items 1-8** (context, questions, approaches, design, spec, self-review, user review). Write the spec to the phase directory. **Override:** do NOT perform item 9 (invoking writing-plans) — after the user approv…
[HIGH] wf-stress-test tells the model to 'adopt adversarial critique mode' of brainstorming, which contains no critique methodology | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-stress-test/SKILL.md:30-31, 44 (vs brainstorming/SKILL.md:6-14)
  NOW: Step 0 dispatches a subagent with 'the brainstorming skill in adversarial critique mode' and Step 2 says 'Read `.agents/skills/brainstorming/SKILL.md` and adopt adversarial critique mode.' brainstorming is a collaborative design skill (one question a…
  FIX: Delete line 44's brainstorming reference and replace Step 2 with: 'Adopt an adversarial mindset: your job is to BREAK the spec, not validate it. Assume everything is wrong until proven otherwise.' Remove 'brainstorming skill in adversarial critique mode' from Step 0 and the <related> table.
[HIGH] Undefined subagent types: `self` and `research` are dispatched but never defined anywhere | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:139 (also wf-verify/SKILL.md:42, wf-stress-test/SKILL.md:28, wf-discuss-phase/SKILL.md:24)
  NOW: Three workflows say 'dispatch a `self` subagent' and wf-discuss-phase says 'dispatch a `research` subagent'. The only subagent-type definition mechanism in the system is SDD's `define_subagent(...)` which defines implementer/spec-reviewer/code-qualit…
  FIX: In each of the four workflows, replace 'dispatch a `self` subagent with:' / 'dispatch a `research` subagent to:' with: 'dispatch a subagent (via `invoke_subagent`) whose prompt contains exactly the following materials:' — and add 'No predefined subagent type is required; construct the prompt from th…
[HIGH] {N}.{M} is load-bearing across four files but no file ever explains where M comes from | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/writing-plans/SKILL.md:18 (also brainstorming/SKILL.md:29; using-quantis/SKILL.md:129-130; wf-plan/SKILL.md:63,164)
  NOW: writing-plans says 'Save plans to `.quantis/phases/{N}.{M}-{slug}/{N}.{M}-PLAN.md`' and brainstorming saves specs to `{N}.{M}-{slug}/SPEC.md`, but nothing defines M. ROADMAP phases are themselves numbered N.M (e.g. 'Phase 3.1'), wf-plan's bash builds…
  FIX: Add to using-quantis 'File Conventions': '**Numbering:** {N}.{M} is the ROADMAP phase number (e.g. Phase 3.1 → directory `3.1-{slug}`). When a phase needs multiple plans, name them `{N}.{M}-PLAN-1.md`, `{N}.{M}-PLAN-2.md`, ... Matching summaries use the same index (`{N}.{M}-SUMMARY-1.md`).' Then ali…
[HIGH] Phase directory lookup `-name "${PHASE}-*" | head -n 1` silently fails for whole-number arguments and silently drops sibling subphases | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:39-44 (same pattern in wf-verify/SKILL.md:59-65)
  NOW: Directories are named `{N}.{M}-{slug}` (e.g. `3.1-workflow-fixes`). If the user runs `/execute 3`, the glob `3-*` matches nothing → 'Error: phase directory starting with 3- not found'. If they run `/execute 3` intending all of phase 3 and dirs `3.1-.…
  FIX: Replace the snippet with: ```bash MATCHES=$(find .quantis/phases -maxdepth 1 -name "${PHASE}-*" -o -name "${PHASE}.*-*" | sort) COUNT=$(echo "$MATCHES" | grep -c .) # 0 matches: error. >1 matches: execute each in order (or ask user). ``` And change argument-hint to `"<phase-number e.g. 3.1>"`.
[HIGH] Summary file naming `$PHASE-SUMMARY.md` is per-phase but created per-plan, breaking the 'PLAN without matching SUMMARY' resume logic | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:45-48, 79
  NOW: Step 2 builds the incomplete-plan list as 'PLAN without matching SUMMARY' — 'matching' is undefined. Step 4.5 says 'After all tasks in plan: create `$PHASE-SUMMARY.md`' — a single filename derived from the phase number, created once per plan. If a ph…
  FIX: Step 4.5: 'After all tasks in plan: create a summary named after the plan — for `X-PLAN.md` create `X-SUMMARY.md` — inside `$PHASE_DIR/`.' Step 2: 'incomplete plans = every `*-PLAN.md` lacking a sibling `*-SUMMARY.md` with the same prefix.'
[HIGH] writing-plans' Execution Handoff presents a user menu that wf-execute and SDD explicitly forbid | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/writing-plans/SKILL.md:134-153 (vs wf-execute/SKILL.md:72-76; subagent-driven-development/SKILL.md:28)
  NOW: writing-plans ends with: offer two execution options and ask '**Which approach?**'. wf-execute says the methodology selection is '**NOT a choice. Do not present a menu. Do not ask the user.**' and SDD says 'Do not ask the user which mode to use.' The…
  FIX: Replace the 'Execution Handoff' section with: 'After saving the plan, announce the path and proceed: check whether `invoke_subagent` is available. If yes → REQUIRED SUB-SKILL: subagent-driven-development. If no → REQUIRED SUB-SKILL: executing-plans. Do not ask the user which approach to use.'
[HIGH] SDD's terminal steps (final whole-implementation review + finishing-a-development-branch) conflict with wf-execute's per-plan wrapping | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/subagent-driven-development/SKILL.md:93-94, 112-113 (vs wf-execute/SKILL.md:67-105)
  NOW: SDD's process graph ends every run with 'Dispatch final code reviewer subagent for entire implementation' → 'Use finishing-a-development-branch skill' (merge/cleanup). wf-execute invokes SDD once *per plan* inside a wave loop, then has its own phase-…
  FIX: Add to wf-execute Step 4, after the methodology selection block: '> **Scope note:** the methodology skill executes ONE plan. Skip its end-of-session steps (final whole-implementation review, finishing-a-development-branch) — wf-execute Steps 5-6 handle phase-level verification and completion.'
[HIGH] Three different mandated responses to the same '3 failed debugging attempts' trigger | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/systematic-debugging/SKILL.md:192-213 (vs context-health-monitor/SKILL.md:27-37; wf-execute/SKILL.md:109-114; PROJECT_RULES.md:198,252)
  NOW: systematic-debugging Phase 4.5: after 3+ failed fixes, 'STOP and question fundamentals... Discuss with your human partner before attempting more fixes' (stay in session, discuss architecture). context-health-monitor Rule 1: 'STOP attempting fixes... …
  FIX: In context-health-monitor Rule 1 and wf-execute <context_hygiene>, add: 'Exception: if the 3 failures match the architectural-problem pattern in systematic-debugging Phase 4.5 (each fix reveals new coupling), first present the architecture question to the user, then pause.' In systematic-debugging P…
[HIGH] PROJECT_RULES says wave tasks 'Run in parallel'; SDD forbids parallel implementer dispatch and wf-execute says SDD is sequential | /Users/piemelel/GitHub/Coding-Workflow/.agents/rules/PROJECT_RULES.md:62-69 (vs subagent-driven-development/SKILL.md:269; wf-execute/SKILL.md:81)
  NOW: PROJECT_RULES (the self-declared 'Single Source of Truth') Wave Execution table says Wave 1 'Run in parallel', Wave 2 'Wait for Wave 1, then parallel'. SDD's Red Flags say 'Never: Dispatch multiple implementation subagents in parallel (conflicts)', a…
  FIX: Change the Wave Execution table's 'Execution' column from 'Run in parallel' to 'No ordering constraint between plans (executed sequentially by SDD)' and add: 'Parallel subagent dispatch is reserved for independent read-only investigations (see dispatching-parallel-agents); implementation tasks are a…
[HIGH] wf-plan's --gaps flag is declared but has no process steps; the gap_closure frontmatter contract is never stated where gap plans are created… | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:21, 68 (vs wf-execute/SKILL.md:49,94)
  NOW: wf-plan declares '`--gaps` — Gap closure mode (reads VERIFICATION.md, skips research)' but the process only mentions it once ('If `--gaps` or `--skip-research`: Skip to step 4') — nothing says to read VERIFICATION.md, what to derive from it, or that …
  FIX: Add to wf-plan after Step 3: '## 3b. Gaps mode (--gaps): Read `$PHASE_DIR/VERIFICATION.md`. For each failed must-have, create a fix plan in `$PHASE_DIR/` using the gap-plan template (frontmatter MUST include `gap_closure: true` and `wave: 1`), then skip to Step 5.' In wf-execute Step 5 FAIL, change …
[HIGH] Token-efficiency rules ('File >200 lines → outline only', '>5 files → stop') contradict the mandatory full reads required by nearly every wo… | /Users/piemelel/GitHub/Coding-Workflow/.agents/rules/PROJECT_RULES.md:206-214 (vs using-quantis/SKILL.md:26,36; systematic-debugging/SKILL.md:131-133; wf-resume-session/SKILL.md:16)
  NOW: The Loading Rules table says 'File >200 lines | Use outline, not full file' and '>5 files needed | Stop, reconsider approach'. But using-quantis mandates 'read the full SKILL.md' for any skill with ≥1% applicability (SDD alone is 317 lines), systemat…
  FIX: Add under 'Loading Rules': '**Exemptions:** SKILL.md files invoked per using-quantis, `.quantis/` state files, and any file a skill explicitly says to read completely are always read in full. The >5-files threshold applies to source-code exploration only, not to workflow-required documents.'
[MEDIUM] Gate checks echo errors but never say whether to halt the workflow | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:51-54 (also wf-new-milestone/SKILL.md:17-21; wf-execute/SKILL.md:30-34)
  NOW: wf-plan Step 1 runs `grep -q "FINALIZED" ".quantis/SPEC.md" || echo "Error: SPEC.md must be FINALIZED"` — the command prints an error but exits 0, and no prose says what to do when the message appears. Same pattern in wf-new-milestone ('echo "Error: …
  FIX: After the bash block in wf-plan Step 1 add: '**If the Error line printed: STOP.** Do not continue to Step 2; tell the user to finalize SPEC.md first. The ⚠️ CONSTITUTION warning is non-blocking.' Add equivalent stop/continue sentences to wf-execute Step 1 and wf-new-milestone Step 1.
[MEDIUM] wf-verify defines a PARTIAL verdict but Step 5 only routes PASS and FAIL | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:192, 216, 224-302
  NOW: The VERIFICATION.md template declares 'verdict: PASS | FAIL | PARTIAL' and the Verdict section repeats '{PASS | FAIL | PARTIAL}'. Step 5 ('Handle Results') has exactly two branches: 'If PASS (all must-haves verified)' and 'If FAIL (some must-haves fa…
  FIX: Add to Step 5: '### If PARTIAL (all automatable must-haves pass; visual must-haves await BROWSER-VERIFY.md): do not update STATE.md to complete. Display the BROWSER VERIFICATION NEEDED banner and stop. Re-run /verify after browser evidence is added.' Or delete 'PARTIAL' from lines 192 and 216.
[MEDIUM] wf-verify's subagent dispatch sources must-haves from SPEC.md while Step 2 sources them from the ROADMAP phase definition | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:42-43 vs 74-76
  NOW: Step 0 (subagent path) says to give the subagent 'The SPEC.md must-haves list'. Step 1-2 (inline path) says read ROADMAP/SPEC/SUMMARYs and 'From the phase definition, identify **must-haves**' — the phase definition lives in ROADMAP.md. So the two exe…
  FIX: Change line 43 from '- The SPEC.md must-haves list' to '- The must-haves extracted from the phase definition in `.quantis/ROADMAP.md` (with `.quantis/SPEC.md` as supporting context)'.
[MEDIUM] wf-verify CLI path never says what verdict/status visually-unverifiable must-haves get | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:129-180
  NOW: Without browser_subagent, the model runs CLI evidence AND generates BROWSER-VERIFY.md for 'any must-have requiring visual proof'. Then the process continues to Step 4 (write VERIFICATION.md with PASS/FAIL per must-have) and Step 5 (route by verdict).…
  FIX: Append to Step 3d: 'In VERIFICATION.md, mark such must-haves **Status: PENDING-BROWSER** (not PASS or FAIL). If any must-have is PENDING-BROWSER and none FAILED, the overall verdict is PARTIAL.'
[MEDIUM] Context-percentage triggers ('~50-70% usage', '70%+ state dump required') give no way for the model to measure context usage | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-pause/SKILL.md:151-157 (also PROJECT_RULES.md:215-221; wf-plan/SKILL.md:33-41)
  NOW: The auto-save trigger table says 'Context usage reaches ~50-70% | Write lightweight state snapshot' and PROJECT_RULES says '70%+ | State dump required, recommend fresh session'. Models on these platforms generally cannot introspect their token usage,…
  FIX: In wf-pause's trigger table, replace 'Context usage reaches ~50-70%' with: 'Observable proxies — any of: 20+ tool calls this session, 10+ files read in full, the platform reports a context warning, or you notice yourself re-reading files you already summarized.' Mirror the same proxies in PROJECT_RU…
[MEDIUM] 'Auto-detects next unplanned phase' has no detection procedure | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:16, 57 (also wf-discuss-phase/SKILL.md:12)
  NOW: Both wf-plan and wf-discuss-phase say the phase argument 'defaults to next unplanned phase', and Step 2 says 'Extract phase number (or auto-detect next unplanned phase from ROADMAP.md)'. 'Unplanned' is never defined. Three plausible readings: (a) fir…
  FIX: In wf-plan Step 2 add: 'Auto-detect = the lowest-numbered phase in the current milestone whose ROADMAP Status is ⬜/🔄 AND whose `.quantis/phases/{N}.{M}-*/` directory contains no `*-PLAN.md`. Confirm the detected phase with the user before proceeding.' Reference the same rule from wf-discuss-phase.
[MEDIUM] ROADMAP.md state is internally contradictory: Phase 3.2 is Complete but 'Depends on: Phase 3.1' which is Not Started, and 3.1's deliverables… | /Users/piemelel/GitHub/Coding-Workflow/.quantis/ROADMAP.md:179-187, 199-218
  NOW: Phase 3.1 is '⬜ Not Started' with deliverables like '`discuss-phase.md` — Enforce full brainstorming checklist' and '`execute.md` — Deterministically use SDD' — but those files were consolidated into wf-* skills by Phase 3.2 (which is ✅ Complete and …
  FIX: Update Phase 3.1: check off the two deliverables completed by 3.2 (execute SDD determinism, verify skill loading), rename targets to `wf-discuss-phase/SKILL.md` and `wf-pause/SKILL.md`, and change Status to '🔄 In Progress (2 of 4 done via Phase 3.2)'. Remove or invert the 'Depends on: Phase 3.1' lin…
[MEDIUM] wf-new-milestone's ROADMAP template implies overwriting the file, contradicting wf-complete-milestone and the project's own accumulating ROA… | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-new-milestone/SKILL.md:53-76 (vs wf-complete-milestone/SKILL.md:86; .quantis/ROADMAP.md:1-44)
  NOW: Step 4 'Update ROADMAP.md' shows a full-file template beginning '# ROADMAP.md / > **Current Milestone**: {name}' — read literally, this replaces the whole file. wf-complete-milestone Step 5 says 'Clear ROADMAP.md phases section (keep header)'. Yet th…
  FIX: In Step 4 change the heading to '## 4. Append to ROADMAP.md' and add before the template: 'Do NOT overwrite the file. Update the `> **Current Milestone**:` line at the top and append the following as a new section, preserving all previous milestone sections.'
[MEDIUM] wf-plan mandates '2-3 tasks max. No exceptions' but the delegated writing-plans skill sets no limit and SDD's example plan has 5 tasks | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:43-44 (vs writing-plans/SKILL.md:36-43; subagent-driven-development/SKILL.md:162)
  NOW: wf-plan's philosophy: 'Each plan: **2-3 tasks max**. No exceptions.' writing-plans — which wf-plan tells the model to 'follow exactly' — defines task granularity (2-5 minute steps) but never caps task count, and SDD's canonical example reads a plan a…
  FIX: Add to writing-plans after 'Bite-Sized Task Granularity': '**Per-plan limit:** 2-3 tasks per plan. If the work needs more, split into multiple plans.' Add to wf-plan Step 5 checklist: '- [ ] No plan exceeds 3 tasks'. Fix the SDD example to a 3-task plan or annotate it as pre-split.
[MEDIUM] SDD's 'When to Use' diagram routes tightly-coupled tasks away from SDD, but wf-execute mandates SDD unconditionally | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/subagent-driven-development/SKILL.md:46-62 (vs wf-execute/SKILL.md:72-76)
  NOW: SDD's decision graph sends 'Tasks mostly independent?' = no to 'Manual execution or brainstorm first', and its frontmatter scopes it to 'plans with independent tasks'. wf-execute says methodology selection is automatic and absolute: if invoke_subagen…
  FIX: Add under 'When to Use': '> **When dispatched by `/execute`:** skip this decision tree — the workflow already chose SDD. Dependent/sequential tasks are supported because SDD executes one task at a time in plan order.'
[MEDIUM] executing-plans opens by telling the model to use SDD instead, with a blanket claim that subagents are natively supported | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/executing-plans/SKILL.md:14
  NOW: wf-execute routes to executing-plans precisely when `invoke_subagent` is NOT available. The first note in executing-plans then says 'When subagents are available (Antigravity 2.0 supports them natively), use subagent-driven-development instead of thi…
  FIX: Replace line 14 with: '**Note:** If the `invoke_subagent` tool is available in your current toolset, use subagent-driven-development instead. If `/execute` routed you here, that check already failed — proceed with this skill.'
[MEDIUM] requesting-code-review's mandatory reviews depend on invoke_subagent with no fallback for the IDE platform | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/requesting-code-review/SKILL.md:14-16, 32-34
  NOW: Reviews are 'Mandatory: After each task in subagent-driven development; After completing major feature; Before merge to main', and the only described mechanism is 'Use `invoke_subagent` with the code reviewer prompt template'. On the IDE (no invoke_s…
  FIX: Add after step 2 in 'How to Request': '**If `invoke_subagent` is NOT available** (IDE): run the review inline — read `code-reviewer.md`, generate the diff (`git diff BASE_SHA..HEAD_SHA`), and evaluate it yourself against the template, reporting Strengths/Issues/Assessment in the same format.'
[MEDIUM] dispatching-parallel-agents' trigger description ('2+ independent tasks') claims the same territory SDD forbids parallelism in | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/dispatching-parallel-agents/SKILL.md:3 (vs subagent-driven-development/SKILL.md:269)
  NOW: The frontmatter description is 'Use when facing 2+ independent tasks that can be worked on without shared state or sequential dependencies' — which literally describes most implementation plans (wave-1 plans are by definition independent). using-quan…
  FIX: Change frontmatter description to: 'Use when investigating 2+ unrelated failures or research questions in independent domains. NOT for implementation plan tasks — those run sequentially via subagent-driven-development.'
[MEDIUM] CONSTITUTION ships unfilled [FILL] placeholders that are 'loaded on every run', plus Article 5 contradicts its own fill option | /Users/piemelel/GitHub/Coding-Workflow/.agents/rules/CONSTITUTION.md:24, 33-34, 51-55
  NOW: The header says the file is 'Loaded by `/plan` and `/execute` on every run. Violations must be justified.' — yet Articles 2/3/5/6/7/8/9 contain literal '[FILL: ...]' choices (e.g. '**Strategy**: [FILL: fail-fast / graceful degradation / hybrid]'). A …
  FIX: Add to the header: '> If a [FILL] is still present, treat the FIRST listed option as the active default.' Change Article 5's last bullet to: '- **File Creation Order** (applies when Test-First = mandatory TDD): Contracts → Tests → Source.'
[LOW] wf-plan Step 2 bash uses $PHASE before anything assigns it, and contains a no-op `tr -d ''` | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:59-64
  NOW: The snippet greps `"Phase $PHASE"` but no prior instruction sets the PHASE variable (the prose says 'Extract phase number' without connecting it to the variable), and `tr -d ''` deletes nothing. A model pasting this verbatim greps for 'Phase ' (empty…
  FIX: ```bash PHASE="$1" # parsed from $ARGUMENTS or auto-detected (see above) PHASE_TITLE=$(grep -iE "Phase ${PHASE}[: ]" .quantis/ROADMAP.md | head -n 1 | sed -E 's/.*Phase [0-9.]+:? (.*)/\1/') ``` and delete `| tr -d ''`.
[LOW] brainstorming points to a wrong relative path for the visual companion guide | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/brainstorming/SKILL.md:163-164
  NOW: The skill says to read `skills/brainstorming/visual-companion.md`. The actual file is at `.agents/skills/brainstorming/visual-companion.md` (verified). A model resolving the given path from repo root finds nothing; it must guess that 'skills/' means …
  FIX: - `skills/brainstorming/visual-companion.md` + `.agents/skills/brainstorming/visual-companion.md`
[LOW] Unexplained '// turbo' annotation in wf-verify | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:108-112
  NOW: Step 3b contains a bare line '// turbo' above the example bash block. No rule file among the rules/skills explains what 'turbo' means (it is presumably an Antigravity auto-run annotation). A model reading the skill must decide: is this a directive it…
  FIX: Either delete the line or add a comment: '<!-- `// turbo` marks a command the Antigravity IDE may auto-run without confirmation; ignore on other platforms. -->'
[LOW] context-health-monitor references 'Rule 3 in GEMINI.md' — a file/rule not discoverable from the rules directory | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/context-health-monitor/SKILL.md:100-106
  NOW: The Integration section says the skill integrates with 'Rule 3 in `GEMINI.md` — Context Hygiene enforcement'. GEMINI.md is not in `.agents/rules/` (the canonical rules location per PROJECT_RULES); QUANTIS-STYLE mentions `.gemini/GEMINI.md` but no aud…
  FIX: - Rule 3 in `GEMINI.md` — Context Hygiene enforcement + `.agents/rules/PROJECT_RULES.md` § Context Management — Context Hygiene rules
[LOW] wf-stress-test's next step '/plan {N}' has no source for N | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-stress-test/SKILL.md:109-118
  NOW: The offer_next banner ends '▶ Fix critical issues, then /plan {N}'. wf-stress-test's argument is a target file, not a phase; nothing in the workflow determines which phase number to substitute. The model must guess (next unplanned phase? phase relate…
  FIX: - ▶ Fix critical issues, then /plan {N} + ▶ Fix critical issues, then /plan (auto-detects the next phase)
[LOW] wf-complete-milestone Step 5c orders an update to STACK.md, a file defined nowhere, and uses gap-toothed step numbering (5, 5c, 5d) | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-complete-milestone/SKILL.md:111-118
  NOW: Step 5c item 3 says 'Update STACK.md — refresh technology and dependency information'. STACK.md appears in no other audited file, no path is given (.quantis/STACK.md? root?), and nothing creates it. Two readings: (a) create it from scratch at a guess…
  FIX: - 3. **Update STACK.md** — refresh technology and dependency information + 3. **Update `.quantis/STACK.md`** (if it exists) — refresh technology and dependency information; skip if the project doesn't maintain one Renumber 5c→6, 5d→7, shifting later steps.
[LOW] PROJECT_RULES contains an empty 'Adapter Pattern' code block and lists deleted directories in Repository Structure | /Users/piemelel/GitHub/Coding-Workflow/.agents/rules/PROJECT_RULES.md:122-128, 170-178
  NOW: 'Adapter Pattern:' is followed by a code block containing only 'adapters/' — the pattern it's meant to illustrate is gone (dead adapters were removed in Phase 3.2), yet the following paragraph still mandates 'Each adapter must begin with...'. Reposit…
  FIX: Remove the empty code block (or list the one remaining adapter, `adapters/ANTIGRAVITY.md`), and delete the '└── examples/' line from Repository Structure.
[LOW] QUANTIS-STYLE has an orphaned sentence where a deleted 'Docs' heading used to be | /Users/piemelel/GitHub/Coding-Workflow/.agents/rules/QUANTIS-STYLE.md:35-41
  NOW: After '### Templates ... Copy, don't reference.' there is a blank gap and then the bare line 'Read-only documentation and examples.' with no heading. It reads as if it modifies the Templates section — directly contradicting 'Copy, don't reference' (a…
  FIX: Delete the line 'Read-only documentation and examples.' and change line 3 to: '> **Comprehensive reference.** Core rules auto-load from `.agents/rules/PROJECT_RULES.md`.'
[LOW] brainstorming's 'You MUST create a task for each of these items' names no mechanism | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/brainstorming/SKILL.md:21-23
  NOW: 'You MUST create a task for each of these items and complete them in order' — 'create a task' is undefined. On platforms with a todo/task tool the model will use it; on platforms without one, two readings: (a) write a checklist in the reply, (b) writ…
  FIX: - You MUST create a task for each of these items and complete them in order: + You MUST track each of these items as a checklist (use your platform's task/todo tool if available, else keep the checklist visible in your messages) and complete them in order:
[LOW] SDD's Model Selection section gives no mechanism for choosing a model | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/subagent-driven-development/SKILL.md:117-131, 142-145
  NOW: The section instructs 'use a fast, cheap model' / 'use the most capable available model', and BLOCKED handling says 're-dispatch with a more capable model' — but the `define_subagent`/`invoke_subagent` pseudo-API shown at lines 34-40 has no model par…
  FIX: Add at the top of Model Selection: '> If your platform's `invoke_subagent` does not accept a model parameter, skip this section — dispatch with the default model. When plans carry an `effort` attribute (see QUANTIS-STYLE), map low→cheap, medium→standard, high/max→most capable.'

## missing-hard-gates (34)
[CRITICAL] Planning Lock is echo-only — nothing stops the model when SPEC.md is not FINALIZED | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:50-54
  NOW: The 'Planning Lock' (the system's flagship hard gate per PROJECT_RULES.md line 21) is implemented as a bash one-liner that merely echoes an error string. There is no instruction to stop, no exit 1, and no prose gate. A model runs the command, sees 'E…
  FIX: ```diff ## 1. Planning Lock ```bash -grep -q "FINALIZED" ".quantis/SPEC.md" || echo "Error: SPEC.md must be FINALIZED" +grep -q "FINALIZED" ".quantis/SPEC.md" || { echo "Error: SPEC.md must be FINALIZED"; exit 1; } test -f ".agents/rules/CONSTITUTION.md" || echo "⚠️ No .agents/rules/CONSTITUTION.md"…
[HIGH] wf-execute environment validation is echo-only with no STOP instruction | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:29-35
  NOW: Step 1 runs `test -f` checks that only echo error strings. No exit, no gate language. 'Validate phase exists in ROADMAP.md' also has no failure branch. A model can print the errors and continue executing a phase that was never planned. Notably, Step …
  FIX: ```diff test -f ".quantis/ROADMAP.md" || echo "Error: run /plan first" test -f ".quantis/STATE.md" || echo "Error: run /plan first" test -f ".agents/rules/CONSTITUTION.md" || echo "⚠️ No .agents/rules/CONSTITUTION.md — quality standards not enforced" ``` -Validate phase exists in ROADMAP.md. +**GATE…
[HIGH] wf-execute never routes to /verify — the VERIFY step of the core protocol is silently skippable | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:96-105, 116-124
  NOW: Step 6 marks the phase '✅ Complete' based only on the inline self-check in Step 5, and the offer_next banner routes straight to '/plan {N+1} or /execute {N+1}'. The independent auditor (/verify) is never suggested or required, so the VERIFY stage of …
  FIX: In Step 6 add: "Mark phase `✅ Complete` but do NOT write 'verified' to STATE.md — only /verify may do that." And change offer_next: ```diff Quantis ► PHASE {N} COMPLETE ✓ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ -▶ /plan {N+1} or /execute {N+1} +▶ /verify {N} — independent audit (run be…
[HIGH] Task commit is not gated on the task's <verify> command passing | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:77-78
  NOW: Step 4 lists 'run `<verify>` commands' and 'Commit per task' as sequential bullets with no conditional between them. There is no instruction for what to do when a `<verify>` fails — a model can run it, watch it fail, and still commit, technically fol…
  FIX: ```diff 3. Follow `<task>` blocks in order, run `<verify>` commands + **GATE:** Each task's `<verify>` must be RUN and PASS (read the actual output) before that task is committed. If it fails, fix it (invoke systematic-debugging after repeated failures). Never commit a task whose `<verify>` failed o…
[HIGH] PARTIAL verdict is defined but has no handling route in Step 5 | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:192, 216, 224-302
  NOW: The VERIFICATION.md template defines three verdicts ('verdict: PASS | FAIL | PARTIAL') but Step 5 'Handle Results' only has 'If PASS' and 'If FAIL' branches. A model landing on PARTIAL must improvise; the path of least resistance is treating it as PA…
  FIX: Add to Step 5 after the FAIL branch: ```markdown ### If PARTIAL (some must-haves unverified, e.g. pending browser evidence): Treat as NOT passed. Do NOT update STATE.md to "Complete and verified". - Create gap closure plans for every FAILED must-have - List UNVERIFIED must-haves and the exact eviden…
[HIGH] BROWSER-VERIFY.md handoff does not cap the verdict — visual must-haves can be marked PASS on curl alone | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:129-180
  NOW: When browser_subagent is unavailable, the workflow says to run CLI evidence and write a BROWSER-VERIFY.md handoff, then continues to Step 4 (write report) with no instruction about how pending browser must-haves affect the verdict. A model can record…
  FIX: Append after the display block (line 180): ```markdown **GATE:** Any must-have whose evidence is pending in BROWSER-VERIFY.md MUST be recorded as `UNVERIFIED` in VERIFICATION.md, and the overall verdict MUST be `PARTIAL` — never `PASS` — until the browser evidence is captured and added. ```
[HIGH] Plan Checker is non-blocking: commit happens regardless, and 'Max 3 iterations' has no exit path | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:166-181
  NOW: Step 5's checklist is descriptive ('For each plan, verify: [ ] ...') with no statement that Step 6 (commit) depends on it passing. 'Max 3 iterations' defines a ceiling but not what happens when it's reached — a model can hit 3 iterations with open is…
  FIX: ```diff **If issues found:** Fix and re-verify. Max 3 iterations. + +**GATE:** Do not proceed to Step 6 (commit) until every checklist item passes for every plan. If issues remain after 3 iterations, STOP — present the unresolved issues to the user and ask how to proceed. Never commit plans that fai…
[HIGH] wf-discuss-phase outputs (DECISIONS.md, STATE.md) are never verified or committed; completion banner has no precondition | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-discuss-phase/SKILL.md:51-73
  NOW: Steps 3-4 are two terse sentences ('Record outcomes in .quantis/DECISIONS.md', 'Update STATE.md. Suggest /plan {N}') with no verification the files were written, no commit step, and no self-check. The 'DISCUSSION COMPLETE ✓ / {X} decisions documented…
  FIX: ```diff ## 3. Document Decisions Record outcomes in `.quantis/DECISIONS.md` using format: ... + +**GATE:** Verify the new entries exist (`grep "D-" .quantis/DECISIONS.md`). If zero decisions were recorded, say so explicitly — do not display the "{X} decisions documented" banner. ## 4. Update State +…
[HIGH] Stress-test findings report is never written to a file — work product lost at session end | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-stress-test/SKILL.md:87-98, 104-106
  NOW: Step 4 'Generate Report' specifies a per-finding markdown format but no file destination, no save instruction, and no commit. Steps 5-6 only touch SPEC.md/DECISIONS.md/STATE.md. A model that prints findings in chat fully satisfies the instructions; t…
  FIX: ```diff ## 4. Generate Report +Write the full report to `.quantis/STRESS-TEST.md` (or next to the target file for non-default targets). **GATE:** verify with `test -f` before continuing — findings that exist only in chat are lost. ... ## 6. Offer Next Steps -Update STATE.md. +Update STATE.md and com…
[HIGH] wf-complete-milestone incomplete-phase check has no explicit STOP instruction | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-complete-milestone/SKILL.md:14-26
  NOW: Step 1 greps for incomplete phases and, if found, displays a warning banner ('⚠️ Cannot complete milestone'). But the process never says to halt — there is no 'STOP, do not proceed to Step 2'. A model can show the warning and continue to archive/rese…
  FIX: ```diff **If incomplete phases found:** ``` ⚠️ Cannot complete milestone — {N} phases incomplete Run /progress to see status. ``` + +**GATE:** STOP here. Do not proceed to Step 2 or any later step. Resume only after the user completes the remaining phases or explicitly removes them from the mileston…
[HIGH] Final milestone verification result does not gate archiving/tagging | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-complete-milestone/SKILL.md:30-36
  NOW: Step 2 says to verify all must-haves, capture evidence, and 'Create VERIFICATION.md if not exists' — but nothing routes on the result. A milestone whose final verification FAILS can still be summarized, archived, reset, committed, and git-tagged in S…
  FIX: Append to Step 2: ```markdown **GATE:** If any must-have FAILS verification, STOP. Do not generate the summary, archive, reset, or tag. Report the failures and route to `/execute {N} --gaps-only`, then re-run /complete-milestone. ```
[MEDIUM] wf-new-milestone SPEC check echoes to stderr but the process continues | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-new-milestone/SKILL.md:15-21
  NOW: Step 1's bash prints an error if SPEC.md is missing but has no exit and the prose has no stop instruction. A model proceeds to gather milestone info and overwrite ROADMAP.md for a project that has no spec, violating the Planning Lock philosophy witho…
  FIX: ```diff if [ ! -f ".quantis/SPEC.md" ]; then echo "Error: SPEC.md required. Run /new-project first." >&2 + exit 1 fi ``` + +**GATE:** If SPEC.md is missing, STOP. Do not gather milestone information or modify ROADMAP.md. ```
[MEDIUM] Subagent-produced PLAN.md files are never verified to exist on disk | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:137-152
  NOW: The subagent path says 'The subagent produces the PLAN.md file(s)' and the orchestrator 'Reviews the plan output' — review can be satisfied by reading the subagent's text response. There is no filesystem check, so a subagent that returns plan content…
  FIX: ```diff The subagent produces the PLAN.md file(s). The orchestrator then: -1. Reviews the plan output +1. **GATE:** Verifies files exist on disk: `ls "$PHASE_DIR"/*-PLAN.md`. If none found, re-dispatch the subagent — do not proceed. +2. Reviews the plan output -2. Checks for quality (task specificit…
[MEDIUM] 'Verify wave complete' is undefined — no concrete checks before opening the next wave | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:79-83
  NOW: The wave gate is a single bolded sentence with no definition of 'complete'. PROJECT_RULES.md lines 70-75 define a 4-step Wave Completion Protocol, but wf-execute doesn't reference it. SUMMARY.md creation (line 79) — the very artifact Step 2 uses to d…
  FIX: ```diff -**Verify wave complete** before proceeding to next wave. +**Wave completion GATE** (per PROJECT_RULES "Wave Completion Protocol") — before starting the next wave, confirm ALL of: +1. Every plan in the wave has a SUMMARY.md on disk: `ls "$PHASE_DIR"/*-SUMMARY.md` +2. Every task's `<verify>` …
[MEDIUM] Phase-goal check softened by 'if specified' — must-haves without commands get a free pass | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:85-94
  NOW: Step 5 item 3 says 'Run verification commands if specified'. For any must-have lacking a pre-written command, the instruction is satisfied by running nothing, letting the model judge PASS from reading code (or SUMMARY claims, despite item 2's warning…
  FIX: ```diff -3. Run verification commands if specified +3. Run a verification command for EVERY must-have. If none is specified, derive one (test run, curl, file check, behavior scenario). Never mark a must-have PASS without running something and reading its output. ```
[MEDIUM] wf-verify subagent path: 'checks that evidence is empirical' has no failure action | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:40-52
  NOW: The orchestrator is told to check 'that evidence is empirical (not claims)' but there is no instruction for what to do when it isn't. The easy path is to present the subagent's PASS/FAIL summary anyway — exactly the 'Trust agent report' anti-pattern …
  FIX: ```diff 2. Checks that evidence is empirical (not claims) + **GATE:** If any must-have's evidence is a claim rather than command output / screenshot path, re-dispatch the subagent for those must-haves. Never present a PASS verdict to the user without empirical evidence per must-have. 3. Presents res…
[MEDIUM] RESEARCH.md created in a directory that is never mkdir'd or verified | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:59-64, 70-79
  NOW: Step 2 computes $PHASE_DIR but never creates it; Step 3 says 'Create RESEARCH.md inside `$PHASE_DIR/`' with no mkdir and no post-write existence check. For a new phase the directory doesn't exist yet, and a failed/skipped write is invisible — nothing…
  FIX: ```diff -- Create RESEARCH.md inside `$PHASE_DIR/` +- Run `mkdir -p "$PHASE_DIR"`, then create RESEARCH.md inside it +- **Verify:** `test -f "$PHASE_DIR/RESEARCH.md" || echo "RESEARCH.md missing — STOP"` before proceeding to Step 3.5 ```
[MEDIUM] wf-pause has no roadmap sync gate and no verification that state writes landed before the 'State saved' banner | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-pause/SKILL.md:96-128
  NOW: Step 4 displays 'State saved to: .quantis/STATE.md / .quantis/JOURNAL.md' with no check that Steps 1-2 actually wrote the sections or that the Step 3 commit succeeded. There is also no check that ROADMAP.md statuses match reality before handoff — ROA…
  FIX: Insert a new step between 3 and 4: ```markdown ## 3.5 Pre-Handoff GATE 1. Roadmap sync: for each phase marked in-progress/complete in ROADMAP.md, confirm the matching `.quantis/phases/` artifacts (PLAN/SUMMARY/VERIFICATION) agree. Fix mismatches now — a stale roadmap poisons /resume-session. 2. Veri…
[MEDIUM] Proactive auto-save uses 'should' for a behavior the section itself calls a hard requirement | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-pause/SKILL.md:144-176
  NOW: The session-limit protection is framed with optional language: 'The agent should auto-save state BEFORE limits are hit.' The closing line says 'Never rely on the user' but the operative instruction is a 'should', which a model under pressure (precise…
  FIX: ```diff -**Solution:** The agent should auto-save state BEFORE limits are hit. +**Solution:** The agent MUST auto-save state BEFORE limits are hit. When any trigger in the table fires, writing the snapshot to `.quantis/STATE.md` is mandatory and comes BEFORE any message to the user. ```
[MEDIUM] context-health-monitor's core obligation is phrased as 'should self-monitor' | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/context-health-monitor/SKILL.md:11-21
  NOW: The skill's entire activation mechanism is 'The agent should self-monitor for these warning signs' — soft language for the one behavior the skill exists to enforce. The Behavior Rules below it are well-gated (STOP, Do NOT), but they only fire if moni…
  FIX: ```diff -The agent should self-monitor for these warning signs: +The agent MUST self-monitor for these warning signs, and MUST take the listed Action the moment a threshold is hit — these are not suggestions: ```
[MEDIUM] wf-resume-session: no gate for missing STATE.md and no wait-for-user on uncommitted changes | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-resume-session/SKILL.md:14-16, 74-82
  NOW: Step 1 assumes STATE.md exists with no failure branch. Step 4's uncommitted-changes warning ends with 'Review before proceeding.' — addressed to nobody, with no instruction to ask the user or wait. A model can print the warning and immediately contin…
  FIX: ```diff -Read `.quantis/STATE.md` completely. +Read `.quantis/STATE.md` completely. **If the file does not exist or has no "Current Position" section, STOP** — tell the user there is no saved session and suggest `/progress` or `/new-project`. ... These may be from the previous session. -Review befor…
[MEDIUM] writing-plans self-review is not blocking — Execution Handoff can begin without it | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/writing-plans/SKILL.md:122-134
  NOW: The Self-Review section says 'This is a checklist you run yourself' — purely descriptive. Nothing ties the Execution Handoff (line 134) to the self-review having happened, and no output is required, so a model can save the plan and jump straight to o…
  FIX: Append after line 132: ```markdown **GATE:** The Execution Handoff below is forbidden until all three checks have actually been run and fixes applied. State the results explicitly before offering execution: "Self-review: spec coverage ✓, placeholders ✓, type consistency ✓". ```
[MEDIUM] SDD: orchestrator trusts implementer's DONE report — no independent commit/diff check before review | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/subagent-driven-development/SKILL.md:134-136
  NOW: 'DONE: Proceed to spec compliance review.' — the orchestrator never verifies the implementer's claimed commits/tests exist. verification-before-completion (lines 49, 103-105) explicitly requires 'VCS diff shows changes' over 'Agent reports success', …
  FIX: ```diff -**DONE:** Proceed to spec compliance review. +**DONE:** Verify independently first — run `git log --oneline -5` and `git diff HEAD~1 --stat` to confirm the claimed commits and files exist (never trust agent success reports; see verification-before-completion). If the diff is empty or missin…
[MEDIUM] SDD inline mode (IDE): self-review stages have no required output, so they can be skipped invisibly | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/subagent-driven-development/SKILL.md:22-27
  NOW: Inline mode asks the model to 'preserve the two-stage review structure' by self-reviewing 'as if' it were each reviewer, but requires no artifact or written verdict. Since the same model just wrote the code, a silent mental 'looks fine' satisfies the…
  FIX: ```diff - Self-review against spec (as if you were the spec reviewer) - Self-review for code quality (as if you were the code quality reviewer) + - **GATE:** Each review stage MUST produce an explicit written verdict in your response (e.g. "Spec review: PASS — all requirements present, nothing extra…
[MEDIUM] executing-plans: 'Mark as completed' is not conditioned on the verifications passing | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/executing-plans/SKILL.md:26-31
  NOW: Step 2 lists 'Run verifications as specified' and 'Mark as completed' as unconditional sequential items. The 'When to Stop' section covers test failures generally, but the per-task loop itself never says the completion mark depends on the verificatio…
  FIX: ```diff 3. Run verifications as specified -4. Mark as completed +4. Mark as completed ONLY if every verification in step 3 ran and passed. If any failed, STOP (see "When to Stop and Ask for Help") — do not mark completed and do not start the next task. ```
[MEDIUM] CONSTITUTION [FILL:] placeholders are never gated — workflows only check the file exists | /Users/piemelel/GitHub/Coding-Workflow/.agents/rules/CONSTITUTION.md:5-6, 24, 33-34, 51-52, 63, 69, 82, 87
  NOW: The template says '⚠️ Fill this during /new-project' but nothing enforces it. wf-plan (line 53) and wf-execute (line 33) only run `test -f` on the file, so a constitution full of '[FILL: ...]' placeholders passes every check and is fed to planners/ex…
  FIX: Add to Governance section: 'A constitution containing `[FILL:` placeholders is NOT ratified.' And in wf-plan Step 1 / wf-execute Step 1 add: ```bash grep -q "\[FILL" .agents/rules/CONSTITUTION.md && echo "⚠️ CONSTITUTION.md contains unfilled [FILL:] placeholders — ask the user to ratify before relyi…
[MEDIUM] Stress-test spec update is optional-sounding ('if applicable') and critical findings never block /plan | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-stress-test/SKILL.md:100-102, 115-117
  NOW: Step 5 is headed 'Update Spec (if applicable)', letting the model decide nothing applies. The banner says 'Fix critical issues, then /plan {N}' but nothing records the critical findings in SPEC.md or blocks planning — and wf-plan's Planning Lock only…
  FIX: ```diff -## 5. Update Spec (if applicable) -Add unresolved questions to SPEC.md's `## Unresolved Questions` section. +## 5. Update Spec (mandatory when findings exist) +Add EVERY unresolved 🔴 Critical and 🟠 High finding to SPEC.md's `## Unresolved Questions` section. +**GATE:** If 🔴 Critical finding…
[MEDIUM] wf-complete-milestone archive uses destructive mv with no post-move verification | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-complete-milestone/SKILL.md:70-80
  NOW: Step 4 moves all phase artifacts (`mv .quantis/phases/* ".quantis/milestones/{name}/"`) with no check that the destination received them or that the move succeeded. If the mkdir or mv fails (e.g., unsubstituted {name}, glob no-match), the workflow pr…
  FIX: Append to Step 4: ```bash # GATE: verify archive succeeded before resetting anything ls ".quantis/milestones/{name}/" | head -5 [ -z "$(ls -A .quantis/phases 2>/dev/null)" ] || { echo "Archive incomplete — STOP, do not proceed to Step 5"; } ```
[LOW] writing-plans: plan save never verified on disk | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/writing-plans/SKILL.md:18-19, 136-138
  NOW: 'Save plans to: .quantis/phases/{N}.{M}-{slug}/{N}.{M}-PLAN.md' has no follow-up existence check, yet the handoff message asserts 'Plan complete and saved to <path>' — a success claim without verification, contradicting verification-before-completion…
  FIX: Add under Execution Handoff: ```markdown **Before sending the handoff message:** verify the plan exists — `ls .quantis/phases/{N}.{M}-{slug}/{N}.{M}-PLAN.md`. Never claim "saved to" without this check. ```
[LOW] SDD Quantis state integration is an afterthought section with no gate tying it to task completion | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/subagent-driven-development/SKILL.md:308-316
  NOW: 'After marking each task complete, update Quantis state files' sits at the bottom of the file, disconnected from the per-task loop. Combined with 'Continuous execution: Do not pause... Execute all tasks without stopping' (line 14), a model in flow wi…
  FIX: ```diff -After marking each task complete, update Quantis state files: +Marking a task complete REQUIRES updating Quantis state files in the same step — do not dispatch the next implementer until STATE.md reflects the completed task: ```
[LOW] requesting-code-review: no re-review loop after fixing Critical/Important issues | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/requesting-code-review/SKILL.md:42-46
  NOW: 'Act on feedback: Fix Critical issues immediately / Fix Important issues before proceeding' — there is no instruction to re-dispatch the reviewer after fixes. SDD has explicit re-review loops, but this skill is also used standalone ('Before merge to …
  FIX: ```diff **3. Act on feedback:** - Fix Critical issues immediately - Fix Important issues before proceeding +- After fixing Critical/Important issues, re-dispatch the reviewer on the new HEAD_SHA. Repeat until no Critical/Important issues remain — do not merge in between. - Note Minor issues for late…
[LOW] using-quantis state updates hinge on undefined 'significant work' with no gate | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/using-quantis/SKILL.md:96-100
  NOW: 'After completing significant work: 1. Update STATE.md...' — 'significant' is the model's judgment call and the list is descriptive, so the system's cross-session memory (which QUANTIS-STYLE calls 'STATE.md is memory') depends on an optional-sounding…
  FIX: ```diff -After completing significant work: +**GATE — before telling the user any task or phase is complete:** 1. **Update STATE.md** — current position and progress 2. **Add to JOURNAL.md** — session summary 3. **Update ROADMAP.md** — check off deliverables +A completion claim with stale STATE.md i…
[LOW] dispatching-parallel-agents: post-integration verification list is descriptive, not blocking | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/dispatching-parallel-agents/SKILL.md:167-173
  NOW: The Verification section lists 'Run full suite' as item 3 of an unnumbered advice list. Nothing forbids reporting success on the basis of the three agents' own summaries — the exact 'trust agent report' failure mode, multiplied by three agents.
  FIX: Append after item 4: ```markdown **GATE:** Do not report the work complete or integrate further changes until the FULL test suite has been run fresh in this session and passes (per verification-before-completion). Agent summaries are claims, not evidence. ```
[LOW] brainstorming User Review Gate message claims 'written and committed' without instructed verification | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/brainstorming/SKILL.md:126-131
  NOW: The template message asserts 'Spec written and committed to `<path>`' but the skill never instructs verifying the write or the commit (steps at lines 111-115 say 'save' and 'commit' with no check). A model can emit the template with the commit silent…
  FIX: Insert before the quoted message: ```markdown **Before sending this message, verify:** `git log -1 --stat` shows a commit containing the spec file. If not, write/commit it now — never state "written and committed" without having confirmed both. ```

## error-handling (27)
[CRITICAL] Planning Lock check prints an error but never halts the workflow | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:50-54
  NOW: Step 1 runs `grep -q "FINALIZED" ".quantis/SPEC.md" || echo "Error: SPEC.md must be FINALIZED"`. The `|| echo` makes the compound command exit 0, and the prose contains no instruction to stop. The very next step is '## 2. Parse Arguments', so a model…
  FIX: After the bash block add: +**If either check fails: STOP. Do not continue to Step 2.** +- SPEC.md missing → tell the user: "No SPEC.md found. Run /new-project first." and end the workflow. +- SPEC.md not FINALIZED → tell the user: "SPEC.md is not FINALIZED. Finish the spec (or run /stress-test) befo…
[CRITICAL] Same non-halting validation anti-pattern in /execute and /new-milestone | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:29-35 (and wf-new-milestone/SKILL.md lines 15-21)
  NOW: wf-execute step 1 runs `test -f ".quantis/ROADMAP.md" || echo "Error: run /plan first"` (and the same for STATE.md) but never says what to do when the message prints — the workflow flows straight into '## 2. Discover Plans'. wf-new-milestone step 1 h…
  FIX: In wf-execute after the bash block: +**If any `Error:` line printed: STOP.** Tell the user which file is missing and to run /plan (or /new-milestone) first. Do not continue to Step 2. In wf-new-milestone step 1, change the bash to `exit 1` inside the if-branch and add: +**If SPEC.md is missing: STOP…
[CRITICAL] /execute marks a never-planned phase as Complete when zero plans exist | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:48-50, 96-105
  NOW: Step 2 says 'Build list of incomplete plans (PLAN without matching SUMMARY). ... If no incomplete plans: phase already complete, skip to step 6.' If the phase directory exists but contains zero PLAN.md files (e.g., /discuss-phase via brainstorming wr…
  FIX: -If no incomplete plans: phase already complete, skip to step 6. +If zero PLAN.md files exist: STOP — "No plans found for phase {N}. Run /plan {N} first." +If all plans have matching SUMMARY.md: skip to Step 5 (Verify Phase Goal) — never mark a phase complete without verifying the goal.
[CRITICAL] /new-milestone destroys unarchived DECISIONS.md and JOURNAL.md content | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-new-milestone/SKILL.md:91-110
  NOW: Step 5b unconditionally truncates DECISIONS.md and JOURNAL.md to a bare header whenever they exceed 5 lines. The note admits archiving only happens 'If running /complete-milestone first' — but nothing checks whether /complete-milestone was actually r…
  FIX: Insert before the reset bash: +**Guard:** If ROADMAP.md shows the current milestone is not complete, or `.quantis/milestones/` has no archive of the current DECISIONS.md/JOURNAL.md, do NOT reset. Either (a) ask the user to run /complete-milestone first, or (b) on explicit confirmation, archive both …
[HIGH] /verify defines a PARTIAL verdict but Step 5 only handles PASS and FAIL | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:188-216 vs 224-302
  NOW: The VERIFICATION.md template (line 192) allows `verdict: PASS | FAIL | PARTIAL`, but '## 5. Handle Results' has exactly two branches: '### If PASS (all must-haves verified):' and '### If FAIL (some must-haves failed):'. A model that records PARTIAL (…
  FIX: Add to Step 5: +### If PARTIAL (some must-haves unverifiable now, none failed): +- Do NOT mark the phase verified in STATE.md; record 'Partially verified — {list} pending'. +- Create gap closure plans only for FAILed items. +- If items are pending browser verification, point the user to BROWSER-VERI…
[HIGH] Mid-execution failure semantics undefined: failed <verify>, failed plan, partial wave | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:67-83
  NOW: Step 4 says 'Follow `<task>` blocks in order, run `<verify>` commands' and 'Verify wave complete before proceeding to next wave', but never defines what to do when a `<verify>` command fails, when one plan in a wave fails while others succeed, or wha…
  FIX: Replace line 83 with: +**Wave gate:** A wave is complete only when every plan in it has a SUMMARY.md and all `<verify>` commands passed. +- `<verify>` fails → use `systematic-debugging`; do not move to the next task until it passes or the 3-strike rule fires. +- Plan blocked after 3 strikes → record…
[HIGH] /complete-milestone: incomplete-phase warning lacks an abort instruction and final-verification failure is unrouted | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-complete-milestone/SKILL.md:14-36
  NOW: Step 1 greps for incomplete phases and shows '⚠️ Cannot complete milestone — {N} phases incomplete' but never says to stop; steps 2-6 follow unconditionally, so a model may print the warning and then archive/tag anyway (step 4's `mv .quantis/phases/*…
  FIX: After the step-1 message add: +**STOP here.** Do not run steps 2-7 until all phases are complete. After step 2 add: +**If any must-have fails: STOP.** Report which failed, suggest `/verify {N}` / `/execute {N} --gaps-only`, and do not archive or tag. Archiving (step 4) is destructive — it must only …
[HIGH] /resume-session has no behavior for a missing, empty, or stale STATE.md | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-resume-session/SKILL.md:12-16
  NOW: Step 1 is simply 'Read `.quantis/STATE.md` completely.' If the file does not exist (fresh clone, /pause never run, session hard-terminated before auto-save) or contains only an auto-save stub, the entire display template in step 2 has nothing to fill…
  FIX: Add to Step 1: +**If STATE.md is missing or near-empty:** reconstruct context from (a) ROADMAP.md statuses, (b) `git log --oneline -15`, (c) PLAN.md files without SUMMARY.md. Announce: "No saved state found — reconstructed from roadmap and git history." +**If STATE.md and ROADMAP.md disagree** on th…
[HIGH] /plan phase resolution silently produces a malformed directory when the phase is not in ROADMAP.md | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:56-64
  NOW: Step 2 derives PHASE_TITLE via `grep -i "Phase $PHASE" .quantis/ROADMAP.md | head -n 1 | sed ...`. If ROADMAP.md is missing (never checked anywhere despite being listed as required at line 25) or the phase number doesn't exist, PHASE_TITLE and PHASE_…
  FIX: Append to the step-2 bash: +if [ ! -f .quantis/ROADMAP.md ]; then echo "Error: no ROADMAP.md — run /new-milestone first"; exit 1; fi +if [ -z "$PHASE_SLUG" ]; then echo "Error: Phase $PHASE not found in ROADMAP.md"; grep -iE '^#+ *Phase' .quantis/ROADMAP.md; exit 1; fi And in prose: **If either erro…
[MEDIUM] Plan Checker caps at 3 iterations with no instruction for what happens after the cap | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:166-174
  NOW: Step 5 says 'If issues found: Fix and re-verify. Max 3 iterations.' Nothing defines iteration 4: does the model ship the flawed plan, discard it, or escalate? A model hitting the cap will most likely silently proceed to Step 6 and commit known-defici…
  FIX: -**If issues found:** Fix and re-verify. Max 3 iterations. +**If issues found:** Fix and re-verify, max 3 iterations. +**After 3 iterations with unresolved issues:** STOP. List the remaining failures, do not commit, and ask the user: accept with documented caveats, narrow the phase scope, or return …
[MEDIUM] Subagent dispatch failure is undefined in every orchestrating workflow | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:137-152 (also wf-discuss-phase 22-32, wf-verify 40-52, wf-stress-test 26-38)
  NOW: All four workflows branch on 'If `invoke_subagent` is available' vs 'NOT available', but none defines what to do when the tool IS available and the dispatch fails: tool error, timeout, subagent returning empty/garbage output, or (in wf-verify line 49…
  FIX: Add to each Platform Check / delegation step: +**If the subagent dispatch fails or its output is unusable** (missing file, empty report, non-empirical evidence): re-dispatch once with explicit feedback on what was wrong. If it fails again, fall back to the inline procedure below and note the fallbac…
[MEDIUM] Gap-closure loop has no iteration cap or escalation (verify → gaps → execute → verify forever) | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:92-94 (and wf-verify/SKILL.md 254-302)
  NOW: On FAIL, /execute step 5 says 'Create gap closure plans, offer `/execute {N} --gaps-only`', and /verify likewise generates fix plans and offers the same command. If the gap fix itself fails verification, the cycle repeats indefinitely with no counter…
  FIX: Add to the FAIL route in both files: +**Gap-closure limit:** If the same must-have fails after 2 gap-closure rounds, do NOT create a third fix plan. Treat it as an architectural/spec problem: read `systematic-debugging` (Phase 4.5), document the history in STATE.md, and ask the user whether to revis…
[MEDIUM] Missing required phase argument is undefined for /execute and /verify | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:17 (and wf-verify/SKILL.md line 29)
  NOW: Both workflows declare '**Phase:** $ARGUMENTS (required — phase number to execute)' but say nothing about what to do when the user runs the bare command. The bash in step 2 then runs `find .quantis/phases -maxdepth 1 -name "${PHASE}-*"` with $PHASE e…
  FIX: Change the context line to: +**Phase:** $ARGUMENTS (if omitted: read the current phase from STATE.md; if STATE.md is ambiguous, list phases and ask the user — do not guess) And guard the bash: `if [ -z "$PHASE" ]; then echo "Error: no phase number"; exit 1; fi` before the find.
[MEDIUM] /verify on a never-executed phase (no SUMMARY.md files) is undefined | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:67-71
  NOW: Step 1 says to read 'All SUMMARY.md files from `"$PHASE_DIR/"`'. If the phase directory exists (plans written) but /execute was never run, there are zero SUMMARY files; the workflow proceeds to extract must-haves and verify a codebase where the work …
  FIX: Add after the directory check: +SUMMARIES=$(ls "$PHASE_DIR"/*-SUMMARY.md 2>/dev/null) +If no SUMMARY.md files exist but PLAN.md files do: STOP and report "Phase {N} not yet executed — run /execute {N}". Do not create gap closure plans for unexecuted work.
[MEDIUM] wf-discuss-phase and wf-stress-test list 'Required files' but define no missing-file behavior | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-discuss-phase/SKILL.md:13-18 (and wf-stress-test/SKILL.md lines 18-22)
  NOW: wf-discuss-phase requires ROADMAP.md, DECISIONS.md, and SPEC.md; wf-stress-test requires SPEC.md, ROADMAP.md, DECISIONS.md. Neither workflow checks they exist nor says what to do if they don't. On a project with no milestone yet, 'defaults to next un…
  FIX: Add to both <process> sections, step 1: +**Preflight:** If SPEC.md or ROADMAP.md is missing, STOP and route: "Run /new-project (no spec) or /new-milestone (no roadmap) first." If DECISIONS.md is missing, create it with a `# Decisions` header and continue.
[MEDIUM] SDD review loops are unbounded — 'Repeat until approved' with no escape hatch | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/subagent-driven-development/SKILL.md:284-288
  NOW: The Red Flags section mandates 'Implementer (same subagent) fixes them / Reviewer reviews again / Repeat until approved / Don't skip the re-review'. Combined with 'Continuous execution: Do not pause to check in' (line 14), a disagreeing reviewer/impl…
  FIX: +**Review loop limit:** If the same reviewer rejects 3 times on one task, STOP the loop. Treat it like BLOCKED: (1) check if the reviewer lacks context — provide it; (2) re-dispatch implementer with a more capable model; (3) if reviewer and plan genuinely conflict, escalate to the human. Never loop …
[MEDIUM] SDD final code review has no rejection branch — graph flows straight to finishing the branch | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/subagent-driven-development/SKILL.md:93-94, 112-113
  NOW: The process graph ends with '"Dispatch final code reviewer subagent for entire implementation" -> "Use finishing-a-development-branch skill"' — an unconditional edge with no decision diamond. Per-task reviews have explicit approve/fix loops, but if t…
  FIX: In the dot graph replace the unconditional edge with: +"Final reviewer approves?" [shape=diamond]; +"Dispatch final code reviewer..." -> "Final reviewer approves?"; +"Final reviewer approves?" -> "Use finishing-a-development-branch skill" [label="yes"]; +"Final reviewer approves?" -> "Dispatch fix s…
[MEDIUM] dispatching-parallel-agents assumes invoke_subagent exists and hand-waves conflict resolution | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/dispatching-parallel-agents/SKILL.md:68-74, 167-173
  NOW: The dispatch pattern calls `invoke_subagent(...)` three times with no platform check — unlike SDD, which explicitly branches on availability. On the IDE platform (no invoke_subagent) a model following this skill attempts a nonexistent tool with no de…
  FIX: +## Platform Detection +If `invoke_subagent` is NOT available, investigate the domains sequentially yourself, one domain at a time with the same focused scope. Do not attempt the tool. In Verification: -**Check for conflicts** - Did agents edit same code? +**Check for conflicts** — if two agents edi…
[MEDIUM] BROWSER-VERIFY.md handoff leaves the verification verdict undefined while evidence is pending | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:129-180
  NOW: When browser_subagent is unavailable, the workflow writes a BROWSER-VERIFY.md handoff for visual must-haves and tells the user to run it in the IDE later ('Save screenshots and recordings as evidence... Update VERIFICATION.md with browser evidence').…
  FIX: Add after the handoff display: +Must-haves awaiting browser evidence get **Status: PENDING (browser)** in VERIFICATION.md and force the overall verdict to PARTIAL. Do not apply the PASS branch of Step 5. After the user runs BROWSER-VERIFY.md in the IDE, re-run `/verify {N}` to finalize.
[MEDIUM] wf-discuss-phase and the brainstorming skill it delegates to have conflicting terminal states, leaving ambiguous workflow position | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-discuss-phase/SKILL.md:41-60 (vs brainstorming/SKILL.md lines 29-32, 66)
  NOW: wf-discuss-phase step 2 says 'Read and follow `.agents/skills/brainstorming/SKILL.md` in discuss/explore mode', then steps 3-4 expect the session to end by documenting decisions and suggesting `/plan {N}`. But brainstorming's mandatory checklist (ite…
  FIX: In wf-discuss-phase step 2: +Follow brainstorming checklist items 1-5 only (context, questions, approaches, design discussion). **Do NOT execute items 6-9** (spec writing and writing-plans handoff) — /plan owns those. The terminal state of this workflow is documented decisions + suggestion to run /p…
[MEDIUM] /plan --gaps mode is declared but its process is never specified, including the missing-VERIFICATION.md case | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:21, 66-68
  NOW: The flag table says '`--gaps` — Gap closure mode (reads VERIFICATION.md, skips research)', but the only process mention is 'If `--gaps` or `--skip-research`: Skip to step 4.' Nothing in step 4 tells the model to load VERIFICATION.md, extract failed m…
  FIX: Add a step 3b: +**If `--gaps`:** Read `$PHASE_DIR/VERIFICATION.md`. If missing, STOP: "No verification report — run /verify {N} first." Create one fix plan per FAILed must-have with `gap_closure: true` in frontmatter (required by /execute --gaps-only). Skip steps 3 and 3.5.
[MEDIUM] Git failures (nothing staged, hook rejection, conflicted/detached worktree) are unhandled in every workflow that commits | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:78, 102-105 (pattern repeats in wf-plan 178-181, wf-pause 98-101, wf-verify 308-311, wf-new-milestone 116-119, wf-complete-milestone 138-142)
  NOW: Every workflow ends with bare `git add ... && git commit -m ...` snippets. None addresses: commit failing due to a pre-commit hook, an in-progress merge/rebase conflict left from a previous session, detached HEAD, or nothing to commit. wf-resume-sess…
  FIX: Add to PROJECT_RULES.md Commit Conventions: +**Commit failure rule:** After every `git commit`, verify exit code 0 (e.g., `git log -1`). On failure: hook rejection → fix reported issues and recommit; merge/rebase in progress → STOP and ask the user before touching the conflict; nothing to commit → r…
[MEDIUM] Resume's uncommitted-changes handling is hand-waved: 'Review before proceeding' with no procedure | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-resume-session/SKILL.md:67-82
  NOW: Step 4 detects dirty files via `git status --porcelain` and prints 'These may be from the previous session. Review before proceeding.' — then step 5 marks the session active and step 6 suggests continuing execution. There is no procedure: no instruct…
  FIX: Replace the warning with: +1. Compare dirty files against STATE.md → 'In-Progress Work: Files modified'. +2. Files listed there: summarize `git diff` for each; offer (a) commit as `wip(phase-N): resume checkpoint`, (b) leave and continue the task. +3. Files NOT listed: show them and ask the user wha…
[MEDIUM] Ambiguous roadmap state (two phases in-progress, STATE/ROADMAP divergence) has no defined handling in /progress | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-progress/SKILL.md:22-30, 78-90
  NOW: Step 2 counts 'In progress (🔄)' phases and the step-4 table maps '| Phase in progress | `/execute {N}` to continue |' — assuming exactly one. If ROADMAP.md has two phases marked 🔄 (a realistic outcome given /execute can fail mid-phase, finding above,…
  FIX: Add rows to the step-4 table: +| Multiple phases 🔄 | Flag inconsistency: list them, ask user which is current, then fix ROADMAP.md statuses | +| STATE.md phase ≠ ROADMAP.md current phase | Show both values, ask user which is authoritative, sync the other |
[MEDIUM] Ecosystem discovery breaks when MANIFEST.md is absent — all 48 core skills are reported as user-installed | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:88-105
  NOW: Step 3.5 runs `if ! grep -q "^- $skill_dir$" MANIFEST.md` for every skill directory. If MANIFEST.md is missing from the project root (an installed end-user project rather than the Quantis repo itself), grep errors on every iteration and the `!` branc…
  FIX: Wrap the loop: +if [ ! -f MANIFEST.md ]; then + echo " - MANIFEST.md not found — skipping custom-skill detection" +else for skill_dir in $(ls .agents/skills/ 2>/dev/null); do ... done +fi
[LOW] Code-review example derives BASE_SHA from a grep that can silently return empty | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/requesting-code-review/SKILL.md:53-57 (and 27-30)
  NOW: The example computes `BASE_SHA=$(git log --oneline | grep "Task 1" | head -1 | awk '{print $1}')`. If no commit message contains 'Task 1' (commit format per QUANTIS-STYLE is `feat(phase-N): {task-name}`, not 'Task 1'), BASE_SHA is empty and the revie…
  FIX: +**Guard:** If BASE_SHA is empty or `git rev-parse` fails, do not dispatch. Fall back to `git merge-base HEAD origin/main` or the first commit of the current plan recorded in STATE.md, and verify both SHAs resolve (`git cat-file -t $SHA`) before filling the template.
[LOW] CONSTITUTION.md [FILL] placeholders have no runtime fallback when the user never customizes them | /Users/piemelel/GitHub/Coding-Workflow/.agents/rules/CONSTITUTION.md:1-7, 22-29
  NOW: The header says the file is 'Loaded by `/plan` and `/execute` on every run' and '⚠️ Fill this during `/new-project`', but several articles ship as raw placeholders, e.g. '**Strategy**: [FILL: fail-fast / graceful degradation / hybrid]'. If the user s…
  FIX: Add under Governance: +**Unfilled placeholders:** If an article still contains `[FILL: ...]` at plan/execute time, treat the FIRST listed option as the default (e.g., fail-fast, mandatory TDD) and note in the plan that the constitution default was assumed. Workflows must not stall on placeholders.

## dead-references (19)
[CRITICAL] wf-execute plan glob `*-PLAN.md` does not match actual on-disk plan files — phase would be marked 'already complete' | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:45-50
  NOW: Step 2 discovers plans with `ls "$PHASE_DIR"/*-PLAN.md` and states 'If no incomplete plans: phase already complete, skip to step 6.' The actual plan files on disk are .quantis/phases/3.1-workflow-reliability-fixes/3.1-PLAN-A.md and 3.1-PLAN-B.md, whi…
  FIX: In wf-execute/SKILL.md step 2: - ls "$PHASE_DIR"/*-PLAN.md 2>/dev/null + ls "$PHASE_DIR"/*PLAN*.md 2>/dev/null And in writing-plans/SKILL.md line 18, document the multi-plan case: - **Save plans to:** `.quantis/phases/{N}.{M}-{slug}/{N}.{M}-PLAN.md` + **Save plans to:** `.quantis/phases/{N}.{M}-{slu…
[HIGH] ROADMAP Phase 3.1 (Not Started) deliverables reference deleted .agent/workflows files | /Users/piemelel/GitHub/Coding-Workflow/.quantis/ROADMAP.md:185-188
  NOW: Phase 3.1 deliverables name `discuss-phase.md`, `execute.md`, `verify.md`, and `pause.md`. These were files in `.agent/workflows/`, which Phase 3.2 (line 207: 'Deleted `.agent/` directory entirely') removed. The workflows now live at `.agents/skills/…
  FIX: - [ ] `discuss-phase.md` — Enforce full brainstorming checklist (always produce SPEC.md) + [ ] `.agents/skills/wf-discuss-phase/SKILL.md` — Enforce full brainstorming checklist (always produce SPEC.md) - [ ] `execute.md` — Deterministically use SDD on Antigravity (no user menu) + [ ] `.agents/skills…
[MEDIUM] wf-verify routes to non-existent `/debug` command (actual command is `/debug-issue`) | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:348
  NOW: The Related Workflows table lists `/debug` for 'Diagnose verification failures'. No skill `wf-debug` exists; the actual skill is `wf-debug-issue` (frontmatter: name: wf-debug-issue) and using-quantis/SKILL.md line 115 lists the command as `/debug-iss…
  FIX: - | `/debug` | Diagnose verification failures | + | `/debug-issue` | Diagnose verification failures |
[MEDIUM] brainstorming references visual companion guide at wrong path `skills/brainstorming/visual-companion.md` | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/brainstorming/SKILL.md:163-164
  NOW: The skill instructs 'read the detailed guide before proceeding: `skills/brainstorming/visual-companion.md`'. From the repo root this path does not exist; the file actually lives at `.agents/skills/brainstorming/visual-companion.md` (verified on disk)…
  FIX: - `skills/brainstorming/visual-companion.md` + `.agents/skills/brainstorming/visual-companion.md`
[MEDIUM] Workflows dispatch undefined subagent types `self` and `research` | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:139 (also wf-stress-test/SKILL.md:28, wf-verify/SKILL.md:42, wf-discuss-phase/SKILL.md:24)
  NOW: Four workflows instruct 'dispatch a `self` subagent' (wf-plan, wf-stress-test, wf-verify) or 'dispatch a `research` subagent' (wf-discuss-phase). Grep across the repo shows these type names appear nowhere else: subagent-driven-development and adapter…
  FIX: - **If `invoke_subagent` is available**, dispatch a `self` subagent with: + **If `invoke_subagent` is available**, dispatch a subagent via `invoke_subagent` (no special type needed) with: Apply the same change in wf-stress-test:28, wf-verify:42; in wf-discuss-phase:24 replace '`research` subagent' w…
[MEDIUM] context-health-monitor cites 'Rule 3 in `GEMINI.md`' — no GEMINI.md at root, and no 'Rule 3' exists in .gemini/GEMINI.md | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/context-health-monitor/SKILL.md:105
  NOW: The Integration section says '- Rule 3 in `GEMINI.md` — Context Hygiene enforcement'. There is no GEMINI.md at repo root (verified: file is at `.gemini/GEMINI.md`), and that file contains no section called 'Rules' — it has 'Core Principles' where ite…
  FIX: - - Rule 3 in `GEMINI.md` — Context Hygiene enforcement + - Core Principle 3 ('Context Is Limited') in `.gemini/GEMINI.md` — context hygiene Alternatively point to the canonical rules: `.agents/rules/PROJECT_RULES.md` § Context Management.
[MEDIUM] PROJECT_RULES repository structure references deleted `.quantis/examples/` directory | /Users/piemelel/GitHub/Coding-Workflow/.agents/rules/PROJECT_RULES.md:175
  NOW: The Repository Structure tree lists `└── examples/ # Usage examples` under `.quantis/`. The directory does not exist (deleted in commit 346a5b3 'chore: delete .quantis/examples/'; verified: ls reports No such file or directory). This is the canonical…
  FIX: - ├── templates/ # Document templates - └── examples/ # Usage examples + ├── templates/ # Document templates + ├── phases/ # Per-phase specs, plans, verification + └── milestones/ # Archived milestone summaries
[MEDIUM] wf-plan MCP discovery hardcodes IDE-only host path `$HOME/.gemini/antigravity-ide/mcp` — always reports 'None connected' on CLI | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:110-126
  NOW: Step 3.5 discovers MCP servers by listing `MCP_DIR="$HOME/.gemini/antigravity-ide/mcp"`. Verified: this directory does not exist on this machine. The path is specific to the Antigravity IDE; on CLI (`agy`) and Standalone — the platforms wf-plan other…
  FIX: Replace the hardcoded path with a guarded multi-location probe, e.g.: - MCP_DIR="$HOME/.gemini/antigravity-ide/mcp" + # IDE stores MCP schemas here; CLI/Standalone expose MCP tools directly in the toolset + MCP_DIR="$HOME/.gemini/antigravity-ide/mcp" + # If the directory is absent (CLI/Standalone), …
[MEDIUM] dispatching-parallel-agents uses invoke_subagent unconditionally with no platform check, contradicting the platform matrix | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/dispatching-parallel-agents/SKILL.md:68-74
  NOW: The dispatch pattern is `invoke_subagent("Fix agent-tool-abort.test.ts failures")` etc., with no availability check and no fallback anywhere in the skill. Per the platform matrix (adapters/ANTIGRAVITY.md lines 17-18: invoke_subagent ❌ on IDE) the too…
  FIX: Add after the Overview: + ## Platform Detection + **If `invoke_subagent` is NOT available** (IDE), do not use this skill's parallel dispatch — investigate the domains sequentially yourself, one domain at a time with the same focused scope/constraints/output structure.
[MEDIUM] requesting-code-review instructs unconditional `invoke_subagent` with no IDE fallback | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/requesting-code-review/SKILL.md:32-34
  NOW: Step 2 says 'Use `invoke_subagent` with the code reviewer prompt template at `code-reviewer.md`' with no availability check. On IDE, invoke_subagent does not exist (platform matrix). SDD — which mandates this review after each task — has an inline se…
  FIX: + **If `invoke_subagent` is NOT available** (IDE): read `code-reviewer.md` and perform the review yourself against the diff between BASE_SHA and HEAD_SHA, reporting findings in the same Strengths/Issues/Assessment format.
[MEDIUM] using-quantis File Conventions table omits the root `.quantis/SPEC.md` that four workflows hard-require | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/using-quantis/SKILL.md:127-136
  NOW: The File Conventions table lists Specs only at `.quantis/phases/{N}.{M}-{slug}/SPEC.md`. But wf-plan (lines 24, 52) greps FINALIZED in `.quantis/SPEC.md`, and wf-discuss-phase (line 17), wf-stress-test (line 20), and wf-verify (line 32) all list `.qu…
  FIX: | What | Location | |------|----------| + | Project spec (Planning Lock) | `.quantis/SPEC.md` | - | Specs | `.quantis/phases/{N}.{M}-{slug}/SPEC.md` | + | Phase specs | `.quantis/phases/{N}.{M}-{slug}/SPEC.md` |
[MEDIUM] 25 templates in .quantis/templates/ are never referenced or copied by any audited workflow — formats are re-inlined and can drift | /Users/piemelel/GitHub/Coding-Workflow/.agents/rules/QUANTIS-STYLE.md:36-38
  NOW: QUANTIS-STYLE says templates are 'Reusable document structures. Copy, don't reference.' All 25 templates exist on disk (VERIFICATION.md, journal.md, state.md, milestone.md, roadmap.md, etc.), but grep across wf-verify, wf-plan, wf-execute, wf-pause, …
  FIX: In each document-producing workflow, replace the inline format block with a copy instruction, e.g. in wf-verify step 4: + Start from the template: `cp .quantis/templates/VERIFICATION.md "$PHASE_DIR/VERIFICATION.md"`, then fill it in. Repeat for journal (wf-pause), milestone summary (wf-complete-mile…
[LOW] brainstorming references plugin skill `elements-of-style:writing-clearly-and-concisely` that does not exist in the repo | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/brainstorming/SKILL.md:113
  NOW: 'Use elements-of-style:writing-clearly-and-concisely skill if available' — no skill of this name (or any `elements-of-style` namespace) exists in .agents/skills/ (verified against the 48-dir listing). It is a leftover from the upstream superpowers pl…
  FIX: - - Use elements-of-style:writing-clearly-and-concisely skill if available + - Write clearly and concisely (see `.agents/rules/QUANTIS-STYLE.md` § Language & Tone)
[LOW] brainstorming names non-existent skills `frontend-design` and `mcp-builder` | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/brainstorming/SKILL.md:66
  NOW: 'Do NOT invoke frontend-design, mcp-builder, or any other implementation skill' — neither skill exists anywhere in .agents/skills/ (verified). These are superpowers-upstream skill names. Harmless as negative references, but they imply to a model that…
  FIX: - **The terminal state is invoking writing-plans.** Do NOT invoke frontend-design, mcp-builder, or any other implementation skill. + **The terminal state is invoking writing-plans.** Do NOT invoke any implementation or execution skill (subagent-driven-development, executing-plans, etc.).
[LOW] requesting-code-review gives template path relative to wrong base in closing line | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/requesting-code-review/SKILL.md:103
  NOW: 'See template at: requesting-code-review/code-reviewer.md' resolves neither from the repo root (actual: .agents/skills/requesting-code-review/code-reviewer.md) nor from the skill's own directory (which would just be code-reviewer.md, as line 34 corre…
  FIX: - See template at: requesting-code-review/code-reviewer.md + See template at: `.agents/skills/requesting-code-review/code-reviewer.md`
[LOW] executing-plans claims 'Antigravity 2.0 supports them natively' for subagents, contradicting the IDE column of the platform matrix | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/executing-plans/SKILL.md:14
  NOW: 'When subagents are available (Antigravity 2.0 supports them natively), use subagent-driven-development instead of this skill.' Per the platform matrix (adapters/ANTIGRAVITY.md: invoke_subagent ❌ on IDE), the Antigravity IDE does not have subagents —…
  FIX: - **Note:** When subagents are available (Antigravity 2.0 supports them natively), use subagent-driven-development instead of this skill + **Note:** When `invoke_subagent` is available (CLI `agy`, Standalone — not IDE), use subagent-driven-development instead of this skill
[LOW] PROJECT_RULES repository structure tree shows PROJECT_RULES.md and QUANTIS-STYLE.md at repo root | /Users/piemelel/GitHub/Coding-Workflow/.agents/rules/PROJECT_RULES.md:158-161
  NOW: The tree places `PROJECT_RULES.md` and `QUANTIS-STYLE.md` at the top level of the repo, relying on trailing comments '(canonical rules, in .agents/rules/)' to correct the picture. Neither file exists at the repo root (verified: root contains only CHA…
  FIX: - PROJECT_RULES.md # ← This file (canonical rules, in .agents/rules/) - QUANTIS-STYLE.md # Style and conventions (in .agents/rules/) + .agents/ + ├── rules/ + │ ├── PROJECT_RULES.md # ← This file (canonical rules) + │ ├── QUANTIS-STYLE.md # Style and conventions + │ └── CONSTITUTION.md # Project qua…
[LOW] QUANTIS-STYLE has orphaned sentence from a deleted docs/ section | /Users/piemelel/GitHub/Coding-Workflow/.agents/rules/QUANTIS-STYLE.md:39-40
  NOW: After the Templates section there is a dangling line 'Read-only documentation and examples.' with no heading. This is the body of a former section describing the docs/ directory, whose heading was removed when docs/ was deleted (commit 4504699 'chore…
  FIX: - Reusable document structures. Copy, don't reference. - - - Read-only documentation and examples. + Reusable document structures. Copy, don't reference.
[LOW] requesting-code-review example uses plan filename `deployment-plan.md` that violates the {N}.{M}-PLAN.md convention | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/requesting-code-review/SKILL.md:60
  NOW: The example shows 'PLAN_OR_REQUIREMENTS: Task 2 from .quantis/phases/{N}.{M}-{slug}/deployment-plan.md'. Per writing-plans (line 18) and using-quantis (line 130), plan files are named `{N}.{M}-PLAN.md`; no workflow produces a file called deployment-p…
  FIX: - PLAN_OR_REQUIREMENTS: Task 2 from .quantis/phases/{N}.{M}-{slug}/deployment-plan.md + PLAN_OR_REQUIREMENTS: Task 2 from .quantis/phases/{N}.{M}-{slug}/{N}.{M}-PLAN.md

## contradictions (35)
[CRITICAL] Two incompatible plan formats: XML <task> blocks vs writing-plans checkbox tasks | .agents/skills/wf-execute/SKILL.md (also wf-plan, wf-verify, QUANTIS-STYLE.md vs writing-plans):wf-execute:77; wf-plan:168-171; wf-verify:275-282; QUANTIS-STYLE.md:67-79; writing-plans:65-104
  NOW: wf-execute step 4 says 'Follow <task> blocks in order, run <verify> commands'. wf-plan's Plan Checker validates '<verify> commands are executable' and '<done> criteria are measurable'. QUANTIS-STYLE.md defines the canonical task as XML (<task type="a…
  FIX: In wf-execute/SKILL.md line 77: - 3. Follow `<task>` blocks in order, run `<verify>` commands + 3. Follow the plan's `### Task N` sections in order, executing each `- [ ]` step and its Run/Expected verification In wf-plan/SKILL.md lines 168-171: - - [ ] `<verify>` commands are executable - - [ ] `<d…
[CRITICAL] SPEC.md lives in two contradictory locations: .quantis/SPEC.md vs .quantis/phases/{N}.{M}-{slug}/SPEC.md | .agents/skills/brainstorming/SKILL.md (vs wf-plan, wf-verify, wf-stress-test, PROJECT_RULES.md, using-quantis):brainstorming:29,111; using-quantis:130; wf-plan:24,52; wf-verify:32; wf-stress-test:16,20; PROJECT_RULES.md:15
  NOW: brainstorming instructs 'Write the validated design (spec) to `.quantis/phases/{N}.{M}-{slug}/SPEC.md`' and using-quantis's File Conventions table agrees ('Specs | `.quantis/phases/{N}.{M}-{slug}/SPEC.md`'). But wf-plan's Planning Lock greps `.quanti…
  FIX: Decide the convention, then either: (a) brainstorming:111 "- Write the validated design (spec) to `.quantis/phases/{N}.{M}-{slug}/SPEC.md`" → "`.quantis/SPEC.md`" and using-quantis:130 likewise; or (b) keep per-phase specs and change wf-plan:52 to `grep -q "FINALIZED" "$PHASE_DIR/SPEC.md"` plus upda…
[CRITICAL] writing-plans mandates an execution-mode menu that wf-execute and SDD explicitly forbid | .agents/skills/writing-plans/SKILL.md:writing-plans:134-153 vs wf-execute:72-76 and subagent-driven-development:28
  NOW: writing-plans' Execution Handoff says: 'After saving the plan, offer execution choice: ... **1. Subagent-Driven (recommended)** ... **2. Inline Execution** ... **Which approach?**' — an explicit user menu. wf-execute says the opposite: '**Select exec…
  FIX: Replace writing-plans:134-153 with: + ## Execution Handoff + After saving the plan, announce: "Plan complete and saved to `<path>`." + Then select execution methodology automatically (do NOT ask the user): + - If `invoke_subagent` is available: use subagent-driven-development + - Otherwise: use exec…
[CRITICAL] wf-discuss-phase ends with 'suggest /plan' while its delegated brainstorming skill mandates writing a SPEC and invoking writing-plans as ter… | .agents/skills/wf-discuss-phase/SKILL.md:wf-discuss-phase:42,51-60 vs brainstorming:26-32,66,133-136
  NOW: wf-discuss-phase step 2 says 'Read and follow `.agents/skills/brainstorming/SKILL.md`', then steps 3-4 say the outcome is decisions recorded in DECISIONS.md and 'Suggest `/plan {N}` or `/stress-test`.' But brainstorming declares a mandatory checklist…
  FIX: In wf-discuss-phase:42 add an explicit override: - **Read and follow `.agents/skills/brainstorming/SKILL.md`** in discuss/explore mode. + **Read and follow `.agents/skills/brainstorming/SKILL.md`** (checklist items 1-8). Override for this workflow: do NOT execute checklist item 9 (invoking writing-p…
[HIGH] Planning Lock (Status: FINALIZED) is bypassed by the brainstorming → writing-plans skill path | .agents/rules/PROJECT_RULES.md:PROJECT_RULES.md:21 vs brainstorming:127-136 and writing-plans (entire file, no lock check)
  NOW: PROJECT_RULES declares 'Planning Lock: No implementation code until SPEC.md contains "Status: FINALIZED"', and wf-plan enforces it (line 52). But brainstorming's flow goes: user approves spec → 'Invoke the writing-plans skill to create a detailed imp…
  FIX: In brainstorming, after the User Review Gate (line ~131) add: + Once the user approves, set `Status: FINALIZED` in the spec file and commit (this satisfies the Quantis Planning Lock). In writing-plans Overview add: + Before writing any plan, confirm the spec contains `Status: FINALIZED`; if not, sto…
[HIGH] PROJECT_RULES says waves 'Run in parallel'; wf-execute and SDD mandate strictly sequential execution | .agents/rules/PROJECT_RULES.md:PROJECT_RULES.md:62-68 vs wf-execute:81 and subagent-driven-development:269
  NOW: PROJECT_RULES Wave Execution table: 'Wave 1 | Foundation tasks, no dependencies | Run in parallel' (and Waves 2-3 'then parallel'). wf-execute's note says 'SDD executes tasks **sequentially** — one subagent per task, one at a time. It is not about pa…
  FIX: PROJECT_RULES.md:62-68: - | 1 | Foundation tasks, no dependencies | Run in parallel | + | 1 | Foundation tasks, no dependencies | Execute first (plans within a wave run sequentially) | - | 2 | Depends on Wave 1 | Wait for Wave 1, then parallel | + | 2 | Depends on Wave 1 | Wait for Wave 1 | Add: "Wa…
[HIGH] Conflicting 3-failure protocols: fresh session (context-health-monitor) vs in-session architecture discussion (systematic-debugging) | .agents/skills/systematic-debugging/SKILL.md:systematic-debugging:199-213 vs context-health-monitor:27-37, wf-execute:109-114, PROJECT_RULES.md:198,252
  NOW: On the third failed fix, context-health-monitor Rule 1 says 'STOP attempting fixes ... Recommend user start fresh session ... Do NOT continue with more attempts'; wf-execute says 'After 3 failed debugging attempts ... Recommend /pause for fresh sessi…
  FIX: In systematic-debugging:211 append: - **Discuss with your human partner before attempting more fixes** + **Discuss with your human partner before attempting more fixes.** First follow the context-health-monitor auto-save protocol (write the state dump to `.quantis/STATE.md`), then recommend continui…
[HIGH] wf-plan defines checkpoint:human-verify tasks; SDD forbids stopping for the human between tasks | .agents/skills/wf-plan/SKILL.md:wf-plan:185-191 and QUANTIS-STYLE.md:96-104 vs subagent-driven-development:14
  NOW: wf-plan's task_types table defines 'checkpoint:human-verify | Visual/functional verification needing user' and 'checkpoint:decision | Implementation choices needing user input'; QUANTIS-STYLE documents the same checkpoint structure. But the execution…
  FIX: subagent-driven-development:14: - The only reasons to stop are: BLOCKED status you cannot resolve, ambiguity that genuinely prevents progress, or all tasks complete. + The only reasons to stop are: BLOCKED status you cannot resolve, ambiguity that genuinely prevents progress, a task explicitly typed…
[HIGH] Conflicting terminal states: SDD/executing-plans end with finishing-a-development-branch; wf-execute ends with phase verification, state upd… | .agents/skills/wf-execute/SKILL.md:wf-execute:85-105,116-124 vs subagent-driven-development:94,113 and executing-plans:32-37
  NOW: SDD's process graph ends: 'Dispatch final code reviewer subagent for entire implementation' → 'Use finishing-a-development-branch skill' (a skill that verifies tests and presents merge options). executing-plans Step 3 likewise: 'REQUIRED SUB-SKILL: U…
  FIX: In wf-execute Step 4 add: + > **Override:** When following SDD or executing-plans inside this workflow, skip their terminal 'finishing-a-development-branch' / final-reviewer step. Control returns here after the plan's tasks complete; phase-level verification and completion are handled in Steps 5-6.
[HIGH] Phase directory naming contradicts itself: {N}-{slug} vs {N}.{M}-{slug} | .agents/skills/wf-plan/SKILL.md:wf-plan:63 vs wf-plan:164; wf-verify:34 vs wf-verify:177
  NOW: wf-plan Step 2 computes PHASE_DIR=".quantis/phases/${PHASE}-${PHASE_SLUG}" (single phase number prefix), but Step 4 says 'Output plans to `.quantis/phases/{N}.{M}-{slug}/`' (two-level prefix). wf-verify's context block requires '`.quantis/phases/{pha…
  FIX: wf-plan:63: - PHASE_DIR=".quantis/phases/${PHASE}-${PHASE_SLUG}" + # $PHASE is the full {N}.{M} number, e.g. 3.1 + PHASE_DIR=".quantis/phases/${PHASE}-${PHASE_SLUG}" # yields {N}.{M}-{slug} and make the grep on line 61 match 'Phase {N}.{M}' headings. wf-verify:177: - .quantis/phases/{N}-{slug}/BROWS…
[HIGH] wf-execute detects completion via per-plan SUMMARY matching but tells the executor to create one phase-level $PHASE-SUMMARY.md | .agents/skills/wf-execute/SKILL.md:wf-execute:45-48 vs wf-execute:79
  NOW: Step 2 builds 'incomplete plans (PLAN without matching SUMMARY)' from `ls "$PHASE_DIR"/*-PLAN.md` and `*-SUMMARY.md` — implying one SUMMARY per PLAN with matching names. Step 4.5 instead says 'After all tasks in plan: create `$PHASE-SUMMARY.md` insid…
  FIX: wf-execute:79: - 5. After all tasks in plan: create `$PHASE-SUMMARY.md` inside `$PHASE_DIR/` documenting what was done + 5. After all tasks in plan: create `{plan-name}-SUMMARY.md` next to the plan (e.g., `3.1-PLAN.md` → `3.1-SUMMARY.md`) documenting what was done
[HIGH] wf-execute marks phases '✅ Complete' in ROADMAP and routes to the next phase without ever suggesting /verify, contradicting wf-progress and … | .agents/skills/wf-execute/SKILL.md:wf-execute:96-105,116-124 vs wf-progress:84-86 and wf-verify:344-346
  NOW: wf-execute Step 6: '**ROADMAP.md:** Mark phase `✅ Complete`' then commits and offers '▶ /plan {N+1} or /execute {N+1}'. /verify is never offered. But wf-progress's recommendation table has a distinct state 'Phase done, not verified | `/verify {N}`', …
  FIX: wf-execute offer_next (lines 119-123): - ▶ /plan {N+1} or /execute {N+1} + ▶ /verify {N} — validate against spec with empirical evidence (recommended) + ▶ /plan {N+1} — continue to next phase
[HIGH] using-quantis internal contradiction: skills are 'not negotiable' vs 'user instructions always take precedence' | .agents/skills/using-quantis/SKILL.md:using-quantis:6-12 vs using-quantis:14-23,85-87
  NOW: The opening block says 'IF A SKILL APPLIES TO YOUR TASK, YOU DO NOT HAVE A CHOICE. YOU MUST USE IT. This is not negotiable. This is not optional. You cannot rationalize your way out of this.' Immediately below, Instruction Priority says '**user instr…
  FIX: using-quantis:9-11: - IF A SKILL APPLIES TO YOUR TASK, YOU DO NOT HAVE A CHOICE. YOU MUST USE IT. - This is not negotiable. This is not optional. You cannot rationalize your way out of this. + IF A SKILL APPLIES TO YOUR TASK, YOU MUST USE IT — unless the user has explicitly told you not to (see Inst…
[HIGH] Workflows and using-quantis reference brainstorming 'modes' (discuss/explore, critique, research) that do not exist in the brainstorming ski… | .agents/skills/wf-stress-test/SKILL.md:wf-stress-test:9,31,44; wf-discuss-phase:42; using-quantis:112-114 vs brainstorming:1-165 (no modes defined)
  NOW: wf-stress-test says 'Critique methodology is powered by `brainstorming`' and '**Read `.agents/skills/brainstorming/SKILL.md`** and adopt adversarial critique mode'; wf-discuss-phase says to follow brainstorming 'in discuss/explore mode'; using-quanti…
  FIX: wf-stress-test:44: - **Read `.agents/skills/brainstorming/SKILL.md`** and adopt adversarial critique mode. + Adopt an adversarial critique mindset. (Do NOT follow the brainstorming skill's design checklist here — this workflow defines its own 7-dimension methodology below.) Remove 'brainstorming ski…
[MEDIUM] SDD internal contradiction: 'Parallel-safe (subagents don't interfere)' vs 'Never dispatch multiple implementation subagents in parallel (co… | .agents/skills/subagent-driven-development/SKILL.md:subagent-driven-development:236 vs 269
  NOW: The Advantages section claims 'Parallel-safe (subagents don't interfere)' while the Red Flags section commands 'Never: ... Dispatch multiple implementation subagents in parallel (conflicts)'. The same file asserts subagents both do and don't interfer…
  FIX: subagent-driven-development:236: - - Parallel-safe (subagents don't interfere) + - Isolated context per subagent (no cross-task contamination); implementation subagents still run one at a time
[MEDIUM] Branch policy conflict: delegated skills require worktree isolation / forbid implementing on main; wf-execute commits directly with no branc… | .agents/skills/wf-execute/SKILL.md:wf-execute:27-105 (no branch/worktree step) vs subagent-driven-development:267,296-297 and executing-plans:63,67-69
  NOW: SDD Red Flags: 'Never: Start implementation on main/master branch without explicit user consent' and Integration: 'using-git-worktrees — Ensures isolated workspace (creates one or verifies existing)'; executing-plans repeats both. wf-execute, the orc…
  FIX: Add to wf-execute after Step 1: + ## 1.5 Branch Check + If on main/master, ask for explicit consent or create a phase branch (`git checkout -b phase-{N}`) before executing. This satisfies the SDD/executing-plans red flag 'never start implementation on main without consent'.
[MEDIUM] executing-plans is defined as a separate-session methodology but wf-execute uses it as the same-session inline fallback | .agents/skills/executing-plans/SKILL.md:executing-plans:3 and subagent-driven-development:59-60,64 vs wf-execute:75
  NOW: executing-plans' description: 'Use when you have a written implementation plan to execute in a separate session with review checkpoints'; SDD's decision graph routes to executing-plans on 'Stay in this session?' → 'no - parallel session'. wf-execute,…
  FIX: executing-plans:3: - description: Use when you have a written implementation plan to execute in a separate session with review checkpoints + description: Use when you have a written implementation plan to execute inline — in a separate session, or in the current session when invoke_subagent is unava…
[MEDIUM] JOURNAL.md update cadence differs between the two execution methodologies: per task (SDD) vs per session (executing-plans) | .agents/skills/executing-plans/SKILL.md:executing-plans:83-89 vs subagent-driven-development:310-314
  NOW: SDD's Quantis State Integration: 'After marking each task complete, update Quantis state files: ... 2. **JOURNAL.md** — Add entry: task name, files changed, verification result' (per task). executing-plans: '2. **JOURNAL.md** — Append session entry a…
  FIX: subagent-driven-development:312-313: - 2. **JOURNAL.md** — Add entry: task name, files changed, verification result + 2. **JOURNAL.md** — After all tasks in the plan are complete, append one session entry listing tasks, files changed, and verification results (matches executing-plans cadence)
[MEDIUM] Commit message format conflict: writing-plans templates omit the mandatory phase scope | .agents/skills/writing-plans/SKILL.md:writing-plans:100-103 vs PROJECT_RULES.md:149-152, QUANTIS-STYLE.md:222-226, wf-execute:78
  NOW: writing-plans' canonical task template ends with `git commit -m "feat: add specific feature"` — no scope. PROJECT_RULES Commit Conventions: 'Scope = phase number for phase work (e.g., `feat(phase-1): ...`)'; QUANTIS-STYLE: 'Scope is phase number for …
  FIX: writing-plans:102: - git commit -m "feat: add specific feature" + git commit -m "feat(phase-{N}): add specific feature" Add a note in Remember: 'Commit scope = phase number for phase work (Quantis convention).'
[MEDIUM] using-quantis command table says /execute uses executing-plans; wf-execute says SDD is the default | .agents/skills/using-quantis/SKILL.md:using-quantis:110 vs wf-execute:9,72-75
  NOW: using-quantis's Workflow Commands table: '| `/execute {N}` | Execute phase plans | executing-plans |'. wf-execute's header: 'Execution methodology is powered by `subagent-driven-development` (CLI-first default) or `executing-plans` (inline fallback)'…
  FIX: using-quantis:110: - | `/execute {N}` | Execute phase plans | executing-plans | + | `/execute {N}` | Execute phase plans | subagent-driven-development (default) / executing-plans (fallback) |
[MEDIUM] Gap closure plans are created in three places with two incompatible formats and no designated owner | .agents/skills/wf-verify/SKILL.md:wf-verify:254-282 vs wf-plan:21,68 and wf-execute:92-94; format vs writing-plans:47-61
  NOW: wf-verify FAIL path: 'For each failed must-have, create a fix plan' using an XML `<task type="auto">` template. wf-plan has a dedicated `--gaps` mode ('Gap closure mode (reads VERIFICATION.md, skips research)') that delegates to writing-plans (checkb…
  FIX: wf-verify:256-258: - **Create gap closure plans:** For each failed must-have, create a fix plan in `"$PHASE_DIR/"`: + **Route to gap planning:** Recommend `/plan {N} --gaps` — it reads VERIFICATION.md and creates gap-closure plans via writing-plans (with `gap_closure: true` frontmatter). Do not auth…
[MEDIUM] Two different files claim to be the auto-loaded canonical rules source: .agents/rules/PROJECT_RULES.md vs .gemini/GEMINI.md | .agents/rules/QUANTIS-STYLE.md:QUANTIS-STYLE.md:3 and context-health-monitor:105 vs PROJECT_RULES.md:3-7 and ROADMAP.md:208
  NOW: QUANTIS-STYLE opens with 'Core rules auto-load from `.gemini/GEMINI.md`.' and context-health-monitor's Integration cites 'Rule 3 in `GEMINI.md` — Context Hygiene enforcement'. PROJECT_RULES declares itself '**Single Source of Truth** for the Quantis …
  FIX: QUANTIS-STYLE.md:3: - > **Comprehensive reference.** Core rules auto-load from `.gemini/GEMINI.md`. ... + > **Comprehensive reference.** Canonical rules live in `.agents/rules/PROJECT_RULES.md` (auto-discovered on all platforms; `.gemini/GEMINI.md` is a Gemini-only adapter). ... context-health-monit…
[MEDIUM] Token-efficiency rules forbid full reads of large files; systematic-debugging and using-quantis mandate complete reads | .agents/rules/PROJECT_RULES.md:PROJECT_RULES.md:45-56,208-213,240 vs systematic-debugging:131-133,254 and using-quantis:36-44
  NOW: PROJECT_RULES: 'Before reading any file completely: 1. Search first', 'File >200 lines | Use outline, not full file', and anti-pattern 'Full file reads when snippets suffice'. systematic-debugging Phase 2: 'If implementing pattern, read reference imp…
  FIX: PROJECT_RULES.md after line 213 add: + **Exceptions:** Read files completely when a skill explicitly requires it — e.g., SKILL.md activation (using-quantis) and reference implementations during Pattern Analysis (systematic-debugging). Skill mandates override the >200-line outline rule.
[MEDIUM] wf-plan's description claims it decomposes requirements into ROADMAP phases, but its process requires phases to already exist | .agents/skills/wf-plan/SKILL.md:wf-plan:3 vs wf-plan:25,57
  NOW: Frontmatter: 'description: The Strategist — Decompose requirements into executable phases in ROADMAP.md'. The body contradicts this: Required files list '`.quantis/ROADMAP.md` — Must have phases defined' and Step 2 'auto-detect next unplanned phase f…
  FIX: wf-plan:3: - description: The Strategist — Decompose requirements into executable phases in ROADMAP.md + description: The Strategist — Create executable PLAN.md files for a phase already defined in ROADMAP.md (phases are created by /new-milestone)
[MEDIUM] ROADMAP.md internal contradictions: completed Phase 3.2 'Depends on' not-started Phase 3.1, and the same deliverable is both unchecked and c… | .quantis/ROADMAP.md:ROADMAP.md:179-187 vs 199-212
  NOW: Phase 3.1 'Workflow Reliability Fixes' is '**Status**: ⬜ Not Started' with deliverable '[ ] `execute.md` — Deterministically use SDD on Antigravity (no user menu)'. Phase 3.2 is '**Status**: ✅ Complete (2026-06-11)' and declares '**Depends on**: Phas…
  FIX: ROADMAP.md:187: - - [ ] `execute.md` — Deterministically use SDD on Antigravity (no user menu) + - [x] `execute.md` — Deterministically use SDD on Antigravity (no user menu) — delivered in Phase 3.2 and either mark Phase 3.1's remaining items explicitly or change Phase 3.2's '**Depends on**: Phase 3…
[LOW] Status emoji vocabulary mismatch: wf-progress defines ⏸️ as 'Blocked' while ROADMAP uses ⏸ for 'CLOSED/SUPERSEDED', and 🔄 'In progress' is n… | .agents/skills/wf-progress/SKILL.md:wf-progress:24-29 vs ROADMAP.md:20,32-38 and wf-execute:96-98, wf-new-milestone:68-72
  NOW: wf-progress counts phases as 'Completed (✅), In progress (🔄), Blocked (⏸️), Not started (⬜)'. The real ROADMAP uses ⏸ to mean 'CLOSED' ('Milestone: v2.1 ... ⏸ CLOSED') and 'SUPERSEDED' ('Phase 3 ... ⏸ SUPERSEDED by Superpowers TDD + SDD skills'), whi…
  FIX: wf-progress:24-29: align the legend with ROADMAP usage (⏸ = closed/superseded, add Blocked marker if needed). wf-execute Step 3/4: add 'Mark phase 🔄 In Progress in ROADMAP.md when execution begins' so the 🔄 state actually exists.
[LOW] wf-verify PASS banner routes to '/execute {N+1}' although plans for N+1 do not exist yet | .agents/skills/wf-verify/SKILL.md:wf-verify:247-249 vs wf-execute:38-44 and wf-plan offer_next:193-202
  NOW: On PASS, wf-verify outputs '▶ Next Up /execute {N+1} — proceed to next phase'. But wf-execute Step 2 hard-fails when the phase directory is missing ('Error: phase directory starting with ${PHASE}- not found') and plans only exist after /plan; wf-plan…
  FIX: wf-verify:249: - /execute {N+1} — proceed to next phase + /plan {N+1} — plan the next phase (or /execute {N+1} if plans already exist)
[LOW] Two different finding-severity taxonomies: Critical/High/Medium/Low (stress-test) vs Critical/Important/Minor (code review) | .agents/skills/wf-stress-test/SKILL.md:wf-stress-test:98 vs requesting-code-review:43-45 and subagent-driven-development:212-216
  NOW: wf-stress-test grades findings '`🔴 Critical` | `🟠 High` | `🟡 Medium` | `🔵 Low`'. requesting-code-review instructs 'Fix Critical issues immediately / Fix Important issues before proceeding / Note Minor issues for later', and SDD's example reviewer use…
  FIX: Standardize on one scale; e.g., requesting-code-review:43-45: - Fix Critical issues immediately / Fix Important issues before proceeding / Note Minor issues for later + Fix Critical issues immediately / Fix High issues before proceeding / Note Medium and Low issues for later (and update SDD's exampl…
[LOW] QUANTIS-STYLE bans sycophancy/celebration while wf-progress and wf-complete-milestone instruct the model to celebrate | .agents/rules/QUANTIS-STYLE.md:QUANTIS-STYLE.md:119-121 vs wf-progress:87 and wf-complete-milestone:146-151
  NOW: QUANTIS-STYLE: '### No Sycophancy ✅ "Phase complete." ❌ "Great job! Phase complete!"'. wf-progress's recommendation table says 'All phases complete | Celebrate! 🎉' and wf-complete-milestone has a step literally titled '## 7. Celebrate' with banner 'M…
  FIX: wf-progress:87: - | All phases complete | Celebrate! 🎉 | + | All phases complete | `/complete-milestone` | wf-complete-milestone:146: rename '## 7. Celebrate' to '## 7. Display Completion'. Alternatively, add a scoping note to QUANTIS-STYLE's No Sycophancy section permitting the milestone banner emo…
[LOW] Auto-save context thresholds disagree: wf-pause snapshots at ~50-70% while PROJECT_RULES requires the state dump only at 70%+ | .agents/skills/wf-pause/SKILL.md:wf-pause:153-157 vs PROJECT_RULES.md:217-221
  NOW: wf-pause's Proactive Auto-Save table: 'Context usage reaches ~50-70% | Write lightweight state snapshot to `.quantis/STATE.md`'. PROJECT_RULES Budget Thresholds: '50-70% | Switch to outline mode, compress context' and '70%+ | State dump required, rec…
  FIX: PROJECT_RULES.md:220: - | 50-70% | Switch to outline mode, compress context | + | 50-70% | Switch to outline mode, compress context, write lightweight STATE.md snapshot (see wf-pause Proactive Auto-Save) |
[LOW] Undefined subagent types: workflows dispatch 'research' and 'self' subagents that no skill defines, while SDD defines a different named set | .agents/skills/wf-discuss-phase/SKILL.md:wf-discuss-phase:24; wf-plan:139; wf-verify:42; wf-stress-test:28 vs subagent-driven-development:32-40
  NOW: wf-discuss-phase says 'dispatch a `research` subagent'; wf-plan, wf-verify, and wf-stress-test say 'dispatch a `self` subagent'. SDD is the only file that defines subagent types, and its set is different: define_subagent("implementer"/"spec-reviewer"…
  FIX: In wf-discuss-phase:24, wf-plan:139, wf-verify:42, wf-stress-test:28, replace the undefined type names: - dispatch a `research` subagent / dispatch a `self` subagent + dispatch a subagent (via `invoke_subagent`) with the following instructions and context: or add a shared 'Quantis subagent roles' re…
[LOW] PROJECT_RULES requires a screenshot for every UI change; wf-verify accepts 'user confirmation' or curl/test output without one | .agents/rules/PROJECT_RULES.md:PROJECT_RULES.md:31-39 vs wf-verify:96-97,334
  NOW: PROJECT_RULES Proof Requirements: '| UI change | Screenshot |' with 'Always require: Captured output, screenshot, or test result.' wf-verify's method table says 'UI (without `browser_subagent`) | CLI verification: curl, test output, or user confirmat…
  FIX: PROJECT_RULES.md:33: - | UI change | Screenshot | + | UI change | Screenshot (via browser_subagent; on CLI, curl/test output + BROWSER-VERIFY.md handoff — must-have stays PARTIAL until the screenshot exists) | And in wf-verify:97 remove 'or user confirmation' or qualify it as recording the user's ex…
[LOW] PROJECT_RULES 'Commit all wave work' at wave completion conflicts with the one-task-one-commit rule | .agents/rules/PROJECT_RULES.md:PROJECT_RULES.md:70-74 vs PROJECT_RULES.md:150, QUANTIS-STYLE.md:224, wf-execute:78
  NOW: Wave Completion Protocol step 3 says 'Commit all wave work' — readable as a single wave-level commit. The same file's Commit Conventions say 'One task = one commit', QUANTIS-STYLE repeats 'One task = one commit', and wf-execute commits per task durin…
  FIX: PROJECT_RULES.md:73: - 3. Commit all wave work + 3. Confirm every task in the wave has its own commit (one task = one commit); commit only remaining state-file updates
[LOW] PROJECT_RULES Repository Structure documents a .quantis/examples/ directory that no longer exists | .agents/rules/PROJECT_RULES.md:PROJECT_RULES.md:170-175
  NOW: The Repository Structure diagram lists '.quantis/ ... └── examples/ # Usage examples'. The directory was deleted (commit 4504699-era cleanup 'chore: delete .quantis/examples/'); a live `ls .quantis/` shows no examples/ folder. The canonical 'Single S…
  FIX: PROJECT_RULES.md:175: - └── examples/ # Usage examples + └── milestones/ # Archived milestone summaries (or simply delete the examples/ line).
[LOW] wf-plan's 'Each plan: 2-3 tasks max. No exceptions.' conflicts with SDD's canonical example of a 5-task plan | .agents/skills/wf-plan/SKILL.md:wf-plan:43-44 vs subagent-driven-development:161-162
  NOW: wf-plan's philosophy section declares 'Aggressive Atomicity — Each plan: **2-3 tasks max**. No exceptions.' The execution methodology's worked example reads '[Read plan file once: .quantis/phases/{N}.{M}-{slug}/{N}.{M}-PLAN.md] [Extract all 5 tasks w…
  FIX: Add to writing-plans' Bite-Sized Task Granularity section: '+ Quantis plans target 2-3 tasks per plan; split larger scopes into additional plans/waves.' And update SDD:162: - [Extract all 5 tasks with full text and context] + [Extract all 3 tasks with full text and context]

## subagent-dispatch (21)
[CRITICAL] PROJECT_RULES mandates parallel wave execution but every execution path is sequential and SDD forbids parallel dispatch | /Users/piemelel/GitHub/Coding-Workflow/.agents/rules/PROJECT_RULES.md:62-68 (also wf-execute/SKILL.md:69, subagent-driven-development/SKILL.md:269)
  NOW: PROJECT_RULES.md (the self-declared 'Single Source of Truth') says Wave 1/2/3 plans 'Run in parallel'. But wf-execute Step 4 iterates 'For each wave in order, for each plan in the wave' sequentially, SDD's Red Flags say 'Never: Dispatch multiple impl…
  FIX: In PROJECT_RULES.md Wave Execution table, change 'Run in parallel' to: - | 1 | Foundation tasks, no dependencies | Sequential by default; parallel dispatch only when `invoke_subagent` is available AND plans touch disjoint files (see `dispatching-parallel-agents`) | And in wf-execute Step 4 add: 'Pla…
[CRITICAL] wf-verify Step 0 subagent path orphans Steps 5-6: gap closure plans, state update, and commit are never assigned | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:40-52, 224-311
  NOW: Step 0 says: subagent 'verifies each must-have and produces VERIFICATION.md', orchestrator then does exactly 3 things (review results, check evidence is empirical, present PASS/FAIL summary). The fallback line 'If `invoke_subagent` is NOT available, …
  FIX: In wf-verify Step 0, replace the 3-item orchestrator list with: 'The orchestrator then: 1. Reviews the verification results 2. Checks that evidence is empirical (not claims) — spot-check at least one PASS by re-running its verification command 3. **Proceeds to Step 5 (Handle Results) and Step 6 (Com…
[HIGH] wf-verify Step 0 lives inside an unclosed <context> block, outside <process> | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:28, 40, 54 (no </context> anywhere in file)
  NOW: `<context>` opens at line 28 and is never closed; `## 0. Platform Check` (line 40) sits between the context metadata and `<process>` (line 54). In wf-discuss-phase (line 22) and wf-stress-test (line 26), Step 0 is inside `<process>`. A model that tre…
  FIX: diff: @@ line 38 @@ > Before starting verification, read and follow the `verification-before-completion` skill for the full methodology. +</context> + +<process> ## 0. Platform Check ... @@ line 54 @@ -<process>
[HIGH] wf-verify Step 0 dispatch prompt omits the VERIFICATION.md format, evidence rules, and BROWSER-VERIFY.md handoff the subagent must produce | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:42-46 (vs 93-101, 129-180, 186-220)
  NOW: The dispatch gives the subagent only: 'The SPEC.md must-haves list / The verification-before-completion skill instructions / The phase directory and codebase access'. But the required VERIFICATION.md frontmatter (`verdict: PASS | FAIL | PARTIAL`, Ste…
  FIX: Rewrite Step 0 dispatch list: '**If `invoke_subagent` is available**, dispatch a `self` subagent with: - The must-haves extracted from the phase definition in ROADMAP.md (Step 2 format) plus relevant SPEC.md requirements - The verification-before-completion skill instructions - The verification meth…
[HIGH] wf-stress-test Step 0 subagent path drops Steps 5-6 (SPEC.md updates, DECISIONS.md, STATE.md) and gives the subagent no findings format | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-stress-test/SKILL.md:26-38 (vs 87-106)
  NOW: Step 0 ends the subagent path at 'Presents findings to the user with recommended actions', while 'If `invoke_subagent` is NOT available... (proceed to Step 1)' frames Steps 1-6 as inline-only. Step 5 (add unresolved questions to SPEC.md, document dec…
  FIX: In Step 0: add '- The report format from Step 4 (severity scale 🔴/🟠/🟡/🔵, Dimension/Problem/Impact/Suggestion per finding)' to the dispatch list, and change orchestrator item 3 to '3. Presents findings to the user with recommended actions, then **continues at Step 5** (update SPEC.md/DECISIONS.md) an…
[HIGH] wf-plan subagent dispatch omits the wave/gap_closure YAML frontmatter contract that wf-execute requires and the orchestrator reviews against | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:139-151 (vs wf-execute/SKILL.md:49,53; writing-plans/SKILL.md:47-61)
  NOW: Step 4 dispatches a `self` subagent with SPEC.md, the writing-plans skill, phase dir, CONSTITUTION, RESEARCH, ARCHITECTURE, and ecosystem context — then the orchestrator 'Checks for quality (task specificity, verification commands, wave structure)'. …
  FIX: Add to the Step 4 dispatch list: '- The required plan frontmatter schema: ```---\nphase: {N}\nplan: {slug}\nwave: {1|2|3}\ngap_closure: false\n---``` (wf-execute groups plans by the `wave` field)\n- Output naming: `.quantis/phases/{N}.{M}-{slug}/{N}.{M}-PLAN.md`' And in Step 5 add: 'If issues found:…
[HIGH] writing-plans ends with a user-facing execution menu that wf-execute explicitly forbids | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/writing-plans/SKILL.md:134-153 (vs wf-execute/SKILL.md:72-76)
  NOW: The Execution Handoff section instructs: offer 'Two execution options: 1. Subagent-Driven (recommended)... 2. Inline Execution... Which approach?' But wf-execute Step 4 says selection is 'automatic — do NOT ask the user' and '**This is NOT a choice. …
  FIX: Replace the Execution Handoff section with: '## Execution Handoff\nAfter saving the plan, do NOT ask the user to choose an execution mode. Announce: "Plan complete and saved to `<path>`. Run `/execute {N}` to implement it." Execution methodology is selected automatically: subagent-driven-development…
[HIGH] dispatching-parallel-agents/SKILL.md is referenced by zero workflows or methodology skills | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/dispatching-parallel-agents/SKILL.md:1-183 (whole file; repo-wide grep)
  NOW: Repo-wide grep shows the skill is referenced only in MANIFEST.md:47, README.md:46, and archived v3.0/v3.1 milestone plans — no wf-* workflow or methodology SKILL.md ever points to it. Its natural integration points all exist and are silent: wf-execut…
  FIX: Add to wf-debug-issue <related> Skills table: '| `dispatching-parallel-agents` | Parallel investigation when failures are independent |' and in its Step 3 add: 'If the issue spans 2+ independent failure domains and `invoke_subagent` is available, read `.agents/skills/dispatching-parallel-agents/SKIL…
[HIGH] Orphaned reviewer dispatch templates contradict their parent skills' explicit 'not a subagent dispatch' instruction | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/writing-plans/plan-document-reviewer-prompt.md:1-9 (also brainstorming/spec-document-reviewer-prompt.md:1-7; writing-plans/SKILL.md:124; brainstorming/SKILL.md:117-124)
  NOW: Two full invoke_subagent prompt templates exist — plan-document-reviewer-prompt.md ('Use this template when dispatching a plan document reviewer subagent') and spec-document-reviewer-prompt.md — but the parent skills never reference them and actively…
  FIX: In writing-plans Self-Review, replace 'This is a checklist you run yourself — not a subagent dispatch.' with: 'If `invoke_subagent` is available, dispatch a reviewer subagent using `./plan-document-reviewer-prompt.md` (fill [PLAN_FILE_PATH] and [SPEC_FILE_PATH]) and fix any Issues Found. If NOT avai…
[HIGH] Platform-detection logic is written five different ways across workflows; only two of five forbid asking the user | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:137-153 (also wf-discuss-phase:22-32, wf-stress-test:26-38, wf-verify:40-52, wf-execute:72-76, subagent-driven-development:16-28)
  NOW: Five different formulations of the same check: wf-discuss-phase and wf-stress-test use '## 0. Platform Check' inside <process>; wf-verify puts it inside <context>; wf-plan has no Step 0 and buries it in Step 4 under '### Subagent Planning (when avail…
  FIX: Define the canonical block once (e.g., in QUANTIS-STYLE.md under a 'Platform Check Pattern' section): '## 0. Platform Check\n**If `invoke_subagent` is available** (CLI `agy`, Standalone): dispatch as described below.\n**If NOT available** (IDE): proceed inline from Step 1.\n> Detection is automatic.…
[MEDIUM] wf-discuss-phase Step 0 dispatch prompt has no return format and omits two of the workflow's three required files | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-discuss-phase/SKILL.md:22-32 (vs 14-17)
  NOW: The `research` subagent is told to explore the codebase, read 'the phase definition from ROADMAP.md', gather dependency context, and 'Return a structured context brief' — but no brief structure is defined, and DECISIONS.md and SPEC.md (both listed un…
  FIX: Replace the Step 0 bullet list with: '- Read `.quantis/ROADMAP.md` (phase definition), `.quantis/DECISIONS.md` (decisions already made — do not reopen these), and `.quantis/SPEC.md` (requirements)\n- Explore the codebase (file structure, recent commits, code relevant to the phase)\n- Return a contex…
[MEDIUM] wf-research-phase — the most context-polluting workflow — has no Step 0 dispatch, unlike all its siblings | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-research-phase/SKILL.md:19-49
  NOW: The workflow runs 'web search, documentation reading, and analysis' inline in the orchestrator, dumping raw search results and docs into the session whose only artifact is a compact RESEARCH.md. Phase 3.2 added 'Subagent dispatch in discuss-phase, pl…
  FIX: Add after Step 1: '## 1.5 Platform Check (L2/L3 only)\n**If `invoke_subagent` is available**, dispatch a `self` subagent with: the phase objective from ROADMAP.md, existing DECISIONS.md entries, the questions to investigate, and the RESEARCH.md template from Step 3. The subagent performs the web/doc…
[MEDIUM] wf-plan does Step 3 research inline even on platforms where it dispatches Step 4 planning | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:66-79
  NOW: Step 3 ('If research needed... Create RESEARCH.md inside `$PHASE_DIR/`') is performed by the orchestrator with no dispatch option, immediately before Step 4 dispatches the actual planning to a subagent. On CLI, the orchestrator therefore absorbs all …
  FIX: In Step 3, after the discovery-level list, add: '**If `invoke_subagent` is available and level is L2/L3:** dispatch a `self` subagent with the phase objective, the questions to answer, and the RESEARCH.md template; it writes `$PHASE_DIR/RESEARCH.md` and returns a 10-line summary. Do not perform L2/L…
[MEDIUM] wf-map runs five read-heavy analysis domains inline; never uses the built-in read-only research subagent | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-map/SKILL.md:20-34
  NOW: The workflow has the orchestrator 'Read and follow `.agents/skills/codebase-mapper/SKILL.md` exactly' across structure, dependency, pattern, integration, and technical-debt analysis — wholesale codebase reading in the orchestrator — to produce two su…
  FIX: Add before Step 2: '## 1.5 Platform Check\n**If `invoke_subagent` is available**, dispatch a `research` subagent with the codebase-mapper skill instructions (`.agents/skills/codebase-mapper/SKILL.md`) and the output templates for `.quantis/ARCHITECTURE.md` and `.quantis/STACK.md`. The subagent explo…
[MEDIUM] wf-debug-issue and systematic-debugging never mention subagents despite debugging being the system's acknowledged context killer | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-debug-issue/SKILL.md:64-79, 204-219 (also systematic-debugging/SKILL.md:50-121)
  NOW: Quantis' entire context-hygiene apparatus (3-strike rule, /pause, context-health-monitor, wf-execute's <context_hygiene> block) exists because debugging pollutes the orchestrator — yet wf-debug-issue's evidence gathering (Step 3) and hypothesis testi…
  FIX: Add to wf-debug-issue after Step 4: '**If `invoke_subagent` is available:** dispatch one subagent per hypothesis test (sequential if they share state, parallel if independent — read `.agents/skills/dispatching-parallel-agents/SKILL.md` for the parallel case). Give each: the symptom from DEBUG.md, th…
[MEDIUM] executing-plans claims 'Antigravity 2.0 supports them natively', contradicting wf-execute's routing for the IDE platform | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/executing-plans/SKILL.md:14
  NOW: wf-execute routes models WITHOUT invoke_subagent (the IDE platform) to executing-plans. The first thing executing-plans tells that model is: 'When subagents are available (Antigravity 2.0 supports them natively), use subagent-driven-development inste…
  FIX: diff: -**Note:** When subagents are available (Antigravity 2.0 supports them natively), use subagent-driven-development instead of this skill for better quality via two-stage review. +**Note:** If the `invoke_subagent` tool is available in your environment (CLI `agy`, Standalone), use subagent-drive…
[MEDIUM] requesting-code-review has no fallback for platforms without invoke_subagent, despite review being 'Mandatory' | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/requesting-code-review/SKILL.md:14-17, 30-34
  NOW: The skill makes review mandatory ('After each task in subagent-driven development / After completing major feature / Before merge to main') and the only dispatch instruction is 'Use `invoke_subagent` with the code reviewer prompt template at `code-re…
  FIX: Add after '## How to Request': '**Platform check:** If `invoke_subagent` is NOT available (IDE), run the review inline: read `code-reviewer.md`, generate the diff (`git diff BASE_SHA..HEAD_SHA`), and evaluate it against the template's criteria as if you were the reviewer. Inline review loses fresh-c…
[MEDIUM] wf-execute's Step 5 phase-goal verification runs inline at peak orchestrator pollution, with no dispatch option | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:85-94
  NOW: After all waves complete — when the orchestrator's context is at its heaviest from wave management and review loops — Step 5 has it 'Check must-haves against **actual codebase** (not SUMMARY claims)' and create gap closure plans inline. This is exact…
  FIX: In Step 5, before item 1, add: '**If `invoke_subagent` is available:** dispatch a `self` subagent with the phase goal and must-haves from ROADMAP.md, the verification commands, and instruction to check the actual codebase and return PASS/FAIL per must-have with command-output evidence. Verification …
[MEDIUM] wf-verify orchestrator is not required to independently verify the subagent's claims, contradicting verification-before-completion | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:47-50 (vs verification-before-completion/SKILL.md:49, 102-105)
  NOW: Step 0 has the orchestrator review the subagent's VERIFICATION.md and 'check that evidence is empirical (not claims)' — but reading a report's pasted command output is still trusting an agent report. verification-before-completion explicitly lists 'A…
  FIX: In Step 0, change orchestrator item 2 to: '2. Checks that evidence is empirical (not claims) — independently re-run at least one PASS verification command and confirm the output matches the report (verification-before-completion: never trust an agent report without independent verification)'.
[LOW] wf-audit-milestone reads an entire milestone archive inline with no dispatch option | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-audit-milestone/SKILL.md:13-80
  NOW: The audit loads the milestone archive, every phase's VERIFICATION.md, TODO.md, and DECISIONS.md, and re-checks must-have evidence — all inline — to produce one compact report. For a multi-phase milestone this is a large read volume into the orchestra…
  FIX: Add a '## 0. Platform Check' using the canonical block: dispatch a `research` subagent with the milestone name, the list of files to read (archive dir, each phase's VERIFICATION.md, TODO.md, DECISIONS.md), and the Step 5 audit-report template as the required return format. Orchestrator reviews and p…
[LOW] SDD Session Setup assumes define_subagent exists wherever invoke_subagent does, with no fallback | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/subagent-driven-development/SKILL.md:30-42
  NOW: Platform detection gates only on invoke_subagent, but Session Setup unconditionally instructs 'define the named subagent types at session start' via define_subagent. If a platform exposes invoke_subagent without define_subagent (the skill never estab…
  FIX: Add after the define_subagent block: 'If `define_subagent` is not available but `invoke_subagent` is, skip type registration: prepend the relevant prompt template contents (implementer-prompt.md, etc.) directly to each `invoke_subagent` call.'

## state-management (18)
[CRITICAL] wf-execute reads plan fields (wave frontmatter, <task>/<verify> blocks) that its delegated writer writing-plans never produces | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:53, 77; cf. writing-plans/SKILL.md:49-104
  NOW: wf-execute Step 3 says "Read `wave` field from each plan's YAML frontmatter" and Step 4.3 says "Follow `<task>` blocks in order, run `<verify>` commands". But wf-plan delegates plan authoring to writing-plans (wf-plan:155), whose prescribed plan form…
  FIX: In writing-plans 'Plan Document Header', prepend the required frontmatter: ```markdown --- phase: {N} plan: {M} wave: {W} gap_closure: false --- # Phase {N}.{M}: {Description} - Implementation Plan ``` Then in wf-execute Step 4.3 replace: - `3. Follow `<task>` blocks in order, run `<verify>` command…
[CRITICAL] wf-execute marks an unplanned phase '✅ Complete' in ROADMAP.md when the phase directory contains no PLAN files | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:48-50, 96-105
  NOW: Step 2 builds the list of incomplete plans and says "If no incomplete plans: phase already complete, skip to step 6." A phase directory that exists but contains zero PLAN.md files (e.g. only SPEC.md — exactly the state of .quantis/phases/3.2-cli-firs…
  FIX: In Step 2 replace: - `If no incomplete plans: phase already complete, skip to step 6.` + `If NO *-PLAN.md files exist at all: STOP — output "Error: no plans found for phase {N}. Run /plan {N} first." Do not touch ROADMAP.md or STATE.md.` + `If plans exist and every PLAN has a matching SUMMARY: phase…
[CRITICAL] SUMMARY file naming collision: wf-execute writes one phase-named SUMMARY per plan, breaking its own PLAN↔SUMMARY completion matching | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:48, 79; cf. .quantis/templates/SUMMARY.md:3
  NOW: Step 4.5 says "After all tasks in plan: create `$PHASE-SUMMARY.md` inside `$PHASE_DIR/`" — the filename is derived from the PHASE, not the plan, so with multiple plans per phase (e.g. 3.1-PLAN-A.md and 3.1-PLAN-B.md, both present in .quantis/phases/3…
  FIX: In Step 4.5 replace: - `After all tasks in plan: create `$PHASE-SUMMARY.md` inside `$PHASE_DIR/`` + `After all tasks in plan: create the matching summary `${PLAN_FILE%-PLAN.md}-SUMMARY.md` inside `$PHASE_DIR/` (e.g. 3.1-A-PLAN.md → 3.1-A-SUMMARY.md)` And in Step 2 define the match explicitly: a plan…
[CRITICAL] Phase-completion ownership is split: wf-execute marks ROADMAP '✅ Complete' before independent verification, and wf-verify never writes ROADM… | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:224-302; cf. wf-execute/SKILL.md:98
  NOW: wf-execute Step 6 writes "**ROADMAP.md:** Mark phase `✅ Complete`" after its own lighter goal-check. wf-verify, the dedicated auditor, updates only STATE.md and only in the PASS branch (lines 228-233). The FAIL branch (lines 254-302) creates gap-clos…
  FIX: In wf-verify Step 5 FAIL branch, before the output banner add: ```markdown Update `.quantis/STATE.md`: - **Phase**: {N} - **Status**: ⚠ Verification failed — {Z} gaps - **Next Steps**: 1. /execute {N} --gaps-only Update ROADMAP.md: set phase status to `🔄 Gap closure`. ``` In the PASS branch add: `Up…
[HIGH] ROADMAP phase statuses '🔄 In Progress' and 'planned' are read by wf-progress/wf-plan but no workflow ever writes them | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-progress/SKILL.md:24-29, 49; cf. wf-plan/SKILL.md:16,57,177
  NOW: wf-progress counts "In progress (🔄)" and "Blocked (⏸️)" phases and renders "🔄 Phase 3: {Name} ← CURRENT". wf-plan and wf-discuss-phase "auto-detect next unplanned phase from ROADMAP.md". But the only ROADMAP status transition any workflow performs is…
  FIX: wf-plan Step 6: add `Update ROADMAP.md: set phase Status to 🗺️ Planned`. wf-plan/wf-discuss-phase: change "auto-detect next unplanned phase from ROADMAP.md" to "next phase whose directory under .quantis/phases/ has no *-PLAN.md". wf-execute Step 3: add `Set phase Status to 🔄 In Progress in ROADMAP.m…
[HIGH] Four incompatible STATE.md schemas plus two appended-section formats, with no replace-vs-append rule; wf-resume-session can only parse one o… | /Users/piemelel/GitHub/Coding-Workflow/.quantis/templates/state.md:10-58; cf. wf-pause:24-65,168-174; wf-new-milestone:82-87; wf-verify:228-233; context-health-monitor:62-84; actual .quantis/STATE.md:1-4
  NOW: The template prescribes frontmatter (`updated:`) plus Current Position fields Milestone/Phase/Status/Plan and an 'Active Decisions' table. wf-pause writes Phase/Task/Status plus Last Session Summary/In-Progress Work/Blockers/Context Dump/Next Steps (…
  FIX: Adopt wf-pause's schema as canonical in templates/state.md (it is what resume-session parses and what the real file uses). Change wf-verify's PASS snippet to update only the Status line inside the existing Current Position section. Change context-health-monitor and wf-pause auto-save to write their …
[HIGH] DECISIONS.md has two conflicting prescribed formats (D-{NNN} vs [DECISION-XXX]) and two writers with no format at all | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-discuss-phase/SKILL.md:52-57; cf. .quantis/templates/decisions.md:7-27; wf-stress-test:102; context-health-monitor:53-57
  NOW: wf-discuss-phase prescribes `### D-{NNN}: {Decision Title}` with Decision/Rationale only. The template prescribes `## [DECISION-XXX] Title` with Date/Status/Context/Decision/Rationale/Consequences/Alternatives. wf-stress-test Step 5 ("Document decisi…
  FIX: Make D-{NNN} canonical (matches actual usage). Update templates/decisions.md's template block to the wf-discuss-phase format, and add to wf-stress-test:102 and context-health-monitor Rule 3: 'Use the D-{NNN} format from .quantis/templates/decisions.md; NNN = highest existing ID across .quantis/DECIS…
[HIGH] JOURNAL.md entry granularity and ordering disagree between writers, making wf-resume-session's 'last entry' read ambiguous | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-resume-session/SKILL.md:58-64; cf. subagent-driven-development:313; executing-plans:83-89; wf-pause:71-92; actual .quantis/JOURNAL.md:3
  NOW: SDD says add a JOURNAL entry after EVERY task ("task name, files changed, verification result" — no Session/Objective/Handoff structure). executing-plans says "Append session entry after completing all tasks". wf-pause "creates" a structured session …
  FIX: In SDD's Quantis State Integration change item 2 to: 'JOURNAL.md — accumulate task results; write ONE session entry (template: .quantis/templates/journal.md) when execution ends or pauses'. In executing-plans:83 change 'Append session entry' to 'Insert session entry at the top of JOURNAL.md (newest …
[HIGH] Two SPEC.md locations: brainstorming writes the spec into the phase directory while wf-plan's Planning Lock and wf-verify read the root .qua… | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/brainstorming/SKILL.md:29, 111; cf. wf-plan:52, wf-verify:32,69
  NOW: brainstorming (invoked by wf-discuss-phase) saves the design doc to `.quantis/phases/{N}.{M}-{slug}/SPEC.md`. wf-plan gates on `grep -q "FINALIZED" ".quantis/SPEC.md"` (root) and feeds 'SPEC.md requirements' to writing-plans without saying which one.…
  FIX: Add to wf-plan Step 4 context list and wf-verify Step 1 reads: + `- Phase design from "$PHASE_DIR/SPEC.md" (if it exists — takes precedence for phase-level requirements; root .quantis/SPEC.md remains the project-level reference)` And in brainstorming line 111 add: 'This is the PHASE spec; it does no…
[HIGH] wf-new-milestone resets DECISIONS.md and JOURNAL.md by overwriting them without archiving — silent data loss if /complete-milestone wasn't r… | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-new-milestone/SKILL.md:91-110
  NOW: Step 5b truncates DECISIONS.md and JOURNAL.md to a bare header whenever they exceed 5 lines (`printf '# Decisions\n\n---\n' > .quantis/DECISIONS.md`). Unlike wf-complete-milestone (lines 78-79), which copies both files into `.quantis/milestones/{name…
  FIX: In Step 5b, before each printf add: ```bash PREV=$(ls .quantis/milestones/ 2>/dev/null | tail -n 1) mkdir -p ".quantis/milestones/${PREV:-pre-$(date +%Y%m%d)}" cp .quantis/DECISIONS.md ".quantis/milestones/${PREV:-pre-$(date +%Y%m%d)}/DECISIONS.md" ``` (same for JOURNAL.md), and update the reset hea…
[MEDIUM] Gap-closure plan creation has two owners: wf-verify creates fix plans directly AND wf-plan --gaps exists to do the same from VERIFICATION.md | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-plan/SKILL.md:21; cf. wf-verify:254-282
  NOW: wf-plan documents a `--gaps` flag: "Gap closure mode (reads VERIFICATION.md, skips research)" — implying /plan turns verification gaps into plans. But wf-verify's FAIL branch already says "Create gap closure plans" itself, writing `gap_closure: true`…
  FIX: Simplest: delete the `--gaps` flag from wf-plan (line 21 and the step-3 mention) since wf-verify owns gap plans end-to-end. Alternatively, replace wf-verify's 'Create gap closure plans' block with: 'Run /plan {N} --gaps to generate fix plans from VERIFICATION.md' and specify in wf-plan how --gaps ma…
[MEDIUM] wf-complete-milestone's completion check greps text labels that don't match what writers produce (case, icons, multi-milestone accumulation) | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-complete-milestone/SKILL.md:16-19, 86; cf. .quantis/ROADMAP.md:6,181
  NOW: Step 1 runs `grep -E "Status.*Not Started|Status.*In Progress"` over the whole ROADMAP.md. Three mismatches with reality: (1) the actual roadmap accumulates ALL past milestones (v2.0 through v3.3) instead of being cleared per Step 5's "Clear ROADMAP.…
  FIX: Replace Step 1 with: extract the current-milestone section first (`sed -n '/## Milestone: {name}/,/^## Milestone:/p' .quantis/ROADMAP.md`), then `grep -E "Status.*:?[[:space:]]*(⬜|🔄|Not Started|In [Pp]rogress)"` on that slice only. In Step 5, replace 'Clear ROADMAP.md phases section' with the policy…
[MEDIUM] Bare 'Update STATE.md' instructions with no fields in four workflows — each model invocation invents its own update | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-discuss-phase/SKILL.md:59-60; cf. wf-stress-test:105, wf-map:40, wf-complete-milestone:87
  NOW: wf-discuss-phase Step 4 says only "Update STATE.md. Suggest `/plan {N}` or `/stress-test`." wf-stress-test Step 6 says only "Update STATE.md." wf-map says "Update STATE.md." wf-complete-milestone Step 5 says "Update STATE.md to show milestone complet…
  FIX: Example for wf-discuss-phase Step 4: ```markdown Update `.quantis/STATE.md` Current Position: - **Phase**: {N} ({name}) - **Task**: Discussion complete — {X} decisions recorded - **Status**: Discussed, ready to plan And set Next Steps: 1. /plan {N} ``` Add analogous field lists to wf-stress-test Ste…
[MEDIUM] BROWSER-VERIFY.md is written but never committed and no workflow ever reads it back; VERIFICATION.md verdict can be finalized before browser… | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-verify/SKILL.md:129-180, 189-193, 306-311
  NOW: Without browser_subagent, Step 3d writes `"$PHASE_DIR/BROWSER-VERIFY.md"` as a handoff and tells the IDE session to "Update VERIFICATION.md with browser evidence." But Step 4 writes VERIFICATION.md with a mandatory `verdict: PASS | FAIL | PARTIAL` fr…
  FIX: In Step 3d add: 'Record each browser-pending must-have in VERIFICATION.md with Status: PENDING-BROWSER and set verdict to PARTIAL.' Change Step 6 to: ```bash git add "$PHASE_DIR/VERIFICATION.md" "$PHASE_DIR/BROWSER-VERIFY.md" 2>/dev/null ``` And in Step 5 PASS gate: 'PASS requires zero PENDING-BROWS…
[MEDIUM] wf-pause never syncs ROADMAP.md, so deliverables completed mid-session are lost from the roadmap on handoff (own roadmap flags this as an op… | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-pause/SKILL.md:20-103; cf. .quantis/ROADMAP.md:188
  NOW: wf-pause writes STATE.md and JOURNAL.md and commits exactly those two files (`git add .quantis/STATE.md .quantis/JOURNAL.md`). ROADMAP.md deliverable checkboxes are only updated by SDD/executing-plans during execution or by wf-execute at phase end — …
  FIX: Add Step 1.5 to wf-pause: ```markdown ## 1.5 Sync ROADMAP For each item in this session's Accomplished list, check the matching deliverable/checkbox in .quantis/ROADMAP.md. Update phase Status if all deliverables are now checked. ``` and extend Step 3 to `git add .quantis/STATE.md .quantis/JOURNAL.m…
[LOW] wf-execute updates REQUIREMENTS.md but excludes it from the state commit; responsibility also overlaps wf-complete-milestone with different … | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-execute/SKILL.md:96-105; cf. wf-complete-milestone:124-132
  NOW: Step 6 instructs "**REQUIREMENTS.md** (if exists): Cross-reference completed tasks with requirement IDs" but the commit below stages only `.quantis/ROADMAP.md .quantis/STATE.md "$PHASE_DIR/"` — the REQUIREMENTS.md edit is left uncommitted, so the req…
  FIX: Change the Step 6 commit to: ```bash git add .quantis/ROADMAP.md .quantis/STATE.md "$PHASE_DIR/" [ -f .quantis/REQUIREMENTS.md ] && git add .quantis/REQUIREMENTS.md ``` and specify the write: 'set each satisfied requirement's Status to `Implemented (Phase {N})`'; wf-complete-milestone 5d then upgrad…
[LOW] Template fields no workflow ever writes or reads: STATE 'Active Decisions' table, ROADMAP 'Progress Summary'/'Timeline' tables, SUMMARY dura… | /Users/piemelel/GitHub/Coding-Workflow/.quantis/templates/state.md:33-39; cf. templates/roadmap.md:67-85, templates/SUMMARY.md:10-15,78-83
  NOW: templates/state.md defines an 'Active Decisions' table inside STATE.md — no workflow writes it (decisions go to DECISIONS.md via wf-discuss-phase/wf-stress-test; wf-pause writes 'Decisions Made' bullets under Context Dump instead) and no reader (wf-r…
  FIX: Delete the 'Active Decisions' table from templates/state.md (replace with: 'Decisions live in DECISIONS.md; reference IDs here if relevant'). Delete 'Progress Summary' and 'Timeline' from templates/roadmap.md (wf-progress derives these live). Drop `duration_minutes` and the Metadata block from templ…
[LOW] wf-progress displays the project name 'from SPEC.md' but its load step never reads SPEC.md | /Users/piemelel/GitHub/Coding-Workflow/.agents/skills/wf-progress/SKILL.md:14-18, 40
  NOW: Step 1 ('Load Current State') reads only `.quantis/STATE.md` and `.quantis/ROADMAP.md`, but the Step 3 display template contains "Project: {project name from SPEC.md}" — a field sourced from a file the workflow never loads. A model either silently sk…
  FIX: In Step 1 add: + `- `.quantis/SPEC.md` — project name (first heading) and Status line only (targeted read, not the full file)`


# FINDING VERDICTS (23 sampled before stop)

- real=True cite-ok=True sev→critical | All cited quotes verified exactly: wf-discuss-phase/SKILL.md L42 delegates 'in discuss/explore mode' (a mode that does not exist anywhere in brainstorming/SKILL.md), while brainstorming L22+L29-32 mandate a 9-item checkl…
- real=True cite-ok=True sev→high | Primary citations are exact: wf-verify/SKILL.md L36 (skill reference), L38 (blockquote 'read and follow'), L44 (subagent bullet with no path), <context> opening L28 with no closing tag anywhere (grep count 0), <process> …
- real=True cite-ok=True sev→high | All quotes verified exactly: wf-execute L75 routes no-invoke_subagent to executing-plans; SDD L22 routes the identical condition to inline SDD mode with two-stage self-review; writing-plans L147 hands off to SDD (user-ch…
- real=True cite-ok=True sev→high | Verified exactly: wf-stress-test/SKILL.md L7 header, L9 blurb, and L44 directive all reference a brainstorming 'critique mode'; grep confirms zero occurrences of 'critique' anywhere in brainstorming/SKILL.md, which is a …
- real=True cite-ok=True sev→medium | Evidence verified verbatim: wf-plan/SKILL.md L141 reduces writing-plans to one of seven context bullets in the subagent branch, while the bolded 'Read and follow ... exactly' (L155) only executes when invoke_subagent is …
- real=True cite-ok=True sev→high | All quotes verified exactly: writing-plans/SKILL.md L134-152 ends the skill with an execution-choice menu ("Which approach?" L144) and in-session execution branches, while wf-execute/SKILL.md L76 forbids exactly that men…
- real=True cite-ok=True sev→medium | Verified by independent grep and read: <context> opens at line 28 of wf-verify/SKILL.md with no closing </context> anywhere; <process> spans 54-313; '## 0. Platform Check' (40-52) and the verification-before-completion d…
- real=True cite-ok=True sev→medium | Evidence verified: L30 'The brainstorming skill in adversarial critique mode' (no path/read instruction) and L38 inline-fallback are exact; the Step 2 read with path (L44) is indeed skipped on the subagent path, since St…
- real=True cite-ok=True sev→medium | Verified: wf-complete-milestone/SKILL.md L30-35 contains exactly the cited three-bullet verification step, and grep confirms 'verification-before-completion' appears nowhere in the file, while wf-verify/SKILL.md L36-38 e…
- real=True cite-ok=True sev→low | The cited quotes are accurate (the /execute row is at line 109, within the cited 104-121 range; evidence said L110, off by one). The inconsistency is real: using-quantis maps /execute to executing-plans only, while wf-ex…
- real=True cite-ok=True sev→medium | Verified: line 210 of wf-plan/SKILL.md contains the exact quoted row, and Step 3 (lines 66-79) defines the L0-L3 research framework with no methodology reference — 'brainstorming' appears only in the <related> metadata. …
- real=False cite-ok=True sev→not-an-issue | Quote is verbatim at wf-pause/SKILL.md L161, but the finding's core claim is wrong: the warning signals a model needs for the Auto-Save Protocol are defined in the 'When to Auto-Save' table at L153-157 of the same file (…
- real=True cite-ok=True sev→low | Citation verified: wf-verify L7 is '# /verify Workflow' with no blockquote, while wf-plan/wf-discuss-phase/wf-execute/wf-stress-test (and also wf-map, wf-research-phase, wf-update-plan) use the '# /cmd → skill' + 'Skill-…
- real=True cite-ok=True sev→medium | Evidence quote verified verbatim at QUANTIS-STYLE.md L28-33; the entire 273-line style guide defines no delegation idiom. All spot-checked matrix citations are accurate, including subtle claims: brainstorming has no 'dis…
- real=False cite-ok=True sev→not-an-issue | Citations are accurate (wf-execute/SKILL.md L144-145 rows exist; Step 5 L85-94 and context_hygiene L109-114 are inline with no skill load), but the finding mischaracterizes deliberate design as an anti-pattern. (1) The <…
- real=False cite-ok=True sev→not-an-issue | Citation is accurate (L354 contains the quoted row, no other mention of context-health in the file), but the finding fails on its premise. (1) The <related> section is navigational metadata by design: the same block list…
- real=True cite-ok=True sev→low | Evidence verified: wf-execute/SKILL.md L67-78 nests methodology selection and the 'Read and follow' instruction inside the per-plan loop exactly as quoted. The structural issue is real and has a concrete consistency basi…
- real=False cite-ok=True sev→low | Quotes are accurate, but the central claim — that brainstorming's phase-folder SPEC.md causes /plan's Planning Lock to error — is refuted by the workflow chain. The two locations are two different artifacts by design: .q…
- real=True cite-ok=True sev→high | Confirmed cross-file contract mismatch: wf-execute:53 instructs reading a `wave` YAML frontmatter field, but writing-plans:45-61 mandates a frontmatter-less header ("Every plan MUST start with this header", H1 + Goal/Arc…
- real=False cite-ok=True sev→low | Quotes verify exactly at all four cited locations, but the finding's core claim ("never defined anywhere; only definition mechanism is SDD's define_subagent") is false. .agents/skills/using-quantis/references/antigravity…
- real=True cite-ok=True sev→critical | Evidence quote is verbatim at wf-discuss-phase/SKILL.md:42. brainstorming/SKILL.md defines no modes (grep confirms 'discuss/explore mode' exists nowhere except the citing line; same phantom-mode pattern appears in wf-str…
- real=True cite-ok=True sev→high | Evidence quote matches wf-stress-test/SKILL.md line 44 exactly; lines 30-31 also match. brainstorming/SKILL.md verifiably contains no critique methodology or any 'mode' — it is a collaborative design skill with contradic…
- real=True cite-ok=True sev→high | Verified, could not refute. Evidence quote is verbatim at writing-plans/SKILL.md:18 and all auxiliary citations (brainstorming:29, using-quantis:129-130, wf-plan:63/164/199) check out. The ambiguity is real and the repo …
- real=True cite-ok=True sev→medium | Evidence quote is verbatim at wf-execute/SKILL.md:40 and wf-verify/SKILL.md:60, and on-disk dirs (3.1-workflow-reliability-fixes, 3.2-cli-first-migration) confirm the {N}.{M}-{slug} naming, so '/execute 3' does fail to m…
- real=True cite-ok=True sev→high | Evidence quote is verbatim at line 79 and the 'PLAN without matching SUMMARY' logic is at lines 45-48 of wf-execute/SKILL.md. The finding survives refutation: line 79 is inside the per-plan loop (line 69) yet names the s…
- real=True cite-ok=True sev→high | Verified: writing-plans/SKILL.md:134-152 instructs the model to 'offer execution choice' ending with '**Which approach?**' (line 144), while wf-execute/SKILL.md:72-76 says methodology selection is 'NOT a choice. Do not p…
- real=True cite-ok=True sev→high | Verified: wf-plan:155 mandates following writing-plans "exactly", which produces markdown checkbox plans with Run:/Expected: lines (writing-plans/SKILL.md:63-104), yet wf-plan:170-171 checks for `<verify>`/`<done>` XML t…
- real=True cite-ok=True sev→high | Evidence quote is verbatim at subagent-driven-development/SKILL.md:112; terminal nodes at lines 93-94/113 and wf-execute:67-105 match the finding. The conflict is real: wf-execute:74 instructs "Read and follow" SDD per p…
