---
phase: 3.5
plan: install-footprint
wave: 1
gap_closure: false
---

# Phase 3.5 · Install/Update Footprint — Implementation Plan

> **Status**: ✅ Implemented & verified (2026-06-13). Bugfix — applied directly (single-author for a consistent footprint), then verified.

**Goal:** Stop `/wf-update` from clobbering a project's `README`/`MANIFEST`/`CHANGELOG`, and stop polluting the project root with a bare `VERSION`. README/MANIFEST/CHANGELOG = Quantis source-only; version marker → `.quantis/VERSION`.

---

## Task 1: `MANIFEST.md` — README/MANIFEST/CHANGELOG marked source-only

- [x] Replace "### Root Files (project root)" (which listed CHANGELOG/MANIFEST/README/VERSION as Core → "replace in project") with two sections: a **"Version marker"** (`.quantis/VERSION`) and **"Quantis SOURCE-ONLY — NEVER copied into a target project"** (README.md, MANIFEST.md, CHANGELOG.md).
- [x] **Verify:** `grep -c "SOURCE-ONLY\|NEVER copied" MANIFEST.md` → ≥1; `grep -c "^- VERSION$" MANIFEST.md` → 0

## Task 2: `wf-update/SKILL.md` — explicit prose guard

- [x] After the "Only replace files in Core sections" line, add: "NEVER copy README/MANIFEST/CHANGELOG into the project (source-only); the only version marker is `.quantis/VERSION`; no Quantis files at the project root."
- [x] **Verify:** `grep -c "NEVER copy .README" .agents/skills/wf-update/SKILL.md` → ≥1

## Task 3: Relocate version marker → `.quantis/VERSION` (all 11 refs)

- [x] Move file: root `VERSION` → `.quantis/VERSION` (content bumped to 3.4.2).
- [x] `scripts/install.sh` + `scripts/upgrade.sh`: `cp "$TEMP_DIR/VERSION" ./` → `cp "$TEMP_DIR/.quantis/VERSION" .quantis/`.
- [x] `wf-install/SKILL.md`: `cp …/VERSION ./` → `…/.quantis/VERSION .quantis/`.
- [x] `wf-quantis-help/SKILL.md`: `cat VERSION` → `cat .quantis/VERSION`.
- [x] `wf-update/SKILL.md` (×3): version-check block, remote_version, `cp "$SOURCE/VERSION"` → `.quantis/VERSION`.
- [x] `wf-upgrade/SKILL.md` (×2): `cp "$SOURCE/VERSION"` + `cat VERSION` → `.quantis/VERSION`.
- [x] `README.md` layout: VERSION → `.quantis/VERSION`.
- [x] **Verify:** `grep -rnE "\bVERSION\b" scripts/ .agents/ | grep -vE "\.quantis/VERSION|QUANTIS|version"` → no bare-root VERSION file refs remain.

## Task 4: Records + verification

- [x] DECISIONS **D-013**; ROADMAP Phase 3.5; STATE + JOURNAL; CHANGELOG **[3.4.2]**; `.quantis/VERSION` = 3.4.2.
- [x] `bash scripts/validate-all.sh` → green.
