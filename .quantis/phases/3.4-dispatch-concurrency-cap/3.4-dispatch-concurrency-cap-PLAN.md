---
phase: 3.4
plan: dispatch-concurrency-cap
wave: 1
gap_closure: false
---

# Phase 3.4 · Parallel-Dispatch Concurrency Cap & Rate Limits — Implementation Plan

> **Status**: ✅ Implemented & verified (2026-06-13). Quick fix — applied directly (single-author for a consistent contract), then verified.
> **For agentic workers:** REQUIRED SUB-SKILL: Read and follow `.agents/skills/subagent-driven-development/SKILL.md`. Steps use checkbox (`- [ ]`).

**Goal:** Stop the 7-way parallel dispatch from blowing the model rate limit. Cap concurrency at ≤3 (waves), and on a 429 wait-then-retry-once-then-inline — one shared rule, inherited by every fan-out workflow.

**Architecture:** One task per file. Prose/process edits only — no product code.

---

## Task 1: `.agents/skills/dispatching-parallel-agents/SKILL.md` — the Concurrency Cap (canonical)

- [x] Add a **"Concurrency Cap & Rate Limits (REQUIRED)"** section after Platform Detection: ≤3 concurrent (waves of ≤3); on 429/"exhausted capacity" → don't re-dispatch immediately (ignore "reset after 0s"), wait, retry once, then inline; re-dispatch failures one at a time (no thundering herd). Cite D-012.
- [x] Annotate the step-3 example: "≤3 is the cap; for more domains, dispatch in waves."
- [x] **Verify:** `grep -c "Concurrency Cap\|at most 3" dispatching-parallel-agents/SKILL.md` → ≥1

## Task 2: `.agents/skills/wf-stress-test/SKILL.md` — waves of ≤3 + 429-aware retry

- [x] Step 0: "7 ... invoked all together" → "the 7 dimensions ... **in waves of ≤3 concurrent** (per the Concurrency Cap)".
- [x] Retry line: on a `429`/rate-limit error, do NOT re-dispatch immediately — wait, retry once, then inline (per the Rate Limits rule).
- [x] **Verify:** `grep -c "all together" wf-stress-test/SKILL.md` → 0; `grep -c "waves of ≤3" wf-stress-test/SKILL.md` → ≥1

## Task 3: `.agents/skills/wf-research-phase/SKILL.md` — waves of ≤3

- [x] "one research subagent per independent question, all invoked together" → "... **in waves of ≤3 concurrent** (per the Concurrency Cap)".
- [x] **Verify:** `grep -c "all invoked together" wf-research-phase/SKILL.md` → 0

## Task 4: `.agents/skills/wf-map/SKILL.md` — ≤3 concurrent

- [x] "one research subagent per domain, all invoked together" → "... **but ≤3 concurrent** (5 domains = two waves)".
- [x] **Verify:** `grep -c "all invoked together" wf-map/SKILL.md` → 0

## Task 5: `.agents/skills/wf-debug-issue/SKILL.md` — ≤3 concurrent

- [x] "invoke them together when the tests are independent" → "invoke them together (**≤3 concurrent**, per the Concurrency Cap) when ...".
- [x] **Verify:** `grep -c "≤3 concurrent" wf-debug-issue/SKILL.md` → ≥1

## Task 6: `.agents/skills/using-quantis/references/antigravity-tools.md` — document the cap

- [x] Parallel Dispatch note: `invoke_subagent` has no built-in backoff → invoke at most 3 concurrently; send more in waves of ≤3.
- [x] **Verify:** `grep -c "at most 3 concurrently\|waves of ≤3" antigravity-tools.md` → ≥1

## Task 7: Records + verification

- [x] DECISIONS **D-012** recorded.
- [x] ROADMAP Phase 3.4 added; STATE + JOURNAL updated.
- [x] `bash scripts/validate-all.sh` → green; no fan-out workflow still says "all together".

## Task 8: `.agents/skills/wf-verify/SKILL.md` — cap the senior-review reviewer batch (audit follow-up)

- [x] Step 4b: "batch the changed files across multiple reviewer dispatches" → "**in waves of ≤3 concurrent** (per the Concurrency Cap)". A missed fan-out site, caught by the post-implementation 6-agent audit (2026-06-13).
- [x] **Verify:** `grep -c "waves of ≤3 concurrent" .agents/skills/wf-verify/SKILL.md` → ≥1
